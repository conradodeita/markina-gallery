## 1. Contratos de regressão

- [x] 1.1 Adicionar teste frontend inicialmente falho que comprove duas colunas efetivas para `.gallery-presentation-grid` abaixo de 560 px, `grid-column: span 1` por foto e ausência de override tardio para uma coluna; verificar Galeria pública, privada e grupos de resultados faciais que reutilizam o componente. Evidência: `layout-system.test.ts` aprovou 11 contratos existentes e falhou somente ao encontrar override mobile efetivo para `grid-template-columns:1fr`; a busca estática confirma que Galeria pública, privada e resultados faciais reutilizam `GalleryPresentation`.
- [x] 1.2 Adicionar testes backend inicialmente falhos que detectem uma única composição do texto nos três sentidos e nos tamanhos mínimo/máximo, com posições extremas, sombra e grade de segurança; verificar que falham pela repetição atual e não pela fixture. Evidência: os três casos parametrizados (horizontal/10/top-left, diagonal/48/middle-center e vertical/96/bottom-right) falharam no número de composições textuais, mantendo uma composição independente da grade de segurança.

## 2. Implementação visual e proteção

- [x] 2.1 Consolidar as regras compartilhadas da grade da cliente para duas colunas em 320–560 px e expansão fluida em tablet/desktop, preservando proporção, gap compacto, foco, seleção e visualizador; verificar testes frontend e inspeção da cascata final. Evidência: os overrides abaixo de 560 px agora mantêm `repeat(2,minmax(0,1fr))`, gap de 8 px e cada foto em `span 1`; os 27 testes direcionados de layout, apresentação e Galeria pública passaram.
- [x] 2.2 Alterar o pipeline Pillow para compor a camada textual exatamente uma vez na âncora configurada, limitando coordenadas após rotação sem modificar o desenho da grade de segurança; verificar os testes de direção, tamanho, posição, opacidade, sombra e ausência de distorção. Evidência: o mosaico foi substituído por uma composição única com âncora limitada; `test_media.py` aprovou 16/16, incluindo três direções, tamanhos 10/48/96, posições extremas e grade independente.
- [x] 2.3 Garantir que o deploy não reenfileire `client_preview` existentes nem dispare reindexação facial; verificar por busca reproduzível que a mudança fica restrita ao algoritmo chamado por novas gerações ou por reprocessamento administrativo futuro. Evidência: `git diff --name-only` limita implementação a `media.py`, CSS e testes; não há diff em `main.py`, workers faciais, workflows, scripts ou Docker. A reenfileiração continua existindo apenas no endpoint administrativo já vigente e não será chamada pelo deploy.

## 3. Validação e entrega

- [x] 3.1 Executar testes frontend direcionados, lint, typecheck e build, além dos testes backend de mídia e worker; corrigir falhas relacionadas e registrar evidências na task. Evidência: frontend aprovou `30` arquivos/`194` testes, ESLint com `0` erros e `23` avisos preexistentes, TypeScript sem erros e build Next.js `16.3.2`; backend aprovou Ruff e `16/16` testes de mídia/worker. A primeira execução backend ampla concorrente colidiu no SQLite compartilhado; repetida isoladamente, aprovou integralmente.
- [x] 3.2 Executar validação OpenSpec estrita e `git diff --check`; revisar o diff para garantir ausência de mudanças em grids administrativos, API, banco, reconhecimento facial e grade de segurança. Evidência: OpenSpec estrito e `git diff --check` aprovados; o diff de implementação contém somente `backend/app/media.py`, seu teste, `frontend/app/globals.css` e o teste estrutural. O bloco da grade de segurança não foi modificado e não há alteração em grids administrativos, API, modelos/migrations, facial, Docker ou automação.
- [x] 3.3 Preparar roteiro de homologação para viewports de 320, 360 e 390 px e para uma nova prévia com marca textual única, registrando que o acervo existente não será reenfileirado; executar deploy já autorizado sem backfill e manter o aceite visual pendente de revisão humana. Evidência: `homologation-checklist.md` cobre os três viewports e uma nova prévia; o workflow `34433083251` publicou o merge SHA `40cde8fd955895fae366484d6899f014b2a951bc`, confirmou `FACIAL_PROCESSING_ENABLED=true`, workers saudáveis e migration `0052 -> 0052`; o inventário final preservou `637` registros/fonte e os três endpoints externos responderam HTTP 200. Detalhes em `deployment-result.md`; somente o aceite visual humano permanece pendente antes de sincronizar/arquivar.
