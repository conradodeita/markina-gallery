## 1. Fundação global de layout

- [x] 1.1 Inventariar e classificar todos os `max-width`, `container`, larguras fixas e cálculos de centralização do frontend como estruturais, legibilidade ou viewport, registrar o resultado na change e verificar que cada ocorrência possui destino definido antes da edição. Evidência: `layout-inventory.md` registra origem, classificação e destino; buscas `rg` em CSS/TSX/TS confirmaram ausência de utilities estruturais adicionais no código-fonte.
- [x] 1.2 Adicionar testes estruturais para os shells administrativo, cliente e autenticação cobrindo largura total, gutters adaptativos e ausência dos limites globais de 840/960/1180 px, e verificar que os testes falham pelo comportamento atual antes da implementação. Evidência: `npx vitest run app/layout-system.test.ts --reporter=verbose` falhou inicialmente em 7/7 cenários contra os limites antigos.
- [x] 1.3 Criar tokens compartilhados de gutter e converter `admin-shell`, `admin-content`, `client-content`, topbars e canvas de autenticação para largura fluida, preservando limites internos legítimos de formulário/texto, e verificar os testes estruturais. Evidência: o mesmo comando passou nos 7 cenários de shell após `--mk-page-gutter` e as regras full-width.
- [x] 1.4 Revisar componentes compartilhados de seção, cards, listas, tabelas, barras flutuantes, diálogos e mídias para remover limites estruturais redundantes, aplicar `min-width: 0` e enquadramento proporcional, e verificar com testes frontend direcionados e busca pelo inventário classificado. Evidência: contrato estrutural compartilhado em `globals.css`, reset de `min-width` e contenção de mídia validados por `layout-system.test.ts`.
- [x] 1.5 Tornar grids compartilhados responsivos ao espaço disponível com `auto-fit`/`auto-fill` ou breakpoints apropriados, preservando ordem DOM e densidade mínima útil, e verificar em testes nas larguras de smartphone, tablet, notebook e desktop largo sem overflow horizontal. Evidência: grids de diretório, galeria pública e apresentação usam `auto-fit` no desktop e breakpoints de 900/560 px; 10/10 testes estruturais passaram.
- [ ] 1.6 Revisar as rotas administrativas, da cliente e de autenticação que ainda possuam wrappers locais de largura, remover apenas os limites estruturais não cobertos pelos compartilhados e verificar que não restam grandes áreas laterais vazias nas quatro viewports de referência.

## 2. Capa dedicada na etapa 03

- [x] 2.1 Adicionar testes backend para Detalhes sem varredura de fotos `content`, retorno de no máximo a capa vigente, compatibilidade de capa legada e registro imediato da intenção da capa mais recente, e verificar que os novos cenários falham antes da implementação. Evidência: os três testes direcionados falharam inicialmente por `cover_photo_id` nulo, catálogo com item extra e ausência do estado de falha.
- [x] 2.2 Alterar o backend para vincular o ativo aceito como `cover_photo_id`, impedir que conclusão fora de ordem recupere uma capa anterior e retornar somente o estado da capa vigente sem listar o acervo, e verificar os testes backend direcionados. Evidência: `python -m pytest tests/test_gallery_workflow_remediation.py -k "dedicated_cover_upload or details_returns_only or latest_accepted_cover" -q` passou 3/3.
- [x] 2.3 Remover da etapa 03 a galeria de candidatas do acervo e manter apenas upload JPEG do dispositivo, estado de processamento, prévia protegida, substituição e controles de tipografia/cor/tamanho/posição, e verificar com testes do editor que milhares de fotos não são solicitadas nem renderizadas como opções. Evidência: o editor renderiza somente a capa vigente mesmo recebendo opção de conteúdo adicional no mock; testes direcionados passaram e não houve PUT manual em `/cover`.
- [x] 2.4 Cobrir upload concorrente, falha de derivado e capa legada no fluxo integrado, e verificar que o último upload aceito permanece vigente, a falha fica recuperável e nenhuma foto de conteúdo é duplicada ou apagada. Evidência: teste integrado confirma dois ativos preservados, segundo upload vigente, conclusão tardia do primeiro sem sobrescrita e erro recuperável retornado por Detalhes.

## 3. Etapa 04 exclusivamente operacional

- [x] 3.1 Remover dos cards de foto a função e os controles `Usar como capa`/`Capa atual`, sem apenas ocultá-los por CSS, e verificar por teste de componente que eles não existem no DOM nem na navegação por teclado. Evidência: função e JSX foram removidos; teste da etapa 04 comprova ausência no DOM e nenhuma chamada PUT para `/cover`.
- [x] 3.2 Confirmar que ampliação, seleção administrativa, exclusão em massa e estados de upload, prévia, reconhecimento e publicação permanecem funcionais, e verificar com os testes direcionados da etapa 04. Evidência: `npx vitest run app/admin/galleries/gallery-editor.test.tsx --reporter=verbose` passou 46/46, incluindo ampliação, lotes com 2.001 fotos, falha parcial, estados transitórios e publicação.

## 4. Seleção e comentários da cliente

- [x] 4.1 Corrigir a barra flutuante para usar a largura fluida compartilhada e exibir um CTA `Prosseguir` com contraste, nome acessível, foco e destino corretos, e verificar por teste visual/semântico nos estados com e sem seleção. Evidência: CTA com regra de cor/foco de especificidade estável e destino `?mode=review`; testes públicos confirmam ausência sem seleção e dois links acessíveis `Prosseguir` após selecionar.
- [x] 4.2 Separar a revisão da seleção da apresentação editorial, omitindo a capa e mantendo somente fotos selecionadas, filtros e resumo comercial, e verificar por teste de rota que a capa não é renderizada nem contabilizada como item. Evidência: o modo opaco `?mode=review` filtra por seleção persistida, nunca passa `coverUrl` e o teste confirma uma escolhida, uma não escolhida ausente e total comercial de uma foto.
- [x] 4.3 Estender o visualizador compartilhado com um painel contextual opcional para a foto ativa, preservando foco, teclado, swipe, proteção e reutilização nas demais superfícies, e verificar seus testes de interação. Evidência: extensão opcional `renderExpandedPhotoContent`/`onExpandedPhotoChange`, painel após a mídia e suíte compartilhada 6/6 cobrindo foco, setas, fechamento, proteção e superfícies sem painel.
- [x] 4.4 Mover lista, formulário e exclusão de comentários para abaixo da foto ampliada na revisão, remover o seletor global e impedir mistura entre fotos, e verificar cenários habilitado, desabilitado, troca de foto e exclusão autorizada. Evidência: formulário recebe o ID contextual, seletor global foi removido e os testes confirmam troca sem mistura, POST, DELETE e ausência completa quando desabilitado.
- [ ] 4.5 Validar a jornada completa selecionar → prosseguir → revisar → ampliar → comentar → checkout em desktop, tablet e smartphone, verificando que somente as fotos escolhidas aparecem e que os valores continuam backend-driven.

## 5. Qualidade, documentação e entrega

- [x] 5.1 Executar testes backend/frontend direcionados, suíte aplicável, lint, typecheck e build; investigar regressões causadas pela change e registrar comandos e evidências verificáveis neste artefato. Evidência: frontend `npx vitest run --testTimeout=20000` passou 189/189, `npm run lint` passou e `npm run build` compilou/validou TypeScript; backend `.venv/Scripts/python.exe -m pytest tests -q` passou 456, ignorou 1 e `python -m ruff check app/main.py tests/test_gallery_workflow_remediation.py` passou; o teste integrado da change passou 11/11. Um timeout de contenção a 10 s foi reproduzido como cenário isolado verde e a suíte ampla passou com margem de 20 s.
- [ ] 5.2 Executar revisão visual autenticada em 1920 px, 1440 px, tablet e smartphone para todas as famílias de rota, documentando evidências de uso da área útil, legibilidade, mídia proporcional, ausência de overflow e funcionamento de teclado/foco.
- [x] 5.3 Reconciliar proposal, design, delta specs e tasks com o comportamento final, validar a change em modo estrito e deixar a sincronização das specs principais e o arquivamento pendentes de revisão humana. Evidência: diff revisado contra os três deltas e design; `npx --yes @fission-ai/openspec@1.11.0 validate expand-fluid-layout-and-simplify-gallery-flow --strict --no-interactive` aprovou a change e `git diff --check` não encontrou erro.
- [ ] 5.4 Antes de qualquer publicação, apresentar inventário do Compose `markina-gallery`, portas/subdomínio e plano de impacto zero; somente após autorização humana específica executar deploy em homologação, validar healthchecks e realizar smoke test sem alterar recursos de terceiros.
