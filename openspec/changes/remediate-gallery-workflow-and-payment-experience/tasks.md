## 1. Contratos e segurança de dados

- [x] 1.1 Inventariar os produtores/consumidores de `PhotoFolder`, publicação, capa, resumo, Vendas e Pagamentos e adicionar testes de contrato inicialmente falhos para o comportamento desta change; verificar que nenhuma rota, tela ou job fique sem classificação antes de alterar o modelo. Evidência: `inventory.md`; `pytest tests/test_gallery_workflow_remediation.py -q` falhou nos cinco contratos novos antes da implementação (purpose ausente, publicação inexistente, release legado, upload incremental e allowlist).
- [x] 1.2 Adicionar propósito controlado `content | cover_assets` às pastas com migration aditiva, backfill conservador e unicidade da pasta técnica por Galeria pública; verificar upgrade do zero e sobre `20260831_0032`, downgrade estrutural, contagens preservadas e ausência de pasta técnica nas consultas de conteúdo. Evidência: `20260831_0033`; upgrade do zero e downgrade para `0032` em SQLite temporário; upgrade de base semeada em `0032` preservou 1/1 pasta com backfill `content`; contrato focado verde.
- [x] 1.3 Implementar allowlist compartilhada de tokens tipográficos e validação servidor-side, incluindo famílias locais neutras, editoriais e manuscritas; verificar rejeição de CSS/URL/fonte arbitrária, fallback de valor legado e ausência de requisição externa. Evidência: `app/gallery_visuals.py` e `test_cover_font_uses_controlled_tokens_and_safe_legacy_fallback`.
- [x] 1.4 Expor largura/altura e estados de publicação nos contratos administrativos e autorizados de foto; verificar que os novos campos não revelam storage, original, EXIF/GPS ou foto fora do escopo. Evidência: contratos usam somente `MediaDerivative.width/height`; teste de conteúdo confirma dimensões/estado e ausência de `storage_key`.

## 2. Capa e etapa Vendas

- [x] 2.1 Implementar upload de capa dedicado que cria/reutiliza a pasta técnica e passa pelo pipeline JPEG/derivados existente; verificar MIME, limites, autorização, processamento, idempotência e que a imagem não aparece como conteúdo da pasta pública/privada. Evidência: `POST /cover-photos` + pipeline `/source`; teste dedicado cobre MIME, idempotência, derivados e exclusão de conteúdo.
- [x] 2.2 Expandir a leitura de Detalhes com opções de capa processadas da mesma origem e manter escolha/limpeza sem duplicação de mídia; verificar galeria vazia, capa dedicada, foto de conteúdo, processamento pendente e tentativa cruzada. Evidência: `cover_options` informa origem/estado/dimensões e `PUT /cover` mantém validação de origem e derivado pronto.
- [x] 2.3 Corrigir o contrato frontend da etapa Vendas para consumir `/sales` disponível e extrair um editor controlado de faixas, PIX, mensagem, prazo, favoritos e comentários; verificar carregamento, edição, erro e persistência sem exigir galeria privada. Evidência: `gallery-editor.test.tsx` cobre indisponibilidade conservadora, edição/persistência integral e recuperação de erro sem perder o formulário; TypeScript e ESLint verdes.
- [x] 2.4 Reutilizar o cálculo de faixa/salto comercial do backend e remover a edição comercial legada divergente do fluxo visível; verificar contiguidade, centavos inteiros, confirmação do salto e imutabilidade de pedido existente. Evidência: `pricing-rules.ts`, rota legada somente leitura com encaminhamento à origem, testes frontend de contiguidade/salto/confirmação e 10 testes backend de preço/herança/snapshot verdes.
- [x] 2.5 Implementar na etapa 03 upload/escolha de capa, lista de tipografias e prévia reativa de título; verificar teclado, leitor de tela, erro de upload, retomada e que Organização não aparece nessa etapa. Evidência: opções acessíveis, upload dedicado com estado/erro e polling de processamento, prévia reativa e testes focados de seleção/fonte/cor/retomada; Organização coberta somente na etapa 04.

## 3. Publicação e gestão incremental de pastas

- [x] 3.1 Implementar publicação de pasta/rodada sem `gallery_ids`, promovendo somente fotos prontas e indisponíveis; verificar primeira publicação, repetição idempotente, foto falha, modo `collective_protected` e ausência de `DerivedGalleryPhoto` criada em massa. Evidência: `POST /publish` e teste de promoção pronta/idempotência/ausência de links; a autorização `collective_protected` permanece no contrato de acesso, não na publicação.
- [x] 3.2 Adaptar temporariamente `/release` para payload sem destinos e recusar destinos privados legados sem efeitos parciais; verificar referências privadas preexistentes intactas e mensagem de migração estável. Evidência: teste retorna 410 antes de mutação e confirma pasta/foto/referências intactas.
- [x] 3.3 Permitir upload em pasta `released` com novas fotos inicialmente indisponíveis, preservando as anteriores; verificar processamento, revisão, publicação incremental, falha parcial, exclusão elegível e ausência de exposição antecipada. Evidência: contrato focado registra e envia JPEG em pasta publicada com nova foto indisponível e conteúdo anterior disponível; publicação promove somente derivado pronto.
- [x] 3.4 Mover organização de pastas para a etapa 04 e substituir “Liberar para galerias privadas” por publicação da Galeria pública/rodada; verificar que Imagens não consulta clientes para publicar e que a prévia representa `individual` e `sequential`. Evidência: etapa Imagens salva/representa ambos os modos e publica via `/publish` sem `gallery_ids` nem consulta de clientes; testes focados verdes.
- [x] 3.5 Atualizar cards e workspace de pasta para distinguir publicadas, prontas para publicar, processando e falhas, mantendo upload e exclusão em pasta publicada; verificar estados vazio, bloqueado, sucesso e recuperação. Evidência: cards e fotos usam estados textuais/visuais independentes, upload/publicação permanecem ativos em pasta `released`, exclusão respeita `can_delete`; contratos backend e 27 testes do editor verdes.

## 4. Clientes e resumo da Galeria pública

- [x] 4.1 Expandir a agregação por cliente com `gallery_status`, `commercial_status`, disponíveis, selecionadas e compradas em lote; verificar precedência financeira, prazo expirado, galeria removida e limite constante de consultas. Evidência: agregações agrupadas e teste com sete clientes cobre toda a precedência, expiração, privada removida e teto de 12 statements.
- [x] 4.2 Reconciliar a lista de fotos disponíveis e a criação administrativa de privada com a nova publicação; verificar cliente sem privada, privada existente, seleção zero, foto de outra origem, repetição e atualização dos contadores. Evidência: `test_admin_private_gallery_uses_only_published_photos_without_selecting_them` cobre todos os casos e permanece verde.
- [x] 4.3 Executar o fluxo real de desvinculação com worker, inventário e polling em teste integrado; corrigir bloqueios indevidos e verificar sucesso, `pending_review`, falha/retry, cadastro/histórico preservados e outra galeria intacta. Evidência: testes `unlink_client_is_idempotent_scoped_and_preserves_history`, política comercial em revisão e contrato de falha/retry executados sequencialmente: 3 verdes.
- [x] 4.4 Extrair um card acessível de cliente reutilizado na etapa 05 e no resumo, com nome acionável, dois badges independentes, contadores e ações; verificar desktop, 390×844, foco, contraste e atualização após disponibilizar/desvincular. Evidência: `client-gallery-card.tsx` é compartilhado, usa dois badges textuais, foco nativo e layout responsivo; 28 testes do editor/resumo, TypeScript e ESLint verdes.
- [x] 4.5 Transformar pastas do resumo em cards com miniatura protegida e link interno para a pasta na etapa Imagens; verificar query manipulada, pasta vazia, capa ausente, abertura, upload adicional e exclusão de foto elegível. Evidência: resumo aponta para `?folder=`, editor valida o identificador contra o contrato carregado e abre workspace existente; testes cobrem miniatura/link, query válida/manipulada, upload incremental e exclusão.

## 5. Controle de Pagamentos

- [x] 5.1 Reescrever a consulta administrativa de comunicações com joins/agregação em lote, snapshots históricos, cursor/limite, resumo e facets; verificar múltiplos pedidos da mesma cliente, galeria removida, ausência de N+1 e compatibilidade temporária da lista plana. Evidência: endpoint retorna pedidos agrupados por cliente, inclusive sem comunicação e com snapshots de galeria removida, em três consultas de domínio constantes; teste cobre dois pedidos da mesma cliente, cursor, resumo/facets, lista plana e teto de seis statements incluindo autenticação.
- [x] 5.2 Implementar filtros backend combináveis por cliente, Galeria pública, período, situação financeira e entrega de mensagem; verificar validação de UUID/data/enums, interseção dos critérios, contagens e isolamento administrativo. Evidência: filtros são aplicados em interseção sobre pedido/comunicação/entrega, enums/UUID/período/cursor são validados pelo backend e testes focados cobrem combinação completa, contagens e contrato administrativo preexistente.
- [x] 5.3 Reestruturar Pagamentos em resumo e grupos por cliente com cards/badges textuais e coloridos, detalhes por pedido e painel recolhível de filtros; verificar carregamento, vazio filtrado, limpar filtros, paginação e responsividade. Evidência: tela usa resumo, grupos cliente/pedido, estados textuais com contraste, `details`, filtros combinados, vazio específico e paginação incremental; três testes de interação, TypeScript e ESLint focado verdes e CSS adapta cards/ações para 560 px.
- [x] 5.4 Preservar decisão manual e retry de mensagem nos cards novos; verificar clique repetido, concorrência, decisão imutável, envio idempotente, falha sanitizada e atualização do estado pelo backend. Evidência: ações usam autorização `can_decide`/`can_retry`, bloqueiam repetição durante a chamada e recarregam o estado do backend; testes backend preexistentes confirmam primeira decisão e outbox idempotentes, novos testes cobrem retry nos cards e o endpoint substitui erro interno por mensagem sanitizada.

## 6. Apresentação compartilhada da galeria

- [x] 6.1 Ler integralmente a documentação local do Next.js 16 relevante a assets locais, fontes, componentes cliente, imagens e parâmetros de busca antes de editar o frontend; registrar neste arquivo as páginas consultadas. Páginas lidas antes da primeira edição frontend: `01-app/01-getting-started/13-fonts.md`, `12-images.md`, `05-server-and-client-components.md`, `03-api-reference/01-directives/use-client.md`, `03-api-reference/04-functions/use-search-params.md` e `03-api-reference/03-file-conventions/page.md`.
- [x] 6.2 Empacotar somente os pesos necessários das fontes escolhidas e suas licenças, registrar tokens CSS e medir impacto do build; verificar que a aplicação funciona offline sem `@import`, Google Fonts ou recurso de terceiro. Evidência: `local-fonts.css` empacota somente quatro WOFF2 variáveis latinos normais e `licenses/OFL-1.1.txt` versiona a licença integral; build reduziu fontes de 16 arquivos/546,0 KiB para 4 arquivos/189,5 KiB (−65,3%), sem URL HTTP, `@import`, Google Fonts ou gstatic nos contratos locais.
- [x] 6.3 Refatorar `GalleryPresentation` para respeitar `folder_display_mode`, dimensões e ordem DOM em grade editorial borderless com gutters uniformes; verificar retratos/paisagens, fallback sem dimensão, sequência, pastas individuais e ausência de layout shift/overflow relevante. Evidência: componente preserva a ordem do array/DOM, usa `width`/`height`, `aspect-ratio` e fallback 4:3, mostra todas as coleções em `sequential` e navegação única em `individual`; testes compartilhados e contratos público/privado verdes.
- [x] 6.4 Reorganizar capa, grade, detalhes e visualizador para fotografia dominante em desktop e smartphone, preservando preview protegido, bloqueios dissuasórios, foco, teclado e navegação por toque. Evidência: grade borderless responsiva (4/2/1 colunas), capa dominante, lightbox com foco/Escape/setas/swipe e prévias sem original/download; testes cobrem screenshot dissuasório, menu de contexto, teclado e contratos de capa dedicada autorizada.
- [x] 6.5 Tornar coração/favorito, seleção e compra marcadores sobrepostos e textualmente acessíveis somente para a cliente; verificar alternância, erro, foto comprada, separação entre ampliar e selecionar, e ausência das ações no modo fotógrafo. Evidência: marcadores independentes usam texto e `aria-pressed`, compra confirmada não expõe seleção, ampliação mantém botão próprio e a prévia administrativa não fornece `renderPhotoMarkers`; 17 testes frontend focados verdes.

## 7. Integração, documentação e homologação

- [x] 7.1 Executar testes backend focados e completos, Ruff, migration do zero/sobre o head e inspeção do schema/planos de consulta; corrigir regressões da change e registrar comandos e resultados verificáveis. Evidência: `ruff check app tests` passou; `pytest tests -q` aprovou 200 testes e ignorou 1; os contratos focados de workflow e derivação aprovaram 8 e 50 testes; `alembic heads` apontou `20260831_0033`; banco SQLite temporário concluiu upgrade do zero, `current`, downgrade para `0032`, novo upgrade e `current`; testes de agregação impõem teto constante de 12 statements para clientes e 6 para Pagamentos incluindo autenticação.
- [x] 7.2 Executar testes frontend focados e completos, ESLint, TypeScript e build; verificar os cinco passos, resumo, Clientes, Pagamentos e apresentação nos viewports desktop e 390×844 com dados sintéticos. Evidência: Vitest aprovou 103 testes em 21 arquivos com payloads sintéticos cobrindo editor, resumo, clientes, Pagamentos e apresentação; `npx tsc --noEmit` e `npm run build` passaram; ESLint encerrou com 0 erros e 18 avisos conhecidos; a aplicação isolada foi inspecionada em 1440×1000 e 390×844, sem overflow horizontal ou erro de console. A revisão autenticada de aceitação permanece separada na task 7.7.
- [x] 7.3 Atualizar documentação operacional para publicação incremental, capa dedicada, Vendas e filtros/estados de Pagamentos; verificar coerência com ciclo de vida, PIX manual, mensageria e ausência de segredos/fontes remotas. Evidência: `docs/OPERACAO-EDITOR-GALERIA-E-PAGAMENTOS.md` documenta o fluxo; os guias de privadas e PIX foram reconciliados; busca focada não encontrou URL externa de fonte nem segredo preenchido nos documentos/assets afetados.
- [x] 7.4 Executar `git diff --check`, revisão de escopo/segredos/licenças e validação OpenSpec estrita desta change e de todas as changes ativas; reconciliar divergências antes de integração. Evidência: `git diff --check` passou somente com avisos de conversão LF/CRLF; a busca por assinaturas de chave/token/credencial não encontrou achado; quatro WOFF2 latinos e a licença OFL integral estão inventariados; validação estrita aprovou a change e todos os 25 itens ativos sem falha.
- [x] 7.5 Preparar inventário de deploy zero-impact com SHA, migration, arquivos de fonte, serviços/volumes, porta/subdomínio, backup e rollback; solicitar autorização explícita antes de publicar em homologação. Evidência: `deployment-inventory.md` registra artefato, `20260831_0033`, quatro WOFF2/OFL, topologia preservada, `127.0.0.1:8080`, subdomínio, dump e rollback; o proprietário autorizou especificamente este inventário em 2026-09-01 antes de qualquer publicação.
- [ ] 7.6 Após autorização específica, integrar e publicar em homologação, confirmar SHA/Alembic/healthchecks e executar smoke sintético sem alterar recursos externos nem dados reais; registrar run e evidências sanitizadas.
- [ ] 7.7 Realizar revisão humana autenticada em desktop e smartphone das etapas 02–05, resumo, Pagamentos e visualizações do fotógrafo/cliente; corrigir defeitos reportados antes de sincronizar ou arquivar a change.
