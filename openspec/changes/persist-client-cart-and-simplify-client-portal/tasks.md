## 1. Inventário e contratos de regressão

- [x] 1.1 Reconciliar implementação e autoridade com `consolidate-shared-private-galleries-and-progressive-sales`, `complete-private-gallery-operations-and-sales`, checkout PIX, projeção comercial, expiração e biblioteca; registrar qualquer desvio no design e verificar por busca reproduzível que a change não altera facial, mídia, preços ou confirmação administrativa. Evidência: o inventário de implementação no `design.md` identifica os módulos atuais e preserva facial, mídia, regras de preço, PIX, decisão administrativa, Docker e workflows; buscas por checkout/seleção/pagamento/biblioteca confirmaram a autoridade existente e o único desvio atual, a remoção prematura de `PhotoSelection` em `checkout.py`.
- [x] 1.2 Adicionar testes backend inicialmente falhos para carrinho preservado após `Prosseguir`, edição do mesmo rascunho, congelamento somente em `Informar pagamento`, pedido complementar, idempotência/concorrência, isolamento cliente+galeria e pedido legado; verificar que as falhas correspondem ao comportamento atual. Evidência: três contratos direcionados falharam por ausência de `frozen_at`, remoção prematura da seleção e criação de dois pedidos; o caso legado foi acrescentado ao mesmo conjunto, enquanto os testes existentes cobrem idempotência de três comunicações, acesso negado a outra cliente e pedidos independentes.
- [x] 1.3 Adicionar testes frontend inicialmente falhos para `Carrinho (n)`, retomada com miniaturas/valor/estado, marcadores por foto e ausência dos textos excessivos aprovados; cobrir Galeria pública, privada, biblioteca e resultado facial compartilhado. Evidência: `gallery`, `public-galleries` e `library` executaram `28` testes; `24` passaram e os quatro novos contratos falharam somente pela ausência de `Carrinho (n)`, do pedido retomável e da linguagem curta, incluindo seleção a partir do resultado facial.

## 2. Persistência e transação comercial

- [x] 2.1 Criar migration aditiva para `SaleOrder.frozen_at`, backfill de todos os pedidos existentes como congelados e garantia de um único rascunho editável por cliente+galeria; verificar ciclo `head anterior -> novo head -> downgrade -> novo head` em PostgreSQL e compatibilidade SQLite dos testes. Evidência: migration `20260910_0053_persistent_client_cart` adicionada; `test_persistent_client_cart_migration.py` validou `head anterior -> novo -> downgrade -> novo` em SQLite e PostgreSQL 17 isolado (`2 passed`).
- [x] 2.2 Refatorar o checkout para criar ou sincronizar o mesmo rascunho sem apagar `PhotoSelection`, recalculando itens e cotação após adição/remoção; verificar retorno, edição, múltiplas pastas e separação entre Galerias públicas. Evidência: `synchronize_editable_draft` e `create_pending_checkout` reutilizam o rascunho sob lock; os contratos direcionados verificam adição/remoção HTTP imediata, seleção em múltiplas pastas, retorno ao checkout e isolamento entre galerias.
- [x] 2.3 Tornar `Informar pagamento` a transação de congelamento: adquirir lock comum, revalidar prazo/disponibilidade/preço, sincronizar snapshot, gravar `frozen_at`, criar uma comunicação e consumir somente a seleção congelada; verificar clique repetido e corrida com mutação do carrinho. Evidência: `freeze_pending_checkout` revalida e congela o snapshot sob o mesmo lock; testes verificam comunicação idempotente, reconsulta após corrida e consumo apenas da seleção incorporada.
- [x] 2.4 Preservar pedidos preexistentes e pedidos já comunicados/confirmados como imutáveis, permitindo novo carrinho complementar sem recompra de foto confirmada; verificar estados pendente legado, não localizado, confirmado e galeria expirada. Evidência: backfill torna legados imutáveis, a projeção exclui somente rascunho realmente editável e os testes cobrem pedido complementar, legado sem `checkout_key`, comprado, origem indisponível e prazo expirado.

## 3. Projeção e APIs da cliente

- [x] 3.1 Implementar projeção comercial compartilhada e em lote para carrinho, rascunho, pedidos congelados, itens e prioridade `purchased > payment_reported > selected > available`; verificar ausência de N+1 relevante e isolamento entre dois membros do mesmo acervo. Evidência: `client_commerce.py` concentra as projeções em lote e a prioridade; contrato com cinco galerias permanece em até seis consultas e testes de associação impedem vazamento entre clientes.
- [x] 3.2 Estender de forma compatível os payloads da biblioteca, Galeria pública, privada, detalhe do pedido e pagamento para restaurar fotos, totais, estados e ações autorizadas; verificar sessão renovada, outro dispositivo, múltiplas galerias e origem indisponível. Evidência: payloads aditivos agora incluem carrinho, miniaturas, rascunho, pedidos, comunicação e estado por foto, sempre reconstruídos no servidor; testes de rota cobrem reload, galerias separadas e referência indisponível.
- [x] 3.3 Aplicar expiração e reabertura às novas mutações: manter consulta de carrinho/pedidos, bloquear edição/comunicação fora do prazo e retomar após aprovação; verificar respostas backend e ausência de autorização inferida pelo frontend. Evidência: leitura permanece disponível, enquanto seleção, remoção, checkout e comunicação reutilizam as validações de prazo do servidor; testes existentes e novos cobrem expiração e reabertura sem autoridade mantida no navegador.
- [x] 3.4 Ampliar a leitura administrativa da galeria privada com favoritos e comentários ativos agrupados por foto e atribuídos à cliente, sem N+1 nem mudança nas mutações da cliente; verificar isolamento e remoção com teste backend direcionado. Evidência: `admin_private_gallery_photos` agrega favoritos, comentários ativos, estados comerciais e nomes de clientes em cinco consultas por lote; o contrato `test_client_interactions_are_private_reversible_and_audited` comprovou autoria, visibilidade e desaparecimento após remoção (`1 passed`).
- [x] 3.5 Corrigir `selected_count` para representar a união sem duplicidade de carrinho e pedidos pendentes ainda não comprados e expor o estado comercial por foto/cliente na leitura administrativa; verificar transições `Selecionada -> Pagamento informado -> Comprada` em duas galerias isoladas. Evidência: `build_commercial_projections` une seleções e itens pendentes congelados e subtrai compras confirmadas; `admin_private_gallery_photos` aplica a prioridade por foto/cliente; os dois contratos backend direcionados passaram (`2 passed`).

## 4. Interface curta e retomável

- [x] 4.1 Criar ou consolidar componente compartilhado de `Carrinho (n)` e revisão por galeria, com miniaturas, total, remoção antes da comunicação, estados de pedidos e CTA único; verificar acessibilidade, mobile/desktop e restauração após reload. Evidência: `client-cart.tsx` é compartilhado pelas superfícies, apresenta contador, miniaturas, remoção nomeada, total, estados e CTA; `30` arquivos/`196` testes frontend, typecheck e build de produção passaram.
- [x] 4.2 Integrar os estados comerciais e o carrinho à Galeria pública, privada e resultados faciais, impedindo duplicação/recompra e permitindo novo carrinho complementar; verificar os fluxos manuais e faciais com a mesma foto. Evidência: Galeria pública e privada consomem `commercial_state`, a seleção facial passa pela mesma derivação/lock e os testes verificam bloqueio de recompra e carrinho complementar.
- [x] 4.3 Simplificar biblioteca e cards para `Minhas fotos`, estado essencial e próxima ação, removendo `Sua área privada`, `Cada evento aparece uma única vez`, `compras preservadas` e explicações redundantes; verificar estados vazio, ativo, pagamento informado, comprado, expirado e origem indisponível. Evidência: a biblioteca usa título curto, resumo essencial e uma ação; testes confirmam estados e ausência das três frases removidas.
- [x] 4.4 Adicionar teste de regressão e manter `Minha galeria` visível na Galeria pública quando o backend devolver uma privada autorizada com carrinho vazio, sem criar acesso local nem remover `Carrinho (n)` quando houver seleção. Evidência: o endpoint leve resolve somente a privada operacional da cliente autenticada; o teste público comprova `Minha galeria` antes da carga de fotos e os contratos existentes preservam `Carrinho (1)` após seleção.
- [x] 4.5 Desacoplar a navegação essencial dos payloads pesados de fotos/pastas nas galerias pública e privada, com regressões que provem a renderização progressiva. Evidência: a Galeria pública separa metadados e fotos com erro local; a privada renderiza a revisão sem aguardar pastas; `27` testes frontend direcionados e o contrato backend de derivação passaram.
- [x] 4.6 Remover da superfície da cliente qualquer diagnóstico de falha, atraso ou retentativa da entrega WhatsApp, preservando `Pagamento confirmado`, `Comprada` e a visibilidade administrativa; verificar com regressão frontend dedicada. Evidência: a condicional cliente foi removida sem alterar o payload ou o painel administrativo; a regressão com notificação `failed` confirma `Pagamento confirmado`/`Comprada` e ausência dos detalhes técnicos.
- [x] 4.7 Compactar os indicadores comerciais sobre fotos e trocar `Favoritar` por coração acessível nas grades pública/privada; apresentar favoritos e comentários junto às fotos na ficha administrativa e verificar com testes frontend direcionados. Evidência: grades pública/privada usam coração com nome acessível e selo comercial compacto; a ficha `Galeria privada · acervo da cliente` exibe autoria, estado e comentário por foto; os três arquivos direcionados passaram (`37 passed`).

## 5. Validação, documentação e entrega

- [x] 5.1 Executar Ruff, testes backend direcionados e completos, migration PostgreSQL, lint/typecheck/testes/build frontend, OpenSpec estrito, gitleaks e `git diff --check`; investigar falhas relacionadas e registrar evidências verificáveis antes de marcar a task. Evidência: Ruff passou; backend completo `472 passed, 2 skipped`; migration SQLite/PostgreSQL `2 passed`; frontend `30 files/196 tests`, ESLint sem erros, TypeScript e build Next.js passaram; OpenSpec estrito validou `34` itens; gitleaks v8.24.3 não encontrou segredo em `225` commits/`9.35 MB`; `git diff --check` passou apenas com avisos de EOL.
- [x] 5.2 Documentar estados, compatibilidade de pedidos legados, concorrência, rollback e roteiro humano de retomada em celular/desktop; verificar que outro executor consegue reproduzir seleção, saída, retorno, edição, comunicação e pedido complementar apenas pelo repositório. Evidência: `operation.md`, `homologation-checklist.md` e `deployment-inventory.md` registram autoridade, transições, legado, concorrência, expiração, diagnóstico, rollback e roteiro reproduzível.
- [x] 5.3 Preparar inventário zero-impact de homologação com SHA, migration, serviços, portas, volumes, backup, estado facial e rollback; solicitar autorização específica antes de deploy e, somente após autorizada, publicar e confirmar healthchecks sem alterar dados, mídia ou infraestrutura de terceiros. Evidência: autorização recebida; PR `#62` e workflow `34511397653` publicaram `0d48ab8de41f8ad59ee26439c9a3edd876e34727`; inventário e backup exclusivos precederam a migration `20260909_0052 -> 20260910_0053`; estado facial permaneceu `true` com workers saudáveis; `/healthz`, `/api/health` e entrada da cliente responderam HTTP `200`. Resultado completo em `deployment-inventory.md`.
- [x] 5.4 Executar testes frontend direcionados, TypeScript, ESLint, OpenSpec estrito e `git diff --check`; revisar o diff e preparar entrega zero-impact da correção, solicitando autorização humana específica antes de push, merge e deploy em homologação. Evidência: frontend direcionado `27 passed`; TypeScript aprovado; ESLint sem erros e com três avisos preexistentes de `<img>`; Ruff aprovado; backend direcionado `1 passed`; OpenSpec estrito e `git diff --check` aprovados; autorização específica para push, merge e deploy recebida em 2026-09-11.
- [x] 5.5 Executar o teste frontend direcionado, TypeScript, ESLint focado, OpenSpec estrito e `git diff --check`; revisar o diff e preparar a entrega sem banco, migration ou alteração de integração, solicitando autorização humana específica antes de push, merge e deploy em homologação. Evidência: frontend direcionado `3 files/37 passed`; TypeScript aprovado; ESLint focado com `0` erros e cinco avisos preexistentes de `<img>`; backend direcionado `4 passed`; Ruff aprovado; OpenSpec estrito e `git diff --check` aprovados. O gate remoto identificou e a validação cirúrgica corrigiu uma expectativa legada que ainda sobrepunha fotos compradas ao contador de selecionadas; o contrato direcionado passou com `1` selecionada e `1` comprada. O ensaio de escalabilidade permanece reservado para solicitação posterior do proprietário.
