## 0. Reconciliação OpenSpec obrigatória antes do código

- [x] 0.1 Confrontar e reconciliar explicitamente `align-admin-gallery-wizard-and-folder-ownership`, `complete-gallery-operational-flow`, `unify-gallery-presentation` e `add-derived-client-galleries` com esta change; verificar que exclusão restrita a galeria vazia, derivação no simples link/cadastro, experiência exclusivamente privada e liberação sem modo de acesso não permanecem como propostas implementáveis conflitantes. Evidência: as quatro changes registram a supersession e validaram individualmente em modo estrito em 2026-08-31; mandato, roadmap e diretrizes de frontend também foram reconciliados com autenticação anterior às prévias e modos de acesso no backend.
- [x] 0.2 Criar deltas formais para `gallery-sales/client-selection-operations`, `gallery-sales/original-gallery-experience`, `media-storage/protected-previews` e `gallery-visualization-and-watermark-controls`, além de ampliar todos os requisitos afetados de `media-storage/staged-folder-release`; verificar cobertura de disponibilidade versus seleção, autenticação antes de qualquer prévia, herança visual/comercial, modos de acesso e retenção histórica mínima. Evidência: os quatro delta specs foram adicionados, `staged-folder-release` cobre os três modos e a change validou em modo estrito em 2026-08-31.
- [x] 0.3 Executar validação OpenSpec estrita de todas as changes ativas após a reconciliação e revisar o grafo de dependências; não iniciar task de código enquanto houver proposal ativa supersedida ou comportamento afetado sem delta correspondente. Evidência: `openspec validate --all --strict` aprovou 23/23 itens em 2026-08-31. As quatro changes sobrepostas apontam para esta autoridade; revisões visuais humanas antigas não bloqueiam trabalho local, WhatsApp permanece um adaptador substituível nos testes e busca facial continua isolada atrás do spike próprio.

## 1. Contrato de dados e migration aditiva

- [x] 1.1 Adicionar modelos de operação de ciclo de vida e manifesto histórico, estados e constraints de idempotência; verificar criação, unicidade, transições válidas e rejeição de estado inválido em testes de banco. Evidência: migration aditiva `20260831_0018`, modelos `GalleryLifecycleOperation`/`CommercialHistoryMedia` e máquina de estados; `pytest tests/test_gallery_lifecycle.py -q` aprovou 3 testes e Ruff/diff-check passaram em 2026-08-31.
- [x] 1.2 Adicionar snapshots obrigatórios de galeria e item comercial e tornar as FKs operacionais de pedidos/itens anuláveis com `ON DELETE SET NULL`; verificar upgrade do zero, upgrade sobre fixture legada e downgrade estrutural sem executar exclusão de dados. Evidência: migration `20260831_0019` preenche e torna obrigatórios os snapshots, substitui as FKs operacionais e recusa downgrade após remoção; fixture legada comprovou preservação após apagar foto/privada. `test_gallery_lifecycle.py` aprovou 4 testes, a suíte focada aprovou 62 e Ruff passou em 2026-08-31.
- [x] 1.3 Implementar backfill idempotente dos snapshots existentes com relatório de lacunas; verificar duas execuções consecutivas, preservação dos valores originais e bloqueio explícito quando faltar mídia de compra confirmada. Evidência: `backfill_commercial_snapshots` preenche somente ausências, emite relatório estruturado e lança `CommercialHistoryGap` para item confirmado sem mídia operacional ou histórica pronta; os testes comprovam primeira/segunda execução, snapshot congelado e desbloqueio após manifesto pronto. `test_gallery_lifecycle.py` aprovou 6 testes e Ruff passou em 2026-08-31.
- [x] 1.4 Adicionar ciclo de vida `deleting` à Galeria pública e índices para consultas de operação, pedidos confirmados e contadores por cliente; verificar plano/quantidade de consultas e que galerias em exclusão rejeitam novas mutações. Evidência: migration `20260831_0020` adiciona o estado independente `active`/`deleting`, constraint e seis índices compostos; os fluxos administrativos, OTP contextual e acessos da cliente recusam alterações concorrentes com `409`. Os testes verificam uma busca indexada por plano, quatro mutações administrativas e seleção da cliente sem persistência indevida; `test_gallery_lifecycle.py` aprovou 8 testes, regressão focada aprovou 62 e Ruff/diff-check passaram em 2026-08-31.
- [x] 1.5 Garantir no banco no máximo uma galeria privada operacional por par Galeria pública + cliente e registrar em cada referência disponível sua origem `admin`, `client` ou origem futura autorizada, separada de `PhotoSelection`; verificar concorrência, coexistência de origens e ausência de seleção criada pela ação administrativa. Evidência: migration `20260831_0021` transforma o índice do par em único, interrompe upgrade diante de duplicatas e preenche referências legadas com origem conservadora `admin`; `DerivedGalleryPhoto` aceita justificativas coexistentes `admin`/`client`/`facial` por foto sem criar `PhotoSelection`. Testes provam a corrida após duas pré-consultas, reutilização administrativa com origens `client` + `admin`, zero seleção, rejeição de origem duplicada e backfill legado; `test_gallery_lifecycle.py` aprovou 10 testes, regressão focada aprovou 62 e Ruff/diff-check passaram em 2026-08-31.
- [x] 1.6 Reconciliar telefone canônico e telefones ativos verificados como chave única de identidade; verificar fixture legada, troca de número, conflito concorrente e ausência de duplicação de `Client`. Evidência: migration `20260831_0022` reconcilia e comprova os telefones canônicos legados, substitui a constraint histórica por índices parciais para telefone verificado e único telefone ativo por cliente e interrompe dados divergentes; `client_identity` centraliza resolução, comprovação e troca transacional. OTP cria/reutiliza uma única identidade, cadastro administrativo reserva telefone ainda não comprovado e a troca aposenta o anterior. Testes provam fixture legada, corrida após pré-consultas, conflito de telefone verificado entre clientes, ausência de duplicação e troca com acesso preservado; `test_gallery_lifecycle.py` aprovou 12 testes, autenticação/troca aprovou 13 e Ruff/diff-check passaram em 2026-08-31.
- [x] 1.7 Adicionar modo de acesso `standard`, `invite_only` ou `collective_protected` à Galeria pública e modelo de token/convite com hash, escopo, estado, expiração, rotação e revogação; verificar backfill conservador, constraints e que UUID ou token em claro não funciona como autoridade persistida. Evidência: migration `20260831_0023` preenche galerias legadas com `invite_only`, valida os três modos e cria `GalleryAccessCapability` com alvo/escopo/estado consistentes, expiração, rotação, revogação e índices de capacidade ativa. `gallery_access` emite segredo de alta entropia uma única vez e persiste só SHA-256; testes provam alvo inválido, modo inválido, UUID sem autoridade, inexistência do segredo no campo persistido, rotação e revogação. `test_gallery_lifecycle.py` aprovou 14 testes, regressão de autenticação/galerias aprovou 57 e Ruff passou em 2026-08-31.

## 2. Histórico comercial e mídia preservada

- [x] 2.1 Implementar serviço de materialização dos snapshots comerciais antes da desvinculação ou exclusão; verificar pedidos confirmados, pendentes e cancelados sem alterar valores, cliente ou auditoria existentes. Evidência: `materialize_commercial_history` bloqueia e prepara somente os pedidos do par/galeria alvo dentro da transação chamadora, completa snapshots de origem, cliente e item, interrompe lacunas e não confirma a operação por conta própria. Fixture com pedidos `confirmed`, `pending` e `cancelled` comprovou materialização e segunda execução idempotente sem alterar estado, total, cliente, confirmação, regra de preço ou auditoria; `test_gallery_lifecycle.py` aprovou 15 testes e Ruff passou em 2026-08-31.
- [x] 2.2 Implementar armazenamento histórico determinístico por item comprado, com prévia mínima protegida, entrega final ou referência segura, checksum e sem PII na chave; verificar repetição sem duplicata, detecção de arquivo divergente, ausência de cópia integral desnecessária e ausência de cópia para foto não comprada. Evidência: `prepare_confirmed_historical_media` preserva a prévia `client_preview` já protegida e, somente quando não existe referência segura, copia a entrega em `items/<item_uuid>/`, verificando SHA-256 antes/depois e reutilizando manifesto pronto. O teste cobre dois itens confirmados, referência externa sem cópia integral, pedido pendente e foto sem item sem manifesto, segunda execução sem duplicata, chaves sem PII/nome e corrupção detectada; lifecycle + mídia aprovaram 25 testes, Ruff completo do backend e diff-check passaram em 2026-08-31.
- [x] 2.3 Adaptar consultas administrativas de pedidos para aceitar galeria/foto operacional removida e usar snapshots; verificar filtros, totais e rótulo “Galeria removida” em testes de API. Evidência: `/admin/purchases` filtra por Galeria pública/cliente, calcula quantidade e valor, usa nomes/IDs congelados e omite URL operacional ausente; `/admin/derived-galleries/{snapshot_id}/orders` continua acessível pelo UUID histórico e declara `Galeria removida`. Teste remove foto, privada, pasta e origem, confirma FKs nulas e valida filtros, totais, rótulos e item via snapshot; três testes focados, Ruff e diff-check passaram em 2026-08-31.
- [x] 2.4 Adaptar a biblioteca do cliente para pedidos, identificação visual e entregas históricas independentes da galeria ativa; verificar autorização do proprietário, negação cruzada e nenhum vazamento de mídia operacional. Evidência: `/library/purchases` usa snapshots e o manifesto histórico quando a galeria/foto operacional já não existe, e as rotas de prévia/entrega autorizam exclusivamente a cliente proprietária por pedido confirmado, servem apenas `MEDIA_HISTORY_ROOT` e preservam a compatibilidade operacional durante a transição. O teste remove toda dependência operacional, valida identificação/entrega histórica, `403` cruzado e ausência do pedido na biblioteca alheia; a regressão da prévia por papel e Ruff passaram em 2026-08-31.
- [x] 2.5 Implementar política configurável e documentada de retenção e minimização de PII histórica sem prazo legal inventado; verificar preservação contábil/auditável, anonimização permitida e que originais não são retidos apenas para histórico. Evidência: `COMMERCIAL_HISTORY_MEDIA_RETENTION_DAYS` vazio desativa expurgo e um inteiro positivo explicitamente configurado limita somente a mídia histórica, com manifesto `purged`, datas e execução idempotente; configuração inválida falha fechada. A minimização exige permissão expressa, limpa apenas nome/telefone congelados, audita por UUID/contagem e preserva cliente, pedido, item, valores e identificação comercial. A migration `20260831_0024`, a documentação `RETENCAO-HISTORICO-COMERCIAL.md`, três testes focados (incluindo upgrade/downgrade descartável) e Ruff passaram em 2026-08-31; o teste 2.2 já comprova que original coberto por referência e fotos não compradas não são retidos.

## 3. Operações idempotentes de ciclo de vida

- [x] 3.1 Implementar orquestrador e worker das etapas `queued`, `preparing_history`, `removing_storage`, `removing_records`, `completed` e `failed`; verificar retomada após falha injetada em cada etapa e ausência de regressão de estado. Evidência: o worker reserva operações novas ou interrompidas por lease, confirma cada etapa separadamente, registra progresso no manifesto, sanitiza falhas e reagenda explicitamente sem repetir etapas concluídas. A migration `20260831_0025` adiciona tentativas/lease e constraint; testes parametrizados injetam falha nas três etapas executáveis, retomam até `completed`, comprovam duas tentativas, timestamps destrutivos e ausência do erro sensível, enquanto o teste da máquina rejeita saltos/regressões. Quatro testes focados, upgrade/downgrade descartável e Ruff passaram em 2026-08-31.
- [x] 3.2 Reconciliar o endpoint administrativo `202`, o progresso e o inventário com a preservação de privadas; verificar autenticação, distinção entre mídia removível e mídia preservada por referência privada, chave repetida e bloqueio de mutações concorrentes. Evidência: o inventário e a operação `202` separam explicitamente fotos/pastas/derivados removíveis de privadas, referências e mídia comercial preservadas; o manifesto é congelado antes do estado `deleting`. O contrato exige admin/chave idempotente, reutiliza a operação e bloqueia uma segunda chave e mutações concorrentes. Os cinco testes reconciliados e Ruff passaram em 2026-08-31.
- [x] 3.3 Reconciliar a ordem de remoção para revogar a origem pública e seus vínculos sem apagar galerias privadas, referências ou ativos ainda usados; verificar em fixture completa que privadas, clientes e histórico permanecem, que fotos sem referência são removidas e que nenhum vínculo fica órfão. Evidência: a etapa de registros revoga capabilities públicas, vínculos e desafios contextuais, minimizando antes a PII OTP, e converte a origem em tombstone interno; privadas, referências, pastas/fotos necessárias, interações, configuração herdada, cliente e histórico permanecem. A fixture completa comprovou remoção apenas da foto pública sem referência e leitura autorizada da privada inclusive por `review` após a exclusão; testes e Ruff passaram em 2026-08-31.
- [x] 3.4 Reconciliar a limpeza física pelo manifesto para apagar somente originais e derivados sem referência privada ou histórica; verificar chaves ausentes, falha parcial, nova tentativa, uma única cópia compartilhada e preservação de visualização privada e mídia histórica comprada. Evidência: o manifesto contém somente chaves de `PhotoAsset` sem `DerivedGalleryPhoto` e derivados correspondentes, valida confinamento ao storage operacional e nunca inclui `MEDIA_HISTORY_ROOT`. A retomada trata arquivo já ausente de forma idempotente e repete após falha parcial; o teste integrado comprovou uma única cópia preservada para a privada, prévia ainda servida, limpeza da mídia exclusivamente pública e histórico intacto. Testes e Ruff passaram em 2026-08-31.
- [x] 3.5 Implementar cancelamento seguro somente antes da primeira etapa destrutiva e erro sanitizado depois desse limite; verificar que nenhuma interface promete restauração após a remoção física. Evidência: a migration `20260831_0026` adiciona o terminal `cancelled`; o endpoint administrativo de cancelamento é idempotente, recusa lease de preparação ativo, reativa a Galeria pública somente antes de `destructive_started_at` e registra ator/operação sem PII. A partir de `removing_storage` responde apenas que a remoção física não pode ser cancelada nem restaurada, sem caminho/erro interno e sem alterar estado. Testes cobrem cancelamento, repetição, recusa irreversível, estado da origem, auditoria e upgrade/downgrade descartável; Ruff passou em 2026-08-31.
- [x] 3.6 Aplicar a política comercial comum a remoção de seleção, referência, vínculo e galeria: descartar carrinho sem pedido, cancelar com auditoria pedido pendente sem pagamento comunicado, bloquear `pending_review` e preparar histórico antes de remover pedido confirmado; verificar cada transição e concorrência com revisão administrativa. Evidência: `apply_commercial_removal_policy` bloqueia o escopo inteiro antes de mutar quando encontra comunicação `pending_review`, cancela/audita pedidos pendentes sem revisão, preserva itens/snapshots e materializa/verifica mídia dos confirmados antes de autorizar a remoção. A política foi aplicada à preparação do worker, exclusão de foto/referência, galeria privada e remoção de seleção; recusa administrativa agora cancela coerentemente o pedido. Teste integrado cobre bloqueio sem mutação, dois cancelamentos, compra preparada, reexecução idempotente e carrinho sem pedido removido; seis regressões de galerias/fotos/pagamento e Ruff passaram em 2026-08-31.

## 4. Derivação privada e desvinculação de cliente

- [x] 4.1 Separar o vínculo individual com a Galeria pública da existência de `DerivedGallery`; verificar que cadastro por link ou pelo administrador não cria galeria privada vazia. Evidência: `ParentGalleryRegistration` permanece a relação explícita e `link_client_to_parent` cria/ativa esse vínculo idempotentemente sem tocar em `DerivedGallery`; o novo endpoint administrativo retorna `private_gallery_id: null`. Testes comprovam repetição administrativa com um registro/zero privadas, cadastro desconhecido por link somente após OTP e reuso do vínculo, além de link para cliente existente sem exposição de fotos; três testes focados e Ruff passaram em 2026-08-31.
- [x] 4.2 Implementar serviço transacional idempotente que cria ou reutiliza a galeria privada, inclui referência disponível com procedência e cria `PhotoSelection` somente por escolha da cliente; verificar primeira seleção, criação administrativa concorrente, coexistência de origens e uma única galeria por cliente e Galeria pública. Evidência: `derive_client_selection` valida vínculo/foto/pasta/janela, resolve corrida por savepoints e constraints, cria ou reutiliza o par, acrescenta justificativa `client` e seleção separadamente sem duplicar; a rota pública e a rota privada usam o serviço. A migration `20260831_0027` materializa vínculos para privadas legadas. Testes cobrem primeira e segunda chamadas, foto adicional, origens `admin` + `client`, duas seleções/uma privada e reutilização do vencedor administrativo; regressão de expiração e upgrade/downgrade também passaram, com Ruff, em 2026-08-31.
- [x] 4.3 Resolver toda seleção adicional pelo par Galeria pública + cliente e reutilizar a privada correspondente; verificar múltiplas origens, referência já disponível por `admin`, nova derivação após encerramento, bloqueio/expiração e ausência de mistura de fotos ou pedidos. Evidência: todas as seleções resolvem a privada pelo par único, verificam origem da foto e janela da privada, e a referência `client` pode coexistir com `admin`; o encerramento operacional libera o par sem alterar vínculo/histórico. Teste com duas Galerias públicas comprova privadas distintas, recusa por expiração sem duplicata, exclusão segura, nova derivação com UUID novo e fotos sempre restritas à origem correspondente; regressão existente cobre galeria bloqueada/expirada e terceiro. Teste focado e Ruff passaram em 2026-08-31.
- [x] 4.4 Exigir sessão antes de qualquer prévia, preservar retorno interno seguro e aplicar no backend o modo `standard`, `invite_only` ou `collective_protected`; verificar login e retorno, sessão existente sem novo OTP, vínculo automático apenas em `standard`, convite exigido, coletivo pendente e ausência de galeria privada vazia. Evidência: desafios OTP persistem somente a capability por ID/hash e retorno interno validado; rotas pública/listagem/prévia exigem sessão e autorização no backend. Testes cobrem `standard` com vínculo automático e sessão existente, `invite_only` negando token público e aceitando convite individual, coletivo pendente sem grade, telefone novo autorizado sem privada vazia, UUID cru sem autoridade e retorno externo descartado. `test_auth.py` e regressões focadas aprovaram 14 testes, e Ruff passou em 2026-08-31.
- [x] 4.5 Implementar criação administrativa de galeria privada com cliente, vínculo explícito, ao menos uma foto disponível e convite seguro, sem seleção automática; verificar cliente inexistente, lista vazia, zero seleções, fotos de outra Galeria pública, repetição, OTP do telefone proprietário e negação a terceiro que possua somente o link. Evidência: `derive_admin_gallery` aceita somente fotos disponíveis de pastas liberadas da mesma origem, cria/reusa o par e referências `admin`; lista vazia cria apenas vínculo e nenhuma privada. A API emite capability `private_invite` com segredo retornado uma vez e hash persistido, não cria seleção, autentica o telefone proprietário e nega terceiro. Teste integrado cobre todos os casos e a suíte `test_derived_galleries.py` aprovou 45 testes; Ruff passou em 2026-08-31.
- [x] 4.6 Encerrar uma galeria derivada pela cliente somente quando não restar referência disponível nem impedimento comercial, preservando referências `admin`, vínculo, `Client` e histórico; verificar zero seleção com foto administrativa, última origem `client`, `pending_review`, compra confirmada preparada, foto restante e que o arquivo público não é apagado. Evidência: `remove_client_selection_and_close_if_empty` remove seleção e somente a origem `client`, conserva a privada com qualquer referência restante e elimina estados privados não comerciais apenas quando ela fica vazia. Teste integrado comprova referência `admin` com zero seleção, fechamento da última origem client preservando foto/vínculo, bloqueio `pending_review` sem mutação e fechamento após compra confirmada com manifesto, pedido e foto preservados; quatro regressões comerciais e Ruff passaram em 2026-08-31.
- [x] 4.7 Manter o fluxo facial desativado e preparar somente a porta de derivação autorizada; verificar que nenhum endpoint expõe grade coletiva ou aceita resultado facial antes da change e do spike próprios. Evidência: `derive_approved_facial_result` é uma porta interna que falha fechada com `FacialDerivationUnavailable`; não existe rota facial/biométrica no OpenAPI e uma rota presumida retorna `404`. O teste conjunto também comprova que `collective_protected` mantém registro pendente e recusa grade/prévia; dois testes focados e Ruff passaram em 2026-08-31.
- [x] 4.8 Implementar inventário e endpoint administrativo idempotente para desvincular cliente da Galeria pública; verificar cliente inexistente, vínculo inexistente, repetição e autorização administrativa. Evidência: `DELETE /admin/parent-galleries/{parent}/clients/{client}` exige admin e `Idempotency-Key`, retorna operação `202` com inventário, bloqueia o par como `unlinking`, repete a mesma operação e rejeita colisão/alvo inexistente. Teste integrado cobre anônimo, chave ausente, cliente/vínculo inexistentes, repetição e conflito; Ruff passou em 2026-08-31.
- [x] 4.9 Reutilizar a preparação histórica e remover somente o registro, galeria privada e interações operacionais do vínculo escolhido; verificar cliente com compra, sem compra e com outra galeria independente. Evidência: o worker usa a política comercial no escopo do par, não remove storage público e elimina somente registro, privada, referências, seleção, interações, configuração e capability do alvo. Teste integrado confirma compra/material histórico e `PhotoAsset` preservados, vínculo sem privada removido e outra origem/privada da mesma cliente intacta; Ruff passou em 2026-08-31.
- [x] 4.10 Registrar auditoria da desvinculação com ator, contagens e resultado sem duplicar PII; verificar conteúdo sanitizado e rastreabilidade do pedido preservado. Evidência: a operação durável registra `actor_admin_id`, inventário e contagens removidas; eventos de fila e conclusão usam somente UUID da operação. Teste comprova dois eventos rastreáveis, ausência de nome/telefone no manifesto e subjects, e pedido confirmado ainda consultável pelo snapshot; Ruff passou em 2026-08-31.
- [x] 4.11 Implementar herança da Galeria pública para preço/faixas, PIX, mensagens, favoritos, comentários, apresentação e prazo padrão, materializando `selection_expires_at` e congelando termos no checkout; verificar mudança antes/depois do pedido e ausência de overrides privados não especificados. Evidência: a migration `20260831_0030` centraliza configuração e preço/PIX na Galeria pública, recusa consolidação legada divergente e possui rollback; a derivação materializa o prazo, leitura/interações acompanham a origem e checkout congela preço, mensagem e PIX. APIs privadas rejeitam escrita herdada e expõem a origem somente para consulta. O teste integrado altera a configuração antes e depois do pedido, comprova prazo estável e snapshot imutável; oito testes comerciais focados, 46 testes da suíte de derivadas (44 na execução ampla e as duas regressões corrigidas/reexecutadas), quatro regressões de ciclo de vida, migration completa e Ruff passaram em 2026-08-31.
- [x] 4.12 Implementar emissão, consumo, rotação e revogação de link público e convite individual com token opaco armazenado por hash; verificar expiração, replay, escopo, resposta neutra, logs sem token, OTP de telefone divergente e UUID isolado. Evidência: links públicos passam a nascer com a Galeria pública e APIs administrativas emitem, consultam sem reexpor segredo, rotacionam e revogam links públicos, convites individuais e privados; o token aparece somente na emissão/rotação e o banco/auditoria guardam UUID e hash. Convites são consumidos sob lock somente depois do OTP e escopo/telefone corretos; públicos permanecem reutilizáveis. Testes cobrem rotação, revogação idempotente, expiração terminal, UUID isolado, telefone divergente, replay neutro, hash e ausência do segredo na auditoria; cinco testes focados, 12 testes de autenticação e Ruff passaram em 2026-08-31.

## 5. Resumo de clientes e contrato da interface

- [x] 5.1 Expandir a API da Galeria pública com `available_count`, `selected_count`, `purchased_count` e `gallery_status` calculados em lote; verificar precedência `pending_registration`, `no_selection`, `blocked`, `expired`, `active`, privada administrativa com zero seleção, itens distintos confirmados e ausência de N+1. Evidência: `/admin/parent-galleries/{id}/clients` carrega clientes e três agregados por lote, conta fotos disponíveis/selecionadas e itens confirmados distintos e aplica a precedência normativa; privada administrativa com foto e zero seleção retorna `no_selection`. Teste com cinco clientes cobre todos os estados, duas fotos ativas, item repetido em pedidos confirmados e limite constante de oito SELECTs incluindo autenticação; regressão do vínculo/summary e Ruff passaram em 2026-08-31.
- [x] 5.2 Migrar DTOs e consumidores novos para `client`, mantendo aliases temporários compatíveis onde necessário; verificar serialização, consumidores existentes e remoção de textos visíveis com “Responsável”, “Galeria-mãe” ou “Acervo-fonte”. Evidência: a listagem e o detalhe de galerias privadas agora publicam `client_count`/`client` como contratos primários, preservam `responsible_count`/`responsible` como aliases idênticos durante a transição e não expõem URL derivada de UUID como link. O consumidor administrativo prioriza `client`, aceita o alias antigo e edita na privada somente nome/acesso, direcionando regras herdadas à Galeria pública. Busca automatizada não encontrou os termos antigos em `backend/app` ou `frontend/app`; teste de contrato backend, 17 testes de componente, TypeScript e Ruff passaram em 2026-08-31.
- [x] 5.3 Criar testes de contrato para inventário de exclusão, progresso, desvinculação e resumo por cliente; verificar códigos HTTP, estados de erro e que o frontend não precisa inferir regra de negócio. Evidência: endpoints autenticados de pré-confirmação agora retornam alvo, inventário, consequências e instrução idempotente tanto para exclusão quanto para desvinculação; a operação `202` publica progresso rotulado, percentual, etapa de falha e ações backend-driven de consulta, cancelamento e retomada. Foi adicionada retomada HTTP exclusiva para operações `failed`, preservando inventário e etapas concluídas. Quatro testes de contrato cobrem `200/202/403/404/409/422`, igualdade do inventário antes/depois da fila, resumo por cliente, falha e retry; Ruff passou em 2026-08-31.

## 6. Experiência administrativa e da cliente

- [x] 6.1 Reestruturar a etapa Clientes da Galeria pública em cartões responsivos com nome acionável, telefone permitido, contadores e badges acessíveis verde/amarelo/preto; verificar desktop, 390×844, navegação por teclado e contraste sem depender somente de cor. Evidência: QA visual local com dados sintéticos em 1440×900 e 390×844 exibiu os cinco estados, contadores e ações sem overflow (`scrollWidth` menor que o viewport), com nome/atalhos focáveis e outline visível. Os badges comunicam estado por texto e usam verde, amarelo ou preto; o bloqueado foi confirmado com fundo `rgb(17,17,17)` e texto branco. Vinte e dois testes focados do editor/dashboard e `tsc --noEmit` passaram em 2026-08-31.
- [x] 6.2 Implementar confirmação única de exclusão com nome e inventário do que será removido/preservado, progresso e retomada; verificar cancelamento, duplo clique, falha e sucesso em testes de componente. Evidência: a ficha da Galeria pública consulta o inventário antes de confirmar, exibe nome, contagens removidas/preservadas e consequência irreversível, envia uma chave idempotente e desabilita duplo envio. O acompanhamento usa integralmente `progress`/`actions` do backend, atualiza por polling e oferece cancelamento antes da fase física, retry após falha e retorno após conclusão. Dezesseis testes de componente, incluindo as quatro trajetórias exigidas, e TypeScript passaram em 2026-08-31.
- [x] 6.3 Implementar ação de desvinculação por cliente com consequências claras e atualização do cartão; verificar vínculo sem compra, com compra, pendente e falha do backend. Evidência: cada cartão consulta o inventário individual antes da confirmação e informa remoção da relação/privada, preservação de cadastro, outras galerias e histórico. A ação usa idempotência, acompanha o worker, atualiza os cartões ao concluir e oferece cancelamento/retry conforme o backend; `pending_review` e falhas permanecem visíveis sem remover o cartão. Dezoito testes de componente cobrem vínculo sem seleção, compra preservada, pendência financeira, falha, sucesso, cancelamento e retomada; TypeScript passou em 2026-08-31.
- [x] 6.4 Atualizar a biblioteca do cliente com seções relacionadas para Galerias públicas abertas, galerias privadas ativas e histórico, incluindo retorno da privada à origem; verificar múltiplas origens, estados vazio, ativo, expirado, desvinculado e removido em desktop e smartphone. Evidência: QA visual local autenticado com dados sintéticos em 1440×900 e 390×844 mostrou duas origens públicas, privadas ativa/expirada/com origem removida e dois pedidos preservados, com retorno disponível somente para origens autorizadas, uma coluna no smartphone e nenhum overflow horizontal. Quatro testes de componente cobrem também biblioteca vazia após desvinculação e falha neutra; a suíte frontend completa anterior, os 22 testes focados após o ajuste visual e `tsc --noEmit` passaram em 2026-08-31.
- [x] 6.5 Implementar navegação conforme `standard`, `invite_only` e `collective_protected`, criação privada pela primeira seleção com retorno após OTP e criação administrativa por cliente + fotos disponíveis; verificar carregamento, confirmação, erro, zero seleção administrativa, ausência de grade coletiva e acesso exclusivo em testes de componente. Evidência: a nova rota da Galeria pública só consulta metadados e prévias após autorização do backend, trata ausência de grade sem simular conteúdo e transforma a primeira seleção em uma privada reutilizável; a entrada OTP preserva capability opaca/retorno interno e reaplica o link para sessão existente. A administração configura explicitamente os três modos e cria/reutiliza a privada escolhendo somente fotos disponíveis de pastas liberadas, sem gerar seleção automática. Vinte e três testes de componente cobriram carregamento, sucesso, erro, coletivo, configuração e zero seleção; três integrações backend cobriram os modos, exclusividade, filtro administrativo e biblioteca, com TypeScript e Ruff aprovados em 2026-08-31.
- [x] 6.6 Atualizar os estados de seleção vazia e encerramento: manter privada administrativa com fotos disponíveis e zero selecionadas; quando não houver referência, explicar o encerramento e permitir retorno à Galeria pública autorizada; verificar histórico preservado e nova derivação posterior. Evidência: a privada administrativa continua apresentando as fotos disponíveis e resumo com zero escolhas; ao retirar a última referência exclusivamente `client`, a resposta informa o encerramento sem expor regra ao navegador e inclui retorno apenas quando o vínculo e a Galeria pública permanecem autorizados. A tela explica preservação de cadastro/histórico e oferece biblioteca como destino seguro. Nove testes de componente e três integrações backend comprovaram zero seleção, encerramento, retorno autorizado, compra histórica preservada e nova derivação posterior; TypeScript e Ruff passaram em 2026-08-31.
- [x] 6.7 Substituir em toda interface visível, mensagens de API exibidas e acessibilidade “Responsável” por “Cliente” e “Galeria-mãe”/“Acervo-fonte” por “Galeria pública”; verificar com busca automatizada limitada às superfícies de produto e testes de regressão das telas afetadas. Evidência: abas, painel, filtros estatísticos, mensagens HTTP e descrições de domínio usam agora exclusivamente “Cliente”, “Galeria pública” e “galeria privada”. A busca automatizada em `backend/app` e `frontend/app`, excluindo testes e caches, terminou sem ocorrências da terminologia antiga; sete regressões de interface, TypeScript e Ruff passaram em 2026-08-31.

## 7. Minimização de PII do OTP

- [x] 7.1 Adicionar fingerprint não reversível e permitir limpeza de nome, telefone e destinatário em claro nos registros transitórios; verificar migration aditiva, ausência de segredo em banco e compatibilidade de desafios ainda ativos. Evidência: a migration `20260831_0031` adiciona fingerprints HMAC e torna anuláveis somente os campos transitórios, preservando linhas ativas e oferecendo downgrade sanitizado; runtime preenche registros legados no próximo uso. O código OTP permanece somente em hash/cifra temporária e nunca em claro. Teste dedicado de upgrade/downgrade, reenvio legado, 42 regressões de autenticação/WhatsApp/identidade e Ruff passaram em 2026-08-31.
- [x] 7.2 Resolver identidade existente pelo telefone comprovado, preservar o nome cadastrado e limpar PII na negação terminal sem convite e depois do consumo autorizado, mantendo rate limit e auditoria pelo fingerprint; verificar telefone conhecido com nome divergente, telefone novo com link, concorrência e que não são criados `Client`, vínculo ou sessão no caminho negado. Evidência: o telefone validado é capturado apenas durante a transação de verificação, a resolução canônica reutiliza a cliente existente sem sobrescrever o nome e a finalização limpa desafio/entregas tanto no sucesso quanto nas negativas terminais. Rate limit e auditoria de OTP usam fingerprint salgado, enquanto capabilities/UUIDs permanecem rastreáveis. Testes diretos cobrem nome divergente, telefone novo, repetição e negação sem cliente/vínculo/sessão; a regressão concorrente das constraints também passou em 2026-08-31.
- [x] 7.3 Implementar limpeza periódica de desafios e entregas abandonados/expirados conforme janela documentada; verificar relógio controlado, reexecução idempotente e que OTP ainda utilizável não é limpo antecipadamente. Evidência: o worker agenda limpeza em intervalo configurável e `cleanup_expired_client_otp_pii` aplica retenção curta configurável, remove nome/telefone/cifra e terminaliza fila residual. Teste com relógio controlado comprovou uma limpeza, reexecução sem efeito e preservação de desafio ainda válido e de expirado dentro da janela; regressões do worker/WhatsApp passaram em 2026-08-31.
- [x] 7.4 Adicionar testes de integração que consultam diretamente o banco após OTP negado e após logins repetidos; comprovar ausência de PII transitória indevida e existência de um único `Client` por telefone verificado. Evidência: os testes consultam `AuthChallenge`, `WhatsAppDelivery`, `AuditEvent`, `Client`, `ClientPhone`, vínculos e sessões após negação e dois logins; comprovam campos em claro/cifra apagados, auditoria sem nome/telefone, nenhum cadastro no caminho negado e exatamente uma identidade/um vínculo no caminho autorizado. A suíte conjunta aprovou 42 testes em 2026-08-31.

## 8. Integração, documentação e validação

- [x] 8.1 Documentar o novo contrato `202`, modos de acesso, tokens/convites, herança, disponibilidade versus seleção, política por estado comercial, retenção mínima, limpeza física, recuperação de falhas e retenção OTP; verificar coerência com proposal, design e todos os delta specs reconciliados. Evidência: `docs/CICLO-DE-VIDA-E-ACESSO-DE-GALERIAS.md` consolida terminologia, inventários/operações/progresso, idempotência, modos/tokens, identidade, derivação, herança/snapshot, estados comerciais, storage/histórico, retomada/rollback e janela/configuração de minimização OTP, sem inventar prazo legal. O texto foi confrontado com proposal, design e deltas desta change em 2026-08-31.
- [x] 8.2 Executar testes backend focados e completos, Ruff, migrations em PostgreSQL descartável, testes de falha do worker e validação de autorização; corrigir regressões e registrar comandos/resultados. Evidência: `python -m pytest -q` aprovou 168 testes e manteve um `skip` esperado em 318,45 s; a intermitência de inspeção do índice após migrations em subprocesso foi corrigida descartando conexões SQLite antigas na fixture, e o teste do plano passou em 20 execuções isoladas e na suíte completa. As suítes focadas cobriram falha/retomada do worker, autorizações pública/privada, OTP, identidade e WhatsApp. `ruff check app tests` e todas as migrations `20260831_0018`–`0031` passaram; a varredura ampla anterior encontrou somente 12 avisos preexistentes de formatação/import em migrations históricas não relacionadas. Em PostgreSQL 17 descartável, `alembic upgrade head`, downgrade para `0030` e novo upgrade chegaram a `20260831_0031`; o container sem volume foi removido em 2026-08-31.
- [x] 8.3 Executar testes frontend focados e completos, lint, typecheck e build; corrigir regressões e registrar resultados, distinguindo avisos preexistentes. Evidência: `npm test -- --reporter=verbose` aprovou 82/82 testes em 18 arquivos; `eslint .` terminou com zero erros e 17 avisos preexistentes (`no-img-element` e duas navegações legadas), `tsc --noEmit` passou e `next build` compilou, tipou e gerou 14 páginas com sucesso em 2026-08-31. Três avisos React `act(...)` dos testes legados de telefone permaneceram informativos e sem falha.
- [x] 8.4 Executar `openspec validate improve-gallery-and-client-data-lifecycle --strict`, revisar o diff limitado à change e confirmar que nenhum dado, segredo ou arquivo de outro projeto foi incluído. Evidência: `npx @fission-ai/openspec validate improve-gallery-and-client-data-lifecycle --strict` aprovou a change; `git diff --check` terminou sem erro (somente avisos CRLF), a revisão de status/stat/nomes confirmou código, migrations, testes, documentação e reconciliações OpenSpec da Markina Gallery, preservando alterações preexistentes sem incluir artefatos temporários. Busca por assinaturas de chaves privadas/tokens em backend, frontend, OpenSpec, docs e Docker não encontrou correspondência, e a busca final das superfícies de produto não encontrou terminologia legada em 2026-08-31.
- [x] 8.5 Apresentar inventário, migration, impacto em armazenamento e plano de rollback antes de qualquer deploy; aguardar autorização humana explícita sem bloquear implementação e testes locais independentes. Inventário preparado em `deployment-inventory.md`: head `20260831_0031`, novo volume persistente `media-history`, novo segredo obrigatório e rollback conservador estão registrados. A lacuna de persistência histórica encontrada no inventário foi corrigida: Compose renderizado possui um mount em API e um no worker, continua publicando somente o Nginx e passou em `config --quiet`. O deploy agora preserva ou gera no próprio servidor o salt de fingerprint com 32 bytes, permissão `0600` e sem imprimi-lo, além de registrar espaço, serviços, imagens e volumes Markina antes da migration; os testes estruturais e de shell da política passaram. O proprietário autorizou explicitamente em 2026-08-31 a integração e o deploy de todo o trabalho para validação humana exclusivamente em homologação. Na repetição pré-integração, backend aprovou 168 testes com um skip esperado, frontend aprovou 82/82, lint não teve erros, typecheck/build passaram, OpenSpec aprovou 23/23 e o Compose publicou somente Nginx; SHA, CI, aprovação do Environment e evidências pós-deploy permanecem na task 8.6.
- [ ] 8.6 Após autorização, validar em homologação somente com dados sintéticos cadastro por telefone novo, reutilização de telefone existente, os três modos de acesso, token rotacionado/revogado, sessão já autenticada, acesso simultâneo a múltiplas Galerias públicas e privadas, roteamento de seleções adicionais, disponibilidade administrativa sem seleção, herança e snapshot comercial, bloqueio em `pending_review`, encerramento sem referências, exclusão completa da Galeria pública, desvinculação, histórico dos dois papéis, arquivos físicos, retomada após falha e limpeza OTP; não marcar concluída sem evidência de SHA, migration, healthchecks e aceite humano visual.
