## 1. Encerramento do benchmark e linha de base

- [x] 1.1 Concluir o workflow protegido de fechamento do lote `benchmark-636-20260907-01` e registrar em `integrate-private-facial-filter` a prova agregada de purge, flags seguras, worker inativo, portas e healthchecks, sem marcar conclusão antes da evidência ARM.
  - Evidência 2026-09-08: workflow `34232853622` verde no SHA `268bc77ec620809367e64c3266700a667f2ad8ef`; removeu 2.443 embeddings de uma galeria e comprovou `embeddings=0`, `candidates=0`, `references=0`, `pending_notifications=0`. Restaurou as três flags seguras, manteve `face-worker` inativo, Alembic em `20260907_0047 (head)`, serviços saudáveis e somente Nginx em `127.0.0.1:8080`.
- [x] 1.2 Reconciliar as tasks 8.5, 9.3, 10.4, 10.7 e 10.11–10.13 de `integrate-private-facial-filter`, aceitando 634/636 como benchmark válido, registrando as duas falhas terminais sem nova retentativa e validando a change estritamente.
  - Evidência 2026-09-08: 9.3, 10.11 e 10.12 foram concluídas com evidência; 8.5, 10.4, 10.7 e 10.13 receberam registro explícito das partes comprovadas e dos remanescentes migrados para esta change, sem falsa conclusão. A decisão humana aceitou 634/636 e encerrou novas retentativas.
- [x] 1.3 Inventariar todos os arquivos, modos, trailers, variáveis e testes exclusivos do benchmark privado, classificá-los como remover, generalizar ou preservar e verificar que nenhum item de produto seja excluído por engano.
  - Evidência 2026-09-08: `benchmark-cleanup-inventory.md` classifica workflow, scripts, testes, trailers, configuração/runtime e documentação, preservando explicitamente fila, criptografia, modelos, progresso, busca, seleção, retenção, purge e isolamento Compose.

## 2. Rollout persistente e gates de produto

- [x] 2.1 Modelar rollout/aprovação facial por ambiente e Galeria pública com UUIDs, UTC, estados, versões, aprovadores e escopo, criar migration Alembic aditiva e validar upgrade em banco limpo/existente e compatibilidade de rollback da aplicação.
  - Evidência 2026-09-08: `FacialRollout` e a migration aditiva `20260908_0048` registram ambiente, Galeria pública, estado, etapa, versões, referência/aprovador e timestamps UTC, com unicidade e checks; não existe backfill ou ativação implícita. Em container Python 3.13, modelos e upgrade de banco limpo/existente mais downgrade local aprovaram `6 passed`; Ruff direcionado passou.
- [x] 2.2 Implementar o repositório de rollout com transições `prepared|active|suspended|revoked`, concorrência segura e auditoria minimizada; testar transições inválidas, duplicidade, revogação e ausência de segredo/biometria.
  - Evidência 2026-09-08: `app.facial.rollout` implementa preparação, ativação, suspensão e revogação por ambiente/Galeria pública, com bloqueio pessimista, transições estritas, versões coerentes e auditoria sem aprovação, segredo ou biometria. Os testes direcionados de rollout, modelo e migration aprovaram no container Python 3.13.
- [x] 2.3 Exigir kill switch e rollout ativo na admissão e execução de indexação e busca; testar flag desligada, galeria fora da allowlist, suspensão entre enqueue/claim e ausência de ampliação por configuração parcial.
  - Evidência 2026-09-08: enqueue/reconciliação de índice e admissão de consulta exigem kill switch mais rollout ativo; workers repetem o gate após o claim e cancelam indexação ou consulta cujo escopo foi suspenso. Flag desligada, escopo não ativo, divergência de versão e suspensão entre enqueue/claim foram cobertos; 33 testes faciais passaram no runtime vetorial e Ruff direcionado aprovou.
- [x] 2.4 Integrar suspensão/revogação ao purge prioritário e à invalidação de consultas abertas, comprovando por testes que embeddings, candidatas e referências do escopo somem sem apagar fotos, seleções, pedidos ou histórico comercial.
  - Evidência 2026-09-08: suspensão e revogação invalidam consultas, candidatas, notificações e jobs de busca imediatamente e enfileiram purge idempotente com prioridade zero; o purge físico também limpa localizadores de consultas já canceladas. A prova agregada chegou a zero e fotos, seleções, pedidos e mídia de histórico comercial permaneceram intactos; 14 testes direcionados e Ruff passaram.
- [x] 2.5 Adaptar o painel administrativo para exibir somente estado técnico, progresso, cobertura, falhas e disponibilidade do rollout aplicável, validando carregamento, vazio, parcial, erro e acessibilidade por teclado.
  - Evidência 2026-09-08: o endpoint administrativo calcula a disponibilidade efetiva por kill switch, ambiente, escopo e versões e expõe somente `status`, `stage` e `available`; o painel mostra rollout, progresso, cobertura e categorias técnicas de falha sem referências de aprovação. Oito testes de componente, 16 testes backend, ESLint e Ruff direcionados passaram, incluindo vazio, parcial, erro e foco de teclado na retentativa.
- [x] 2.6 Permitir ao administrador refazer o reconhecimento facial incompleto ou falho de uma galeria habilitada, reconciliando o acervo existente e reutilizando jobs/arquivos de forma idempotente inclusive em lotes acima de 2.000 fotos.
  - Evidência 2026-09-08: o endpoint autenticado `facial-index/reprocess` reconcilia somente quando há fotos elegíveis sem índice e recoloca todas as falhas vigentes na fila sem criar upload, prévia ou job duplicado. O painel oferece `Refazer reconhecimento facial` apenas com rollout disponível. Trinta e um testes backend passaram, incluindo retentativa agregada de 2.003 falhas e recusa anônima; oito testes do componente e ESLint direcionado também passaram.

## 3. Workers, prioridade e backpressure

- [x] 3.1 Separar claims e entrypoints para classes `search`, `index` e `maintenance`, preservando o banco como fonte durável, idempotência e leases; testar isolamento de claims, retomada após crash e ausência de starvation de purge/cleanup.
  - Evidência 2026-09-08: o repositório filtra claims por classe (`search`, `index`, `maintenance`), o wake-up Redis usa filas derivadas por classe e três entrypoints dedicados reutilizam o banco durável, idempotência e leases existentes. Purge/cleanup são isolados de busca/backfill, purge mantém prioridade e leases expirados retomam sem permitir conclusão pelo worker antigo; 11 testes e Ruff passaram.
- [x] 3.2 Configurar serviços Compose sem porta e com limites independentes por classe, mantendo prévias e rotas interativas prioritárias; validar `docker compose config`, healthchecks e consumo ocioso sem modelos carregados.
  - Evidência 2026-09-08: o profile `facial` agora contém `face-search-worker` (512 MiB/0,75 CPU), `face-index-worker` (768 MiB/1 CPU) e `face-maintenance-worker` (256 MiB/0,25 CPU), todos sem portas, com healthcheck e entrypoint próprio. `docker compose config --quiet` passou; inspeção do modelo confirmou limites/healthchecks/zero portas, e o teste do worker de manutenção comprovou execução ociosa sem carregar modelos.
- [x] 3.3 Implementar medição de profundidade/idade da fila e limite de admissão antes de armazenar a referência, retornando indisponibilidade temporária retentável quando saturado; testar que requests aceitos nunca se perdem e requests recusados deixam zero temporários.
  - Evidência 2026-09-08: a admissão mede profundidade e idade do job de busca mais antigo no banco antes de criar request ou arquivo; limites configuráveis recusam com HTTP 503 e `Retry-After`. O teste saturado comprovou zero request e zero diretório temporário, enquanto o ensaio existente preservou 100 requests/jobs aceitos e isolados; 24 testes de busca/config/fila e Ruff passaram.
- [x] 3.4 Expor progresso/estimativa honestos e backoff de polling no contrato da cliente, interrompendo polling em terminal, revogação ou expiração; validar com relógio controlado e refresh/fechamento da aba.
  - Evidência 2026-09-08: o contrato informa itens restantes, estimativa explicitamente indisponível quando não há base observada e intervalo de polling por fase; a cliente aumenta o intervalo até 10 s quando não há progresso, restaura pelo backend e para em terminal, expiração ou 404 por revogação. Dezesseis testes frontend, 21 testes backend nos runtimes leve/vetorial, ESLint e Ruff passaram com relógio controlado.

## 4. Experiência autenticada da cliente

- [x] 4.1 Consolidar a recuperação backend da consulta facial mais recente por cliente e Galeria pública, com autorização repetida e expiração; testar navegador sem estado local, IDs trocados, vínculo removido e duas galerias concorrentes.
  - Evidência 2026-09-08: a recuperação mais recente filtra cliente, Galeria pública ativa, vínculo ativo e validade, ignorando canceladas, expiradas e não terminais vencidas; a UI já usa esse endpoint quando não há estado local. IDs trocados, vínculo removido, expiração e duas galerias simultâneas foram aprovados em 10 testes backend, com Ruff limpo.
- [x] 4.2 Validar o upload mobile-first, progresso, cancelamento e mensagens sanitizadas para zero rosto, múltiplos rostos e baixa qualidade, comprovando que a referência nunca é renderizada nem persistida no navegador.
  - Evidência 2026-09-08: a entrada aceita somente JPEG e solicita câmera frontal no mobile; a referência segue apenas no corpo da requisição e a sessão guarda exclusivamente o UUID opaco. Upload, progresso, cancelamento, remoção da retomada, ausência de `data:`/`blob:` e mensagens sanitizadas para zero rosto, múltiplos rostos e baixa qualidade foram aprovados em 15 testes frontend; ESLint direcionado passou.
- [x] 4.3 Garantir que `Melhores resultados encontrados` e `Outros resultados encontrados` apareçam no topo e reutilizem cards, favoritos, seleção e cotação existentes; testar ordenação, deduplicação, rejeição, seleção e retomada.
  - Evidência 2026-09-08: os dois grupos reutilizam o mesmo renderer do acervo antes da coleção integral, deduplicam por foto conservando o menor `rank` e mantêm seleção, cotação e rejeição no contexto facial. Após a primeira seleção, o favorito reutiliza o endpoint da galeria privada e seu estado volta no payload público. Quinze testes frontend, um teste backend de integração, ESLint e Ruff passaram, cobrindo ordem, deduplicação, seleção, favorito, cotação, rejeição e retomada.
- [x] 4.4 Provar por testes de API e banco que consulta sem seleção cria zero galeria privada, membership, seleção, pedido e pagamento, e que a primeira escolha reutiliza exatamente o fluxo manual para `Galeria pública + cliente`.
  - Evidência 2026-09-08: um teste integrado de API/banco lê candidatas prontas e comprova contagens `(0, 0, 0, 0, 0)` para galeria privada, membership, seleção, pedido e comunicação de pagamento. A primeira seleção facial cria exatamente uma galeria/membership/seleção com cotação de R$ 7,00; repetir a escolha pelo endpoint manual reutiliza os mesmos registros e payload comercial. Teste passou no código atual montado em container com SQLite descartável; Ruff passou.
- [ ] 4.5 Validar visualmente em homologação autenticada os fluxos administrativo e cliente em mobile/desktop, sem versionar capturas identificáveis, e registrar a evidência humana ou o bloqueio real.
  - Bloqueio 2026-09-08: `https://markina-homolog.duckdns.org/admin` respondeu com a interface, porém a sessão disponível foi recusada com “Acesso administrativo indisponível”. A inspeção autenticada mobile/desktop requer login/OTP humano e, posteriormente, o deploy desta change; nenhuma captura identificável foi criada ou versionada.
  - Evidência humana parcial 2026-09-08: após o rollout canary, o painel administrativo autenticado apresentou “Reconhecimento concluído”, disponibilidade na galeria e etapa canary; mostrou 636/636 fotos prontas, zero aguardando prévia/indexação, processando, na fila ou com falha, além de cobertura de 623/636 fotos com rosto (98%) e 2.472 rostos detectados. Nenhuma captura ou identificação foi versionada. Ainda faltam o fluxo autenticado da cliente e a conferência dos dois breakpoints.

## 5. Fluxo infantil e direitos

- [x] 5.1 Modelar prova não biométrica de representação legal com escopo, versão, validade e revogação, usando migration aditiva; testar autoridade, expiração, troca de cliente/galeria e minimização dos campos.
  - Evidência 2026-09-08: `FacialLegalRepresentation` e a migration aditiva `20260908_0049` armazenam somente UUIDs, referências opacas, autoridade/método, versão, validade e revogação, sem nome, documento, imagem ou biometria do menor. Criação, autoridade inválida, troca de cliente/galeria/versão, expiração, revogação idempotente, auditoria minimizada, banco limpo/existente e downgrade foram aprovados em 5 testes; Ruff passou.
- [x] 5.2 Subordinar a admissão de referência infantil à representação válida e ao consentimento específico versionado, mantendo a indexação administrativa independente; testar menor recusado sem prova e aceito somente no escopo comprovado.
  - Evidência 2026-09-08: a disponibilidade infantil agora depende de prova ativa do mesmo cliente, Galeria pública e versão; a interface envia somente seu UUID opaco e a admissão repete escopo, validade, revogação e consentimento antes de armazenar a referência. Ausência/troca/expiração são recusadas, enquanto a indexação administrativa permanece sem classificação etária nem consulta à prova. Treze testes backend, 21 frontend, Ruff e ESLint passaram.
- [x] 5.3 Remover `FACIAL_MINOR_SEARCH_ENABLED` como autorização suficiente, mantendo compatibilidade fail-closed durante a migração e eliminando o gate transitório após testes de configuração e deploy.
  - Evidência 2026-09-08: o campo foi eliminado de `FacialSettings`, parser, política, admissão, Compose e `.env.example`; um valor legado `true` é ignorado e não disponibiliza busca infantil sem representação persistida válida. Trinta e oito testes de configuração, busca, representação, rollout, política e runtime passaram; Ruff e `docker compose config --quiet` aprovaram. Referências remanescentes pertencem somente às ferramentas/histórico do benchmark já classificados para remoção na task 7.4.
- [x] 5.4 Implementar revogação e atendimento a direitos com inventário e prova agregada por escopo autorizado; testar exclusão idempotente dos derivados sem alcançar histórico comercial com base independente.
  - Evidência 2026-09-08: o inventário por UUID de representação conta apenas requests infantis ativos, referências, candidatas e notificações pendentes; o atendimento revoga a prova, elimina o arquivo, apaga candidatas e cancela requests/jobs/notificações com relatório agregado. A repetição produz zero alterações. Seis testes de purge/direitos passaram e comprovaram que embedding administrativo, foto, seleção, pedido e mídia histórica comercial permanecem intactos; Ruff passou.

## 6. Observabilidade, segurança e capacidade

- [x] 6.1 Definir e documentar SLOs e limites para admissão, p95 de consulta, idade de fila, taxa de falha, purge, CPU e memória, validando que cada métrica e alerta contém somente ambiente, tipo, estado e contagens agregadas.
  - Evidência 2026-09-08: `OPERACAO-BUSCA-FACIAL-PRODUCAO.md` fixa disponibilidade de admissão, p95 de consulta, idade máxima, falha terminal, retenções e tetos por classe para CPU/memória; os mesmos limites são constantes tipadas. O validador exige exatamente `environment`, `type` e `state` em allowlists e valor numérico finito. Quinze testes direcionados e Ruff passaram.
- [x] 6.2 Implementar métricas e alertas para as três classes de worker, retenção e kill switch; testar payloads contra imagem, embedding, score, landmarks, caixa facial, nome inferido e telefone.
  - Evidência 2026-09-08: o coletor autenticado agrega admissão, kill switch, contagem/idade/falha/duração de `search|index|maintenance`, p95 de consulta, retenção vencida, CPU e memória, com alertas de divergência e SLO. Testes recusam nomes/dimensões com imagem, foto, embedding, vetor, score, landmarks, caixa facial, nome inferido, telefone, IDs de negócio e valores não numéricos; o endpoint não autenticado retorna 403 e o autenticado expõe somente agregados.
- [x] 6.3 Criar ensaio automatizado de pelo menos 100 consultas simultâneas com isolamento por cliente/galeria, backpressure e restart de worker; validar zero perda de jobs aceitos, zero cruzamento e recuperação terminal.
  - Evidência 2026-09-08: um ensaio com barreira e 100 threads admitiu 100 referências sintéticas para 100 clientes distribuídas em duas galerias; persistiu exatamente 100 requests/jobs e recusou a 101ª antes de criar temporário. Após expirar o lease do primeiro claim e abrir nova sessão, outro worker retomou o mesmo job e levou os 100 a `completed`/`no_face`, apagando todas as referências; as 100 leituras conservaram o par cliente/galeria e a leitura cruzada foi recusada. Teste isolado passou em 20,64 s; Ruff passou.
- [ ] 6.4 Executar teste de carga autorizado no ARM de homologação, registrar p50/p95, throughput, fila, CPU, memória e falhas agregadas e comparar com os SLOs antes de liberar rollout de produção.
  - Bloqueio 2026-09-08: a execução externa exige primeiro o deploy da change e o rollout persistente em homologação, além de inventário ARM imediatamente anterior, corpus sintético ou explicitamente autorizado, janela e autorização humana específica para carga. O ensaio local de 100 consultas não substitui essa evidência; nenhuma carga foi disparada contra o host compartilhado.
- [x] 6.5 Executar revisão de segurança de autorização, AEAD/AAD, rotação de chaves, armazenamento temporário, logs, rate limiting e abuso; corrigir achados críticos/altos e registrar evidência verificável.
  - Evidência 2026-09-08: `REVISAO-SEGURANCA-FACIAL-PRODUCAO.md` cobre autorização repetida, AEAD/AAD, rotação, temporários, retenção, logs/métricas, backpressure e abuso. O achado alto `SEC-FACIAL-001` foi corrigido com rate limiting persistido por fingerprints do vínculo e rede, sem UUID/IP em claro. Ruff passou e 51 testes consolidaram busca, worker, rollout, representação, purge, criptografia, armazenamento e limite no runtime facial NumPy/OpenCV; não restou achado crítico/alto conhecido.
- [ ] 6.6 Executar calibração/equidade com corpus permitido e critérios documentados, sem inferir atributos no runtime, e bloquear produção enquanto limiar ou grupos relevantes não possuírem aprovação humana registrada.
  - Bloqueio parcial 2026-09-08: os critérios e o baseline sintético estão documentados em `CALIBRACAO-E-EQUIDADE-FACIAL.md`; o gate persistente exige aprovação humana de todos os grupos e correspondência exata do limiar/modelo em produção, falhando fechado após revogação ou drift. Ainda falta executar a calibração representativa com corpus permitido e obter a aprovação humana; nenhum corpus novo foi presumido ou acessado.

## 7. Deploy persistente e higienização

- [x] 7.1 Alterar o deploy para detectar e preservar combinações coerentes `flag=false/worker ausente` e `flag=true/worker saudável`, atualizando API, worker de mídia e workers faciais e testando rollback seguro nos dois estados.
  - Evidência 2026-09-08: `deploy-homolog.sh` lê uma única flag persistente e aceita somente `false` com as três classes ausentes ou `true` com `search|index|maintenance` saudáveis e configuração efetiva coerente em API/worker/workers. O deploy atualiza API, web, worker de mídia e, quando habilitadas, as três classes faciais; healthcheck e rollback preservam o mesmo conjunto. Validação sintática, testes shell dos dois estados, divergência e rollback ativo/inativo, além das políticas operacionais existentes, passaram.
- [x] 7.2 Criar operação protegida de ativação/suspensão por ambiente e etapa `dark|canary|limited|general`, exigindo SHA, inventário, backup, gates, allowlist e confirmação; testar recusa de campo ausente, escopo divergente e produção não aprovada.
  - Evidência 2026-09-08: `app.facial.rollout_operation` e o CLI `app.facial.manage_rollout` exigem SHA integral, referências opacas de inventário/backup/gates, conjunto exato de sete gates, allowlist explícita sem duplicidade, administrador e confirmação vinculada a ação/ambiente/etapa. O recibo aditivo `20260908_0051` guarda somente digest e contagem do escopo. Dezoito testes de operação, rollout, calibração e migration passaram; Ruff, `git diff --check` e o entrypoint `--help` também aprovaram.
- [ ] 7.3 Migrar homologação do manifesto de benchmark encerrado para rollout persistente, deixar `FACIAL_PROCESSING_ENABLED=true` entre deploys e validar interface adulta, jobs, chaves, modelos, portas e healthchecks com inventário imediatamente anterior.
  - Bloqueio 2026-09-08: a implementação local está pronta, mas a migração exige inventário imediatamente anterior e mutação da homologação compartilhada por deploy/operação protegida. A autorização histórica do benchmark encerrado não autoriza esse novo deploy; a task permanece pendente até apresentação do inventário e autorização humana explícita, sem bloquear trabalho local independente.
  - Execução 2026-09-08: após autorização humana, o merge `602750a136d95ae16866203a89e4a2684af1c745` passou backend, frontend, OpenSpec e gitleaks no run `34271508558`, mas o job protegido `deploy-homolog` `102215658919` falhou em 2 s no início remoto. A causa foi o script novo consultar via `docker compose ps` três serviços que ainda não existiam no Compose do checkout anterior; a correção passa a consultar containers exclusivamente pelos labels do projeto/serviço Docker durante o bootstrap e possui regressão que simula o Compose anterior desconhecendo esses workers. Testes shell/política e sintaxe passaram localmente; novo push e deploy foram autorizados, mas a task continua pendente até a evidência remota completa.
  - Execução 2026-09-08: o run corretivo `34276334812`, SHA `22f9cdb1853748fa7424f10e38a04164cae99837`, comprovou inventário, backup, checkout, migrations até o novo head, preparação íntegra dos modelos ARM e criação das três classes; falhou no healthcheck de `face-maintenance-worker`, ainda sem diagnóstico suficiente nesse run. A primeira hipótese acionável eliminou a reciclagem por contagem para maintenance, que não carrega modelos e deve drenar lotes acima de `FACIAL_MAX_JOBS_PER_PROCESS`; um teste força backlog acima do limite e exige o segundo ciclo. O deploy também passou a registrar somente estado, health, exit code, OOM, reinícios e presença de erro, sem conteúdo de jobs ou PII. O run seguinte permitiu refinar a causa conforme a evidência abaixo.
  - Execução 2026-09-08: no run `34278110913`, SHA `c436eb423aa8517a65f2839dc4ebfe4af1ea43be`, o diagnóstico comprovou `face-maintenance-worker` em reinício, `exit_code=1`, `oom=false` e `restart_count=10`, descartando OOM e encerramento saudável por limite. Search/index permaneceram saudáveis no runtime facial completo; maintenance era o único construído pela imagem básica. A correção alinha sua imagem ao `Dockerfile.face`, mantendo o carregamento do provider desnecessário para purge/cleanup, e limita diagnóstico futuro a classe de exceção e estado agregado, sem mensagem nem conteúdo de job.
  - Evidência remota 2026-09-08: o run `34279446391` e o job protegido `102241757540` publicaram com sucesso o SHA `4b33ed2ce1dd73a7bd503a51cfd2166460d5c171`. O inventário confirmou backup lógico exclusivo, Alembic `20260908_0051 (head)`, modelos ARM preparados, configuração/chave facial válida nos processos, `FACIAL_PROCESSING_ENABLED=true` persistente e `search|index|maintenance` saudáveis e sem portas; apenas o Nginx permaneceu em `127.0.0.1:8080`. `/healthz`, `/api/health`, `/admin` e `/` responderam HTTP 200 externamente. A task permanece aberta somente porque o rollout persistente de uma allowlist de galeria e a busca adulta autenticada ainda precisam de operação protegida e validação humana; o kill switch sozinho não concede esse escopo.
  - Continuação 2026-09-08: para tornar acionável a operação persistente sem acesso SSH manual, será adicionada uma ponte protegida de homologação com modos separados de inventário, ativação e suspensão. Ela fica limitada a uma Galeria pública por execução, confirma o SHA já publicado, repete healthchecks/portas/configuração, cria backup lógico antes da mutação, não imprime UUID de galeria ou administrador e reconcilia paginadamente o acervo já enviado antes de reiniciar somente o worker de índice. A validação humana da cliente permanece posterior à ativação.
  - Execução 2026-09-08: o deploy `34288885236` publicou o SHA `428c04792207c285b46c6582efadc61462e7f572` com todos os gates verdes e runtime facial saudável. A primeira ativação protegida `34289853993` confirmou topologia, portas, serviços, endpoints e migration no head, mas falhou fechada antes do backup e de qualquer mutação porque o preflight aceitava somente o rótulo canônico `homolog`, enquanto o runtime histórico usa `APP_ENV=homologation`. A correção canonicaliza apenas esse alias no rollout, preservando a igualdade estrita das credenciais com o ambiente real; 18 regressões de rollout/operação, Ruff, política operacional, OpenSpec estrito e `git diff --check` passaram. A task permanece pendente até novo deploy, ativação e validação humana.
  - Diagnóstico 2026-09-08: o deploy corretivo `34291010949` publicou com sucesso o SHA `0381606f64ecdaed133f1b9d6d359019fa4c981b`, mas a ativação `34291530750` mostrou que o `APP_ENV` efetivo também não é `homologation`; novamente o preflight encerrou antes de backup ou mutação. O inventário passa a registrar somente `enabled` e o rótulo não sensível do ambiente para eliminar suposições antes da próxima correção.
  - Evidência de inventário 2026-09-08: o run somente leitura `34292166523` comprovou `FACIAL_PROCESSING_ENABLED=true` e `APP_ENV=staging`, além de todos os serviços saudáveis e apenas Nginx em `127.0.0.1:8080`; encerrou antes do backup e sem mutação. A correção final preserva `staging` como escopo persistente interno e o associa a homologação somente no operador fixado por projeto, checkout, portas e subdomínio, derivando a confirmação interna sem relaxar a autorização humana `HOMOLOG`.
  - Evidência remota 2026-09-08: o run `34293012960` publicou com sucesso o SHA `4399afa4761fcd64f34616e163995784f8b86ecf`; backend, frontend, OpenSpec, gitleaks e o deploy protegido passaram. Após aprovação humana, a ativação canary `34293453612` repetiu inventário de topologia, serviços, portas, migration `20260908_0051 (head)` e runtime `enabled=true/environment=staging`, criou backup lógico exclusivo antes da mutação e persistiu uma allowlist de uma galeria com os sete gates obrigatórios. A reconciliação paginada varreu 636 fotos em sete páginas, criou ou reaproveitou 636 jobs elegíveis sem duplicar arquivos e reiniciou somente o worker de índice, que retornou saudável. A task permanece aberta apenas para acompanhar o resultado agregado no painel autenticado e validar a busca adulta pela interface cliente.
  - Evidência humana 2026-09-08: o painel autenticado confirmou o término de todos os 636 jobs, com zero itens pendentes, em processamento, na fila ou falhos; 623 fotos tiveram ao menos um rosto detectado (98%), somando 2.472 rostos. As 13 fotos sem rosto utilizável permaneceram corretamente contabilizadas como processamento concluído. Resta somente validar uma consulta adulta autenticada pela interface da cliente para concluir a task.
- [x] 7.4 Remover workflows, modos, tokens, trailers, variáveis e testes exclusivos do benchmark conforme o inventário da task 1.3, preservando utilitários genéricos de observabilidade/carga; validar que CI, deploy, rollback e runtime não referenciam a janela privada.
  - Evidência 2026-09-08: o workflow facial privado, operador/monitor do lote e seus três testes foram removidos; CI voltou a um único deploy comum sem trailers, pausas ou jobs do benchmark. Configuração, Compose, `.env.example` e worker não possuem janela nem variáveis `FACIAL_HOMOLOG_*`. A política estrutural nova preserva observabilidade, harness genérico e deploy. Ruff, 15 testes de configuração/runtime/rollout, `docker compose config`, parse YAML, testes shell e busca negativa passaram.
- [x] 7.5 Atualizar `.env.example`, runbooks, RIPD, matriz controlador/operador e resposta a incidente com defaults seguros e ciclo de produção, verificando ausência de segredo e coerência com proposal, specs e design.
  - Evidência 2026-09-08: `.env.example` documenta flag facial desligada por default, rollout independente e campos secretos vazios. O runbook operacional e o checklist cobrem `dark → canary → limited → general`, gates, allowlist, suspensão e primeiro deploy seguro. `GOVERNANCA-E-INCIDENTE-BUSCA-FACIAL.md` registra RIPD versionado, matriz controlador/operador, direitos, contenção, rotação, comunicação e critérios de recuperação. A política estrutural, Ruff, parse YAML, busca de valores sensíveis e `git diff --check` passaram.

## 8. Validação e rollout de produção

- [x] 8.1 Executar Ruff e toda a suíte backend, lint/testes/build do frontend, migrations, Compose, testes operacionais, OpenSpec estrito, gitleaks e `git diff --check`; investigar e corrigir regressões causadas pela change.
  - Evidência 2026-09-08: no runtime facial Python 3.13, a suíte backend completa aprovou inicialmente `444 passed, 1 skipped` em 25m57s; após a regressão do worker, o run final `34279446391` aprovou `445 passed, 1 skipped` em 3m55s e Ruff completo. Alembic subiu banco descartável até `20260908_0051 (head)`. No frontend, `npm run lint` passou com zero erros e 22 avisos preexistentes após ignorar somente `.pytest_cache`, `172` testes passaram e o build Next.js/TypeScript concluiu. Compose, scripts shell, políticas operacionais, parse YAML, OpenSpec estrito e `git diff --check` passaram. Gitleaks v8.24.3 encontrou zero vazamentos nos 199 commits/8,53 MB e também nos diretórios versionáveis atuais com a allowlist sintética do projeto; dependências e builds ignorados ficaram fora da varredura de fonte.
- [x] 8.2 Apresentar inventário de produção, projeto/serviços, portas/subdomínio, capacidade, backup, migrations, gates, canary e rollback de impacto zero, aguardando autorização humana explícita antes de qualquer mutação.
  - Evidência 2026-09-08: `PRODUCAO-FACIAL-INVENTARIO-E-PLANO.md` registra a partir de inspeção local 10 serviços, seis volumes, redes, única porta Compose, três workers sem porta e teto facial de 2 CPU/1.536 MiB; também cobre migration `0051`, dump restrito, gates, canary e rollback confinado à Markina. O documento deixa explícitos SHA publicável, subdomínio, binding, host/capacidade e backup reais ainda pendentes. Nenhum host ou recurso de produção foi acessado ou alterado.
- [ ] 8.3 Após autorização, publicar em produção com `FACIAL_PROCESSING_ENABLED=false`, validar migrations, modelos, chaves, containers, portas, healthchecks e smoke sem PII e registrar SHA/runs.
  - Bloqueio 2026-09-08: não existe ainda SHA integral publicável desta change, inventário imediatamente anterior do host, subdomínio/binding de produção nem autorização explícita para deploy. Produção não foi acessada; a flag segura permanece o único estado permitido no primeiro deploy.
- [ ] 8.4 Ativar canary somente para a allowlist aprovada, observar os SLOs pelo período definido e validar indexação, consulta adulta, retenção, purge e rollback antes de qualquer ampliação.
  - Bloqueio 2026-09-08: depende do deploy 8.3, do teste ARM 6.4, da calibração 6.6 e de allowlist/janela/aprovação humanas específicas. Nenhum canary foi ativado.
- [ ] 8.5 Habilitar o fluxo infantil somente após aprovação jurídica/humana da representação e do consentimento; manter esse escopo desligado e registrar o bloqueio enquanto qualquer gate estiver pendente.
  - Bloqueio 2026-09-08: o gate técnico exige representação persistida, termos/consentimento correspondentes e vínculo vigente, mas a aprovação jurídica/humana de produção ainda não existe. O escopo infantil permanece desligado; nenhuma flag isolada o habilita.
- [ ] 8.6 Promover `canary → limited → general` somente com evidência e aprovação em cada etapa, ou reverter ao estado anterior ao primeiro alerta crítico; sincronizar specs principais e arquivar a change apenas após revisão humana final.
  - Bloqueio 2026-09-08: nenhuma etapa de produção foi iniciada; promoção, sincronização da spec principal e arquivamento permanecem proibidos até evidência das etapas anteriores e revisão humana final.
