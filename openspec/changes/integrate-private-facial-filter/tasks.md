## 1. Contratos e fundação

- [x] 1.1 Atualizar `INSTRUCOES_EXECUTOR_CLAUDE_CODE.md`, `DIRETRIZES_FRONTEND_MARKINA_GALLERY.md` e a Fase 7 do `ROADMAP_ARQUITETURA.md` para distinguir galeria anônima proibida de filtro na Galeria pública autenticada, preservando revisão do fotógrafo quando houver ampliação de acesso; verificar coerência textual com proposal, specs e design.
- [x] 1.2 Confrontar os contratos e o código de `consolidate-shared-private-galleries-and-progressive-sales`, registrar qualquer dependência ainda ausente e validar por testes existentes que biblioteca agrupada, seleção pública persistente e unicidade `Galeria pública + cliente` estão disponíveis antes do acoplamento.
  - Evidência 2026-09-05: `DerivedGalleryMembership` impõe `uq_membership_parent_client`, `PhotoSelection` inclui `client_id` na unicidade e `derive_client_selection` reutiliza o resolvedor transacional. Quatro regressões direcionadas de biblioteca agrupada, primeira seleção, isolamento e corrida passaram (`4 passed`); nenhuma dependência funcional ausente foi encontrada.
- [x] 1.3 Incorporar manifesto, atribuições e download verificado de YuNet/SFace em build facial separado, sem pesos no Git; testar falha fechada para hash, versão, arquitetura ou licença divergente.
  - Evidência 2026-09-05: `Dockerfile.face` usa manifesto fixado na revisão oficial `47534e2`, valida tamanho/SHA-256/licença/arquitetura e mantém os pesos fora do Git; Ruff passou e `test_facial_model_assets.py` aprovou `3 passed`, incluindo bytes, versão, origem, arquitetura e licença divergentes.
- [x] 1.4 Alinhar mandato, roadmap, contexto OpenSpec e runbook à homologação privada com dados reais de adultos e menores, exigindo upload pelo administrador/fotógrafo, origem documentada, autorização humana explícita por execução, acesso autenticado, criptografia, retenção mínima e exclusão controlada; preservar produção bloqueada até seus gates próprios.
  - Evidência 2026-09-07: `AGENTS.md`, o mandato, o roadmap, `openspec/config.yaml` e o runbook passaram a permitir lote real de adultos e menores somente na homologação privada controlada, sem estender a autorização à produção ou ao fluxo infantil comum. A busca por proibições categóricas não encontrou regra normativa remanescente nesses documentos, `git diff --check` passou e a validação OpenSpec estrita aprovou a change (`1 passed, 0 failed`).

## 2. Schema aditivo e configuração

- [x] 2.1 Modelar política, embeddings, jobs, requests, candidatas e outbox facial com UUIDs, UTC, checks/FKs de escopo, estados, índices e defaults desativados; validar modelos SQLAlchemy sem segredo, imagem ou score persistido indevidamente.
  - Evidência 2026-09-05: as estruturas faciais aditivas foram modeladas com envelopes `LargeBinary`, snapshot mínimo por foto, FKs compostas e checks de estado/progresso; `Base.metadata.create_all` passou em SQLite, Ruff passou e `test_facial_models.py` aprovou `4 passed`.
- [x] 2.2 Criar migration Alembic aditiva no head vigente, com upgrade vazio e compatibilidade de rollback da aplicação; testar upgrade em banco limpo e existente, constraints entre galerias e ausência de ativação/backfill implícitos.
  - Evidência 2026-09-05: a revision `20260905_0043` cria somente as tabelas faciais (incluindo itens mínimos do snapshot) e a chave composta de escopo de foto, sem inserir política, embedding, job, consulta ou candidata. Ruff passou; `test_facial_migration.py` validou upgrade em banco limpo e em `0042` com foto preexistente preservada, FKs/uniques compostas e downgrade local. A validação conjunta com os modelos aprovou `6 passed`.
- [x] 2.3 Adicionar configurações documentadas para kill switch, versões jurídicas/modelo/calibração, AEAD, retenção, fila e recursos, mantendo valores seguros no `.env.example`; testar startup fail-closed e garantir ausência de segredos no Git/frontend.
  - Evidência 2026-09-05: `app.facial.config` centraliza flag global, vínculo ao `APP_ENV`, manifesto/modelos verificados, versões jurídicas e técnicas, chaveiro AEAD versionado, retenções máximas e limites de worker/upload. A feature permanece `false` no `.env.example`, menores continuam bloqueados e chaves não aparecem no `repr` nem no frontend. Ruff passou e os testes de configuração/modelos aprovaram `7 passed`, incluindo configuração incompleta, ambiente divergente, retenção insegura e gate infantil.

## 3. Criptografia e provedores isolados

- [x] 3.1 Implementar envelope AEAD versionado com AAD de ambiente/galeria/foto ou request/modelo, rotação explícita e erros sanitizados; testar round-trip, adulteração, troca de escopo e chave ausente.
  - Evidência 2026-09-05: `FacialCipher` usa AES-256-GCM, nonce aleatório e AAD canônica com ambiente, galeria, foto/request, finalidade, modelo e versão do dado; aceita chave anterior somente enquanto autorizada e faz rotação explícita para a chave ativa. Ruff passou e `test_facial_crypto.py` mais configuração aprovaram `12 passed`, cobrindo round-trip, adulteração, transplante entre cinco dimensões de escopo, chave removida e sanitização.
- [x] 3.2 Adaptar o provider YuNet/SFace validado no spike para detectar zero ou mais rostos do acervo e exatamente um na consulta, sem importar ferramentas de benchmark no runtime; testar dimensões, normalização e estados de qualidade com fixtures sintéticas.
  - Evidência 2026-09-05: o runtime isolado `app.facial.provider` carrega somente YuNet/SFace, aceita zero ou mais faces no acervo, normaliza exatamente 128 dimensões e exige uma única face tecnicamente utilizável na referência. Não há import de `scripts.face_spike`; Ruff passou e `test_facial_provider.py` aprovou `3 passed` com fixtures sintéticas de zero/uma/múltiplas faces, embedding inválido, desfoque, baixa confiança e rosto pequeno.
- [x] 3.3 Implementar armazenamento temporário cifrado por request com limites de path, bytes, pixels, formato e prazo; testar upload malformado, decompression bomb, cancelamento e limpeza idempotente sem sair do diretório autorizado.
  - Evidência 2026-09-05: `FacialReferenceStore` valida JPEG, bytes e pixels antes da gravação, trata `DecompressionBomb`, cifra conteúdo e localizador com AAD do request/galeria/modelo, usa escrita atômica e restringe nomes ao diretório dedicado. Ruff passou e a suíte conjunta de crypto/store aprovou `11 passed`, incluindo formato inválido, limites, transplante de locator, arquivo externo preservado e exclusão repetida.
- [x] 3.4 Implementar avaliador técnico OpenCV versionado por rosto correspondente (nitidez local, corte, tamanho/proeminência e pose), com bandas `best|other` não destrutivas; testar grupos, rosto periférico, desfoque e corte sem inferir atributos ou estética.
  - Evidência 2026-09-05: `assess_face_quality` classifica somente a face correspondente com nitidez local, enquadramento, tamanho relativo, proeminência no grupo e proxies geométricas de pose; retorna apenas `best|other`, mantendo indicadores detalhados destinados ao envelope cifrado e sem campo de idade, gênero, emoção, estética ou identidade. Ruff passou e provider/qualidade aprovaram `10 passed` com casos sintéticos de grupo, face pequena/periférica, desfoque, corte e pose.
- [ ] 3.5 Executar spike comparativo isolado do OFIQ no ARM com corpus sintético, licenças de código/pesos/dependências, throughput e memória; registrar decisão sem acoplar o runtime antes da aprovação dos gates.
  - Bloqueio 2026-09-05: a execução comparativa exige provisionar dependências OFIQ e medir no host ARM compartilhado. Nenhuma alteração nesse ambiente será feita sem inventário e autorização operacional específicos; o runtime principal permanece desacoplado do OFIQ e as tasks locais independentes continuam.

## 4. Jobs, indexação e purge

- [x] 4.1 Implementar repositório e dispatcher de jobs faciais com idempotência, lease, retentativa, prioridade e erros sanitizados; testar concorrência, retomada após crash e ausência de bloqueio da fila de mídia.
  - Evidência 2026-09-05: `FacialJobRepository` usa idempotency key, `FOR UPDATE SKIP LOCKED`, token/expiração de lease, prioridade, progresso e retentativa terminal; `FacialJobDispatcher` persiste antes do wake-up Redis e toca somente a fila facial. Ruff passou e `test_facial_jobs.py` aprovou `5 passed`, cobrindo despacho duplicado, reserva exclusiva, prioridade de purge, retomada após lease expirado, rejeição do worker antigo e erro sanitizado.
- [x] 4.2 Enfileirar indexação somente após `admin_preview` limpa e `client_preview` protegida prontas sob política ativa, usando fingerprint exclusivo da variante limpa e backfill único paginado ao ativar; testar ausência de marca d'água no insumo facial, scan recorrente ou exposição pública da variante interna, além de preservar upload/publicação quando a face está desativada ou falha.
  - Evidência 2026-09-06: fingerprint, backfill, status e engine passaram a usar exclusivamente `admin_preview`, exigindo também `client_preview` pronta antes da indexação. A rota pública continua servindo apenas a variante protegida; alteração posterior da proteção reenfileira somente `client_preview`, preservando bytes/fingerprint internos. As regressões direcionadas e a suíte integral do backend aprovaram esse fluxo (`341 passed, 1 skipped`).
- [x] 4.3 Implementar no `face-worker` a substituição atômica do índice e qualidade por foto/modelo/fingerprint e a comparação linear vetorizada apenas dentro da galeria; testar múltiplos rostos, zero rosto, troca de versão e sentinela de outro evento.
  - Evidência 2026-09-05: `replace_photo_index` revalida foto, prévia e política, calcula fingerprint, cifra embedding/indicadores por face e substitui o conjunto em savepoint; `search_gallery_index` decifra e compara vetorizadamente somente registros da galeria/versão/fotos disponíveis, reduz múltiplas faces por foto e ordena `best` antes de `other`. O executor consome o lease e persiste progresso/conclusão. Ruff passou e engine/jobs aprovaram `8 passed`, incluindo rollback em falha intermediária e sentinela idêntica de outra galeria ausente do resultado.
- [x] 4.4 Integrar purge prioritário a desativação, revogação, troca de modelo, exclusão de foto/galeria e limpeza reconciliadora; testar que índice e candidatas somem mesmo quando mídia/histórico comercial permanecem.
  - Evidência 2026-09-05: purge prioridade zero é enfileirado por suspensão/revogação, mudança de versão e foto; exclusões de foto/Galeria pública eliminam inferências antes dos registros operacionais, preservando mídia referenciada e histórico. O worker de purge não cancela o próprio lease, e o reconciliador remove índice/candidatas com política ou foto inválida sem reprocessar conteúdo. Ruff passou e `test_facial_purge.py` aprovou `4 passed`, incluindo repetição idempotente e origem/mídia preservadas.
- [x] 4.5 Implementar espera bloqueante, limite de jobs por processo e descarregamento dos modelos após ociosidade configurável; testar fila vazia sem reprocessamento/CPU ativa e retomada após reload.
  - Evidência 2026-09-05: `BlockingFacialWorker` consulta jobs duráveis, bloqueia em `BRPOP` quando vazio, carrega o provider somente sob demanda, descarrega após o timeout ocioso e recicla ao atingir o limite configurado por processo; não existe varredura de galeria no loop. Ruff passou e `test_facial_runtime.py` aprovou `2 passed`, provando espera vazia sem carregar modelo, descarga, novo carregamento e limite de jobs.
- [x] 4.6 Substituir a ativação manual por política técnica interna automática: garantir política e job quando as prévias ficam prontas e executar reconciliação idempotente única no startup para galerias ausentes/divergentes, sem scan recorrente ou reindexação de fotos compatíveis; testar feature global desligada, upload novo, upgrade e reinício.
  - Evidência 2026-09-06: `ensure_automatic_policy` cria/reconcilia a política técnica sem ator administrativo; o hook de `admin_preview` garante a política e usa a geração no idempotency key. O `face-worker` executa uma reconciliação finita no startup e só pagina galerias ausentes/divergentes. Ruff passou e política/indexação aprovaram `11 passed`, cobrindo gate global desligado, upload, política legada pendente, reinício sem scan e versão divergente com purge e novo índice.

## 5. Política e operação administrativa

- [x] 5.1 Implementar leitura/preparação/ativação/suspensão da política facial com inventário de requisitos ausentes e auditoria sem biometria; testar papel administrativo, defaults, versões e recusa de ativação incompleta.
  - Evidência 2026-09-05: endpoints administrativos leem, preparam, ativam e suspendem a política; o inventário expõe requisitos ausentes, a ativação revalida configuração/modelos e agenda backfill explícito paginado, enquanto suspensão/mudança ativa agenda purge. Auditoria contém somente IDs/geração. Ruff passou e política/purge aprovaram `7 passed` (incluindo rota anônima recusada, kill switch desligado, versões, ativação, suspensão idempotente e troca de modelo).
- [x] 5.2 Implementar status agregado com progresso real `prontas/total`, fila, processamento e falhas, além de retentativa administrativa seletiva sem expor vetores, scores ou PII; testar paginação, contagens, falhas parciais e idempotência.
  - Evidência 2026-09-05: jobs de índice agora registram modelo, versão de qualidade e fingerprint, permitindo que o painel conte a última execução compatível por foto, inclusive quando zero rostos gera índice vazio. O endpoint agregado expõe prontas/total, fila, processamento, não indexadas e falhas paginadas só com categoria sanitizada; retry é seletivo, escopado e idempotente. Ruff passou e status/jobs/modelos/migration aprovaram `14 passed`, incluindo versão obsoleta fora da prontidão.
- [x] 5.3 Integrar ação de revogação/eliminação e prova de limpeza ao lifecycle existente sem apagar pedidos, seleções ou mídia histórica; testar galeria pública removida com privada preservada.
  - Evidência 2026-09-05: revogação administrativa é idempotente, desativa a política e agenda purge; o endpoint de prova conta somente embeddings, candidatas, referências e notificações pendentes. O purge está acoplado à remoção operacional pública antes da exclusão de fotos e não alcança pedidos, seleções ou mídia histórica/privada preservada. Ruff passou e política/purge aprovaram `8 passed`, incluindo prova antes/depois, revogação repetida e origem/foto preservadas pelo purge isolado.

## 6. Consulta e autorização da cliente

- [x] 6.1 Implementar disponibilidade, aviso/consentimento versionado e criação de request por cliente autenticada e vinculada, com declaração adulta/infantil; testar ausência de vínculo, aviso obsoleto, política suspensa e fluxo infantil bloqueado sem controles.
  - Evidência 2026-09-05: rotas da Galeria pública repetem sessão/vínculo no backend, disponibilidade mantém seleção manual, expõe versões/retenção e progresso real, e o upload JPEG cria request/job duráveis com referência cifrada. Consentimento divergente e declaração infantil falham antes da gravação; busca sozinha cria zero privadas/memberships. Ruff passou e busca/configuração aprovaram `7 passed`; junto à regressão de autoridade pública foram `4 passed`, cobrindo sessão/vínculo ausentes e modos protegidos.
- [x] 6.2 Implementar processamento durável da referência com snapshot do índice, estados/contagens reais `waiting_index|validating_reference|searching|ranking`, consulta por galeria e persistência somente de foto/rank/banda autorizados; testar saída da tela, retomada, nenhum rosto, múltiplos rostos, baixa qualidade, índice incompleto, nenhum candidato e falha técnica.
  - Evidência 2026-09-05: cada request congela IDs e fingerprints mínimos antes do upload ser aceito; o worker retoma por lease em outra sessão, espera apenas itens pendentes, compara estritamente os fingerprints do snapshot e persiste somente foto/rank/banda. A referência é eliminada em estados terminais e erro técnico é sanitizado. Ruff passou e `test_facial_search_worker.py` aprovou `7 passed`, cobrindo retomada sem UI, quatro resultados normativos, foto posterior excluída, índice incompleto e falha terminal.
- [x] 6.3 Implementar leitura, cancelamento/exclusão e rejeição de candidata com autorização repetida e expiração; testar troca de `request_id`, `gallery_id`, `photo_id` e cliente sem revelar existência.
  - Evidência 2026-09-05: leitura, cancelamento e rejeição repetem o escopo composto cliente/request/Galeria pública, registro ativo, lifecycle, disponibilidade e prazo; IDs trocados recebem somente indisponibilidade genérica. Cancelar elimina referência/candidatas e invalida job pendente de modo idempotente. Ruff passou e a suíte direcionada de busca aprovou `15 passed`, incluindo trocas de quatro dimensões de autoridade.
- [x] 6.4 Reutilizar a mutation de seleção manual para candidatas e provar por testes de banco que busca sem seleção cria zero privadas/vínculos/pedidos, enquanto a primeira seleção cria ou reutiliza exatamente uma privada e atualiza a cotação existente.
  - Evidência 2026-09-05: a rota de seleção de candidata apenas revalida o resultado e chama `derive_client_selection`, devolvendo o mesmo carrinho/cotação da seleção pública manual. Busca concluída isolada mantém zero privadas e vínculos; duas seleções da mesma candidata resultaram em exatamente uma privada, um membership e uma seleção. As regressões de seleção pública/cotação passaram (`3 passed`).

## 7. Retenção, auditoria e mensagens

- [x] 7.1 Implementar exclusão automática da referência/embedding em todo estado terminal ou 15 minutos e expiração de candidatas em 24 horas; testar relógio, crash entre etapas, repetição da limpeza e ausência de dado biométrico residual.
  - Evidência 2026-09-05: estados terminais apagam a referência antes da confirmação; jobs `cleanup` independentes recuperam expiração após crash, removem referência em até 15 minutos e candidatas em até 24 horas e adiam sem varredura ativa até o próximo prazo. Purge físico elimina arquivos, envelopes e índices de origem inválida de modo idempotente. Ruff passou e busca/retention/purge aprovaram os casos de relógio, arquivo já eliminado, repetição e ausência residual.
- [x] 7.2 Cobrir política, consentimento, consulta, feedback, revogação e purge com auditoria minimizada em UTC; testar que logs, traces, métricas e `AuditEvent` não contêm imagem, vetor, score, landmarks, caixa, nome inferido ou telefone.
  - Evidência 2026-09-05: política, consentimento, conclusão, cancelamento, rejeição, retenção, revogação e purge gravam somente evento, UUIDs, versão/estado, contagem e motivo sanitizado usando timestamps UTC do modelo. A suíte direcionada inspeciona todos os `facial.*` e rejeita imagem, embedding, score, landmarks, caixa e telefone; payloads públicos e de status também excluem campos internos.
- [x] 7.3 Implementar outbox idempotente de conclusão `ready|no_candidates|failed` e consumo pelo canal transacional configurado mesmo sem UI ativa, com cancelamento antes do envio; testar retry/callback duplicado, canal indisponível e payload/link sem biometria ou nova capacidade de acesso.
  - Evidência 2026-09-05: conclusão cria outbox única por request/resultado com conteúdo neutro cifrado; o consumidor revalida request, cliente, vínculo e canal WhatsApp configurado, limpa o payload após envio e não repete entrega ambígua. Cancelamento/revogação invalidam pendências. Os testes cobrem idempotência, envio único, canal/transiente, lease vencido, link sujeito à sessão e ausência de rosto, score, identidade, contagem ou telefone no payload persistido.
- [x] 7.4 Notificar o fotógrafo na interface administrativa após OTP concluído em contexto de galeria, reutilizando cliente por telefone e deduplicando por desafio consumido; testar acesso público/privado, cliente preexistente, repetição técnica e ausência de OTP/biometria no payload.
  - Evidência 2026-09-06: `client_verify` cria `client_logged_in:<challenge_id>` depois da autorização pública/privada e mantém o redirecionamento independente da leitura administrativa. A outbox reutiliza o cliente canônico, omite OTP/biometria e deixa envio externo `skipped`; a migration `20260906_0045` amplia o check preservando eventos existentes. Ruff passou e os fluxos público, cadastro por link, privado e migrations aprovaram `7 passed`.

## 8. Interfaces administrativa e cliente

- [x] 8.1 Adicionar tipos e cliente HTTP para os contratos faciais sem transportar campos internos; validar typecheck e testes de contrato contra respostas backend.
  - Evidência 2026-09-05: `facial-search-client.ts` tipa política, status, disponibilidade, request, progresso e candidatas, e mantém galeria/request/foto no path das mutations sem aceitar vetor, score ou imagem em resposta. `npx tsc --noEmit`, lint e três testes de contrato passaram.
- [x] 8.2 Construir painel administrativo de política, prontidão, progresso, suspensão, revogação e retentativa com linguagem clara e defaults desligados; testar estados vazio/carregando/erro/parcial e acessibilidade por teclado.
  - Evidência 2026-09-05: o painel integrado à etapa Imagens apresenta gates, prontas/total, fila, processamento, falhas, preparação, ativação, suspensão, revogação confirmada e retry seletivo, mantendo ativação indisponível quando incompleta. Quatro testes cobrem carregando, vazio/default desligado, parcial, erro sanitizado e ação por teclado; TypeScript e lint passaram.
- [x] 8.3 Construir ação `Procurar por reconhecimento facial`, consentimento destacado, upload mobile-first, progresso real/retomável, cancelamento e confirmação de exclusão automática sem guardar referência no navegador; testar fechamento/refresh, estados normativos e reenvio seguro.
  - Evidência 2026-09-05: a cliente envia JPEG com consentimento/declaracão explícitos, acompanha progresso real, pode fechar/retomar pelo único `request_id` em `sessionStorage`, cancelar/excluir e iniciar outra busca somente após limpeza bem-sucedida. A referência nunca é renderizada nem armazenada no navegador; estados terminais orientam sem bloquear seleção manual. Testes cobrem consentimento, retomada, foco/Escape e exclusão.
- [x] 8.4 Renderizar `Melhores resultados encontrados` e `Outros resultados encontrados` acima do acervo integral usando os cards, favoritos, seleção e cotação existentes; testar ordenação por rosto correspondente, deduplicação de estado, nova busca, desmarcar, retomada e ausência de segundo card/rota de galeria facial.
  - Evidência 2026-09-05: grupos `best|other` aparecem em uma região distinta antes das pastas e reutilizam as mesmas fotos, mutation de seleção, estado e resumo/cotação do acervo; rejeitar remove somente a candidata. Não existe rota ou card de galeria facial. A suíte pública validou ordenação, seleção, rejeição, desmarcação e retomada.
- [ ] 8.5 Validar responsividade, contraste, foco, leitores de tela e layouts com fotos horizontais/verticais em viewport móvel e desktop; quando usar imagens reais, limitar a evidência visual ao lote privado documentado e não versionar capturas identificáveis.
  - Parcial 2026-09-05: CSS usa grade responsiva de quatro, duas e uma colunas, preserva proporções horizontal/vertical, alvos móveis de 40 px e contraste explícito dos marcadores. Regiões, títulos, `aria-live`, progresso e diálogo têm nomes/descrições; modal recebe foco e fecha por Escape. Dezesseis testes de UI compartilhada/facial passaram, além de TypeScript e lint sem erros. Falta a evidência visual autenticada em viewport móvel/desktop, que só será executada após publicação autorizada com a feature ainda desligada para dados reais.
- [x] 8.6 Exibir somente na apresentação da cliente um diálogo acessível de direitos autorais ao interceptar menu de contexto, cópia, arraste ou `PrintScreen`, preservando a marca d'água e sem prometer bloqueio absoluto; testar texto legal, foco/fechamento, prevenção do evento e ausência do diálogo na prévia administrativa.
  - Evidência 2026-09-06: menu de contexto, cópia, arraste, `PrintScreen` e atalho de salvar abrem diálogo acessível somente para a cliente, com foco, Escape, texto informativo da Lei nº 9.610/98/art. 79 e aviso explícito de que o navegador não bloqueia screenshots de modo absoluto. A prévia administrativa não recebe o diálogo; testes compartilhados aprovaram eventos, texto e isolamento.
- [x] 8.7 Ampliar a proteção visual global com transparência, sombra, posição em nove pontos e linhas transversais opcionais, mantendo texto/tipografia/cor/tamanho/orientação e prévia ao vivo; persistir com migration aditiva, validar API/renderização e provar que somente `client_preview` é alterada e reenfileirada.
  - Evidência 2026-09-06: a configuração global passou a persistir opacidade, sombra, nove posições e grade transversal pela migration aditiva `20260906_0044`, preservando as opções anteriores. API, prévia ao vivo e renderizador usam os controles; a regressão de mídia comprova que salvar a proteção altera somente os bytes da `client_preview` e mantém `admin_preview` limpa. Frontend aprovou `143 passed`, build/TypeScript e lint sem erros; backend integral aprovou `341 passed, 1 skipped`.
- [x] 8.8 Remover da interface administrativa os controles de preparar/ativar/suspender/revogar política e apresentar somente indexação automática, progresso, falhas e retentativa; reforçar no consentimento da cliente o uso temporário da referência e sua exclusão, com testes de estados e acessibilidade.
  - Evidência 2026-09-06: o painel deixou de buscar/mutar política e informa processamento automático, `prontas/total`, fila, falhas e retry seletivo. O modal da cliente explicita que o checkbox autoriza somente o uso temporário daquela referência, a exclusão automática e a ação `Excluir busca`; a central administrativa ganhou o evento de login OTP. Vitest aprovou `14 passed` nos três fluxos direcionados e `tsc --noEmit` passou.
- [x] 8.9 Fazer a barra de `Processamento automático` → `Reconhecimento facial` da etapa 04 abranger, desde a persistência do upload, todas as fotos de conteúdo de todas as pastas da Galeria pública, sem contar ativos técnicos de capa; manter atualização enquanto houver foto aguardando preparo, na fila ou em processamento e testar agregação entre pastas, transição prévia → índice, conclusão e falha terminal sem polling infinito.
  - Evidência 2026-09-07: `gallery_index_status` passou a usar como denominador todas as fotos persistidas em pastas `content`, inclusive `available=false` durante a preparação, mantendo `cover_assets` fora; o endpoint expõe `waiting_previews` e trata ambiente facial válido sem política criada como pendente. A etapa 04 informa que agrega todas as pastas, mostra aguardando prévias/indexação, fila, processamento e falhas, recebe um refresh assim que o primeiro registro de cada novo upload é persistido e mantém polling somente enquanto houver trabalho acionável. Ruff passou; status/indexação/worker aprovaram `23 passed`; a suíte frontend isolada aprovou `160 passed`, e a validação focada aprovou `53 passed`, incluindo início de novo lote após painel estável, transição `0/636 → 636/636` e ausência de polling infinito quando resta somente falha terminal.

## 9. Segurança, integração e desempenho

- [x] 9.1 Executar testes negativos completos entre dois ambientes, galerias, clientes e membros, incluindo IDs válidos trocados; verificar zero vazamento em API, prévia, resultado, outbox e auditoria.
  - Evidência 2026-09-05: AEAD rejeita transplante entre ambientes, galerias, objetos e versões; busca vetorizada exclui sentinela idêntica de outro evento; leitura/rejeição/seleção/cancelamento recusam IDs válidos de outra galeria, cliente e foto, além de request divergente, sempre com indisponibilidade genérica. Constraints compostas impedem cruzamento no banco. Contratos públicos, outbox cifrada e todos os `AuditEvent facial.*` foram inspecionados sem imagem, vetor, score, landmarks, caixa, nome ou telefone; os testes direcionados passaram.
- [x] 9.2 Executar teste integrado automatizado de upload → prévia → índice → consentimento → filtro → seleção → privada/cotação → revogação/limpeza usando fixtures sintéticas; verificar que falha facial preserva a jornada manual.
  - Evidência 2026-09-05: `test_facial_integration.py` gera dois JPEGs abstratos/sintéticos, produz prévias, agenda e conclui índices, cria consulta adulta consentida, encontra candidata, só então deriva uma privada e calcula R$ 7,00. Nova referência sem rosto mantém a privada/seleção e permite seleção manual da segunda foto/cotação de R$ 14,00. Revogação e purge removem índice/candidatas e preservam as duas fotos, a privada e duas seleções (`1 passed`).
- [ ] 9.3 Medir worker local/ARM com limites de concorrência, lote autorizado de 500–1.000 JPEGs sintéticos ou reais de adultos e menores, carga simultânea de prévias e ciclo carregar/processar/descarregar; registrar throughput, CPU, memória, fila, progresso e backpressure sem persistir identificadores pessoais no relatório.
  - Parcial 2026-09-05: o spike já mediu 504 JPEGs sintéticos no AMD64 (3,525 fotos/s; RSS +89,6 MiB) e Oracle ARM64 físico limitado a 2 CPUs/1 GiB (14,362 fotos/s; RSS +132,7 MiB; consulta mediana 0,943 ms), sem violações. O runtime atual prova bloqueio ocioso, carregamento sob demanda, descarga e reciclagem; resta medir no ARM alvo a concorrência real com a fila de prévias antes de ativar o profile.
  - Bloqueio: o ensaio restante exige iniciar carga limitada no host ARM compartilhado e observar os containers da Markina; depende de inventário imediatamente anterior e autorização operacional específica, sem ampliar a autorização antiga do spike.
  - Preparação 2026-09-07: `scripts/benchmark-homolog-facial.py` oferece snapshot e monitor somente leitura, vinculados ao SHA/ARM/profile ativo, `BENCHMARK_AUTHORIZED_PRIVATE_FACIAL_HOMOLOG`, lote, autorização, quantidade exata e declaração sobre menores. O observador coleta apenas agregados de fotos, derivados, jobs, embeddings, CPU, memória, PIDs, disco, health e tempos, calcula throughput e termina somente após receber exatamente o lote autorizado e estabilizar a fila; não lê nomes, telefones, imagens, vetores ou scores. O inventário imediatamente anterior confirmou homologação no SHA `07aa6dffdcbeb70e3a0f7eba91cc66dfaa150961`, `20260906_0046 (head)`, `face-worker` saudável com 1 CPU/768 MiB/zero portas, menores desligados, 29% de disco e healthchecks externos 200. A medição do novo lote ainda depende da ativação manual aprovada e do upload pelo administrador/fotógrafo.
- [x] 9.4 Executar backend lint/typecheck/testes, frontend lint/typecheck/testes/build, OpenSpec estrito, gitleaks e `git diff --check`; investigar regressões antes de marcar a integração pronta.
  - Evidência 2026-09-05: Ruff aprovou `app` e `tests`; a suíte integral do backend aprovou `335 passed, 1 skipped` após reconciliar um teste legado que proibia qualquer rota facial, preservando o stub/endpoint antigo desabilitado. O backend não configura mypy, pyright ou ty no `pyproject.toml`, dependências ou CI, portanto não há typecheck Python separado aplicável. No frontend, ESLint encerrou com zero erros e 22 avisos preexistentes, `tsc --noEmit` passou, Vitest aprovou `142 passed` e o build Next de produção concluiu. Alembic confirmou `20260905_0043 (head)` e migration/modelos aprovaram `6 passed`; Compose validou o profile padrão e o profile facial opcional. OpenSpec estrito aprovou `27 passed, 0 failed`. Gitleaks 8.24.3 encontrou zero vazamentos nos 161 commits e zero no código-fonte/artefatos novos da árvore de trabalho; `git diff --check` passou apenas com avisos de normalização LF/CRLF.
  - Evidência complementar 2026-09-06: após separar análise limpa da apresentação protegida e ampliar a marca d'água, Ruff aprovou `app`, `tests` e a nova migration; backend integral aprovou `341 passed, 1 skipped`. Frontend aprovou lint com zero erros/22 avisos preexistentes, `143 passed` e build/TypeScript de produção. Os testes das políticas operacionais passaram, o workflow YAML foi parseado, OpenSpec estrito aprovou a change e `git diff --check` permaneceu limpo.
  - Evidência complementar 2026-09-06: após política automática e notificação de login, Ruff aprovou `app`, `tests` e `0045`; backend integral aprovou `345 passed, 1 skipped`. Frontend aprovou lint com zero erros/22 avisos preexistentes, `144 passed` e build/TypeScript de produção. OpenSpec aprovou a change e os 28 itens estritos; `git diff --check` permaneceu limpo. O Ruff aplicado indiscriminadamente às migrations históricas ainda aponta 14 problemas preexistentes de imports, fora do comando e do escopo do CI; nenhum pertence à revision nova.
- [x] 9.5 Executar integração de upload sem ação administrativa → política/índice automático → login OTP notificado → consentimento/busca isolada → resultado na própria Galeria pública → privada somente após seleção; validar que reinício não duplica índice, cliente nem notificação.
  - Evidência 2026-09-06: a integração sintética passou a iniciar sem `GalleryFacialPolicy`; os dois uploads criam automaticamente uma única política sem ator e dois índices, a busca adulta mantém zero privadas até a primeira seleção e preserva cotação, seleção manual e histórico após purge (`1 passed`). Em conjunto, os testes de política/indexação e autenticação provam reinício sem novo backfill, telefone canônico sem cliente duplicada e uma notificação por desafio OTP.

## 10. Operação e gates humanos

- [x] 10.1 Adicionar `face-worker` opcional ao Compose sem porta, scan recorrente ou CPU ativa em fila vazia, com limites/healthcheck/ociosidade, além de runbook de modelos, secrets, backup, migration, limpeza e rollback; validar `docker compose config` com flag desligada.
  - Evidência 2026-09-05: o profile `facial` adiciona `face-worker` sem `ports`, com 1 CPU/768 MiB, healthcheck, derivados read-only e referências exclusivas. O entrypoint real consome jobs duráveis e outbox, bloqueia em Redis, não carrega modelos para cleanup/purge e verifica manifesto no startup. O profile padrão mantém `FACIAL_PROCESSING_ENABLED=false`; `docker compose config --quiet` passou e exibiu somente o bind Nginx `8080`. Runtime aprovou `3 passed`; Ruff passou.
- [x] 10.2 Documentar RIPD operacional, matriz controlador/operador, textos versionados, direitos, fluxo de representação legal e critérios de calibração/equidade; manter produção e uso fora da homologação privada controlada bloqueados até revisão jurídica e humana registrada.
  - Evidência 2026-09-05: `privacy-operating-assessment.md` separa finalidades, papéis, minimização, retenção, transparência, direitos, revogação, riscos, controles infantis e critérios mensuráveis de calibração/equidade. Revisão 2026-09-07: o documento admite lote real de adultos e menores somente na homologação privada controlada e mantém produção e qualquer ampliação de finalidade sujeitas aos demais gates.
- [x] 10.3 Preparar inventário zero-impact e roteiro de homologação privada controlada; não executar migration, deploy, ativação ou carga real sem autorização humana específica posterior.
  - Evidência 2026-09-05: `homologation-plan.md` registrou projeto/arquivo Compose, subdomínio, única porta, serviços/volumes/redes preservados, migration aditiva, recursos do novo profile e roteiro adulto sintético com healthchecks, métricas, limpeza e rollback. Revisão 2026-09-07: o roteiro passou a admitir lote real de adultos e menores sob origem documentada, autorização explícita por execução, ambiente privado autenticado, criptografia, retenção mínima e exclusão controlada, sem alterar os limites zero-impact.
- [ ] 10.4 Após deploy autorizado, confirmar migration no head, flag desligada, containers/portas inalterados, `/healthz`, `/api/health`, smoke inicial sem dados pessoais, métricas e rollback; somente então executar a carga real autorizada e submeter experiência administrativa/cliente e ativação a revisão humana antes de sincronizar ou arquivar a change.
  - Parcial 2026-09-05/06: os commits funcionais `f608a22`, de correção do CI `39e93f2` e de proteção operacional `bd2bbd0` foram publicados em `develop`. O run CI `34002581333` aprovou backend (`335 passed, 1 skipped`), frontend, OpenSpec e gitleaks; seu deployment `6287710991`/job `101405083667` terminou com sucesso no SHA integral `bd2bbd0a11b687337a6fce47fceb5fbc3a62132b`. O log remoto confirmou migration `20260905_0043 (head) -> 20260905_0043 (head)`, `FACIAL_PROCESSING_ENABLED=false`, profile/`face-worker` inativo, containers saudáveis e somente o Nginx publicado em `127.0.0.1:8080`; o snapshot operacional registrou métricas do banco e mídia sem alterar dados. Externamente, `/healthz` e `/api/health` responderam 200; rotas faciais administrativas e públicas negaram acesso anônimo com 403 e o endpoint legado permaneceu 404. O smoke integrado com adultos sintéticos já está verde localmente e no CI; falta a validação visual autenticada móvel/desktop da task 8.5 antes de concluir esta task. Naquele run, dados reais e menores estavam fora do escopo autorizado.
  - Parcial 2026-09-06: o piloto sintético adulto foi ativado no SHA `11de2ee30e1d20980aadd574820eb99b812dd803`; migration, containers, portas e healthchecks foram reconfirmados. A sessão administrativa disponível para inspeção visual expirou e exibiu “Acesso administrativo indisponível”, portanto a revisão autenticada móvel/desktop e o teste humano continuam pendentes. Naquele run, a autorização cobria somente o lote sintético adulto.
  - Parcial 2026-09-07: após inventário e autorização explícita vinculada ao SHA, o run `34161635703`, job `101865163360` e deployment `6316179770` publicaram `10fbda094eb14306b294d04205f16d05b5d9c5b4` em `homolog`. Backend, frontend, OpenSpec e gitleaks passaram; o deploy confirmou migration no `head`, `FACIAL_PROCESSING_ENABLED=false`, `face-worker` inativo e healthchecks internos/externos antes de concluir. Após a publicação, `/healthz` e `/api/health` responderam 200 e o deployment do ambiente aponta para o SHA integral autorizado. O administrador/fotógrafo confirmou em seguida a exclusão em massa bem-sucedida das 636 fotos. A ativação do novo lote privado, o benchmark ARM e a validação visual autenticada permanecem pendentes.
- [x] 10.5 Publicar e executar a ativação auditável do piloto sintético adulto no ARM, gerando AEAD somente no host, iniciando apenas API/`face-worker`, mantendo zero porta nova e registrando inventário, healthchecks, SHA/run e rollback; impedir deploy comum enquanto o piloto estiver ativo.
  - Evidência 2026-09-06: após três falhas seguras com rollback (`34035862306`, `34036268125` e `34036841442`), foram corrigidos a origem dos pesos Git LFS, o vínculo ao `APP_ENV=staging` real e o empacotamento do manifesto público na imagem da API. O run `34037257484` concluiu CI, deployment `6293721237` e ativação `6293730176` no SHA integral `11de2ee30e1d20980aadd574820eb99b812dd803`. O ARM `aarch64` possui 4 CPUs e 24.564.524 KiB de memória; `face-worker` ficou saudável e limitado a 1 CPU, 768 MiB, concorrência 1 e zero portas. A API validou política sintética, AEAD e manifestos, com menores desabilitados; migration permaneceu em `20260905_0043 (head)`, somente Nginx continuou publicado em `127.0.0.1:8080`, e `/healthz` e `/api/health` externos responderam 200 após a ativação. O deploy comum agora falha fechado enquanto o piloto estiver ativo e o rollback isolado permaneceu disponível.
- [x] 10.6 Executar upgrade controlado do piloto sintético: inventariar, pausar somente `face-worker`/flag facial com backup restrito, publicar a correção, aplicar migration aditiva, reativar o piloto no mesmo gate e reconfirmar SHA, head, portas e healthchecks sem tocar em recursos de terceiros.
  - Evidência 2026-09-06: o run `34041768113` aprovou backend (`341 passed, 1 skipped`), frontend (`143 passed`/build), OpenSpec e gitleaks; seu deployment `6294710054` pausou o piloto com backup restrito e publicou o SHA integral `32e40a14c196a2ca3231a7f3dea54bfe85212d56`. A reativação `6294729003` confirmou `20260906_0044 (head)`, API e `face-worker` saudáveis, ARM64 com 4 CPUs/24.564.524 KiB, limite do worker em 1 CPU/768 MiB/concorrência 1/zero portas e menores desligados. Somente Nginx permaneceu publicado em `127.0.0.1:8080`; `/healthz` e `/api/health` externos responderam 200. A sessão administrativa mostrou os novos controles globais sem alterar dados.
- [ ] 10.7 Executar upgrade controlado do piloto privado com política automática e notificação de login, confirmar migration/head, backfill único, worker/portas/healthchecks e registrar SHA/runs; permitir o gate temporário de menores somente na execução e no lote expressamente autorizados e submeter a experiência autenticada ao teste humano.
  - Parcial 2026-09-06: a implementação funcional local aprovou backend `345 passed, 1 skipped`, frontend `144 passed`/build, OpenSpec `28 passed` e validações operacionais. Naquele momento, o push ficou bloqueado antes da mutação porque a autorização vigente cobria somente o piloto sintético e a reconciliação automática alcançaria galerias elegíveis existentes sem inventário comprovado; por isso foi exigida confirmação explícita do escopo ou autorização destrutiva para limpar galerias/clientes de teste preservando administrador, configurações e WhatsApp.
  - Parcial 2026-09-06/07: após autorização explícita, o run `34073273777` publicou `8a9891fa97beed17c98533a845ec1f7d353ec046`, aplicou `20260906_0046 (head)` e executou a limpeza autorizada com backup. A ativação facial falhou de forma segura porque a recriação isolada da API deixou o Nginx com o endereço interno anterior, resultando em 502 nos healthchecks; o rollback restaurou a flag/configuração facial e interrompeu o worker. A correção acionável é recarregar e validar o Nginx da própria Markina após qualquer recriação da API nesse runbook, sem reiniciar proxy, portas ou recursos de terceiros, e então repetir a ativação protegida.
  - Parcial 2026-09-07: a correção foi publicada no SHA `07aa6dffdcbeb70e3a0f7eba91cc66dfaa150961`; o run `34073819354`, deployment da aplicação `6300430858` e ativação `6300438039` terminaram verdes. O ARM confirmou `aarch64`, 4 CPUs, 24.564.524 KiB, migration `20260906_0046 (head)`, API e `face-worker` saudáveis, concorrência 1, limite de 1 CPU/768 MiB, zero portas no worker, `facial_enabled=true` e `minor_search_enabled=false`. Somente Nginx permanece em `127.0.0.1:8080`; `/healthz` e `/api/health` externos responderam 200. O backfill automático encontrou inventário vazio após a limpeza autorizada, portanto não processou imagens desconhecidas. Essa evidência descreve o gate anterior; a política revisada admite o próximo lote real controlado após implementação e validação dos novos gates.
  - Revisão de escopo 2026-09-07: a política e os gates locais agora admitem dados reais de adultos e menores somente na homologação privada, mediante autorização e registro operacional por execução. A execução no ARM, a experiência autenticada e o teste humano continuam pendentes.
  - Continuidade operacional 2026-09-07: após a autorização do lote `benchmark-636-20260907-01`, a primeira tentativa de iniciar `activate-private` não produziu run porque a branch padrão ainda não continha o workflow. A branch mínima `codex/register-facial-homolog-workflow` foi publicada no commit `db7f713`, adicionando somente esse YAML com blob idêntico ao validado em `develop`; a revisão humana aprovou a PR `#51`, mergeada em `main` no SHA `ccaeb72d29d6a6877c3e56d4430748c82df2af92`. O workflow está registrado, mas a ativação anterior não foi executada: o operador precisa dispará-la novamente e o upload continua bloqueado até conclusão verde do gate e início confirmado do monitor.
- [x] 10.8 Atualizar configuração, scripts e workflows de homologação para substituir os tokens exclusivos de adulto sintético por autorização vinculada a um identificador não pessoal do lote, habilitar menores somente durante a janela aprovada, coletar métricas agregadas, executar purge/prova de limpeza e restaurar o gate seguro ao final; cobrir recusa sem registro, escopo divergente e rollback.
  - Evidência 2026-09-07: o runtime valida ambiente privado, quatro referências opacas, lote de 500–1.000 itens, retenção de 1–72 horas, janela UTC vigente e correspondência exata do gate de menores; o worker interrompe ao expirar. `manage-homolog-facial.sh` implementa ativação e fechamento vinculados ao lote, backup/rollback restritos, manifesto `0600`, purge síncrono, prova agregada e restauração das três flags seguras. O workflow manual protegido substituiu a ativação automática por push e o benchmark exige lote/autorização/quantidade/menores idênticos ao runtime. Ruff, sintaxe Bash, YAML, Compose, políticas operacionais, teste dinâmico de entradas, `git diff --check` e OpenSpec estrito passaram; `test_facial_config.py` aprovou 9 casos locais e os testes focados em container aprovaram 14 casos. O conjunto adicional de config/policy/purge/runtime aprovou 23 casos e a integração sintética, repetida com a dependência vetorial usada no CI, aprovou 1 caso.
- [x] 10.9 Implementar a transição auditável do piloto legado para o primeiro deploy do fluxo privado: exigir trailer/token exatos, inventário, flag ligada, worker ativo e marcador privado desligado; fazer backup restrito, pausar somente flag/worker facial, recriar somente API/Nginx da Markina, validar rollback e impedir reativação automática, sem excluir fotos ou alcançar recursos de terceiros.
  - Evidência 2026-09-07: o trailer exato `Homolog-Facial: pause-legacy-for-private-upgrade` aciona antes do deploy somente o modo `pause-legacy-for-private-upgrade`, protegido por `PAUSE_LEGACY_FACIAL_FOR_PRIVATE_UPGRADE`. O script recusa marcador privado ativo, flag desligada ou worker ausente, registra inventário, salva backup `0600`, altera somente `FACIAL_PROCESSING_ENABLED=false`, para somente `face-worker`, recria somente a API, recarrega o Nginx da Markina e confirma healthchecks; falha restaura ambiente/API/worker. Não há reativação pós-deploy nem exclusão de fotos. O teste dinâmico detectou e corrigiu a necessidade de retornos explícitos nas precondições; Git Bash validou sintaxe e recusa, YAML foi parseado e `scripts/test_deploy_homolog.sh` concluiu com todas as políticas e mocks verdes. A validação final aprovou `388 passed, 1 skipped` no backend, `160 passed` no frontend e OpenSpec estrito nas duas changes relacionadas. A transição foi executada com sucesso no run `34161635703` antes da publicação do SHA `10fbda094eb14306b294d04205f16d05b5d9c5b4`; o job verde comprova que a pausa, o gate seguro pós-deploy e os healthchecks passaram sem reativação automática.
- [x] 10.10 Propagar o gate facial ao worker de mídia e recriá-lo junto da API em ativação, pausa, fechamento e rollback; implementar reconciliação corretiva idempotente, escopada ao manifesto do lote privado ativo, para fotos já prontas sem job, com inventário, contagem exata, validação de SHA/autorização/menores, testes e evidência agregada antes de retomar o benchmark.
  - Parcial 2026-09-07: a investigação do lote `benchmark-636-20260907-01` identificou que o Compose não repassava o bloco facial ao worker de mídia e que a ativação recriava somente a API; por isso as prévias concluíram com o gate antigo e não emitiram jobs, enquanto o reconciliador único do `face-worker` já havia executado antes do upload. A correção local compartilha o ambiente facial com o worker, espera job de mídia em andamento antes de recriá-lo nas futuras transições e verifica o gate efetivo em API/worker. O modo `reconcile-private` exige o mesmo SHA, lote, autorização, quantidade e declaração de menores do gate/manifesto ativos, seleciona exatamente as fotos de conteúdo persistidas dentro daquela janela, exige as duas prévias prontas e enfileira pela idempotência normal sem reiniciar worker, alterar mídia ou ampliar o prazo. Sintaxe Bash, recusas dinâmicas, política estrutural, YAML, Compose, `git diff --check`, compilação dos heredocs Python e OpenSpec estrito passaram. A suíte Python local ficou indisponível porque o venv referencia um Python 3.12 removido; o CI completo é gate obrigatório antes da operação. A reconciliação no ARM continua pendente de inventário imediatamente anterior e autorização operacional explícita.
  - Parcial 2026-09-07: o primeiro CI da reconciliação, run `34168242115`, preservou homologação porque o gate frontend detectou uma regressão preexistente e reproduzível no foco do OTP do painel PIX; OpenSpec e gitleaks passaram e os jobs remotos foram ignorados. O campo de confirmação recebeu `autoFocus`, alinhado ao teste e ao requisito acessível já existente. Localmente, o teste direcionado aprovou `3 passed`, a suíte frontend aprovou `160 passed` e o build/TypeScript de produção concluiu; a repetição do CI permanece obrigatória antes da reconciliação.
  - Parcial 2026-09-07: o segundo CI, run `34169811270`, aprovou backend, frontend, OpenSpec e gitleaks e ignorou corretamente o deploy comum. A reconciliação recusou antes do inventário e de qualquer foto porque os trailers informavam 636, enquanto o log da ativação autorizada `34165078082` comprova `EXPECTED_COUNT=637`; o gate permaneceu intacto. Após correção documental dos trailers para 637, o terceiro CI `34170260615` aprovou novamente todos os gates, excluiu o deploy comum e inventariou o ARM, mas comprovou `lote persistido divergente: 636/637` antes de enfileirar. A sequência operacional informada pelo administrador confirma a causa: das 637 fotos originais, uma foi excluída isoladamente e o novo upload reutilizou as 636 restantes. A reconciliação passa a exigir os valores registrado (637) e observado/autorizado (636), comprovar as 636 fotos e prévias na mesma janela e registrar a correção sem alterar lote, autorização, menores, retenção ou vencimento.
  - Evidência 2026-09-07: o run `34171451074` aprovou backend, frontend, OpenSpec e gitleaks, omitiu o deploy comum e, após aprovação protegida do Environment, reconciliou no SHA publicado `10fbda094eb14306b294d04205f16d05b5d9c5b4` exatamente 636 fotos prontas. A prova agregada registrou `photos=636` e `job_states.queued=636`, corrigiu somente a quantidade 637→636 no ambiente/manifesto e preservou lote, autorização, menores, retenção e vencimento. O benchmark `34165381143` observou 636 prévias concluídas, 163 indexações completas, 473 jobs ainda enfileirados e zero falhas/cancelamentos; a janela expirou de forma fechada durante a fila, motivando a retomada separada da task 10.11.
- [ ] 10.11 Implementar retomada protegida de lote expirado com jobs faciais duráveis pendentes: exigir nova autorização e janela limitada, preservar todos os demais vínculos e retenção, comprovar cobertura exata sem falhas, atualizar ambiente/manifesto atomicamente, recriar somente processos Markina e retomar o benchmark desde o escopo original, com rollback, testes e evidência ARM agregada.
