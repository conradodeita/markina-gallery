## 1. Contratos e fundação

- [x] 1.1 Atualizar `INSTRUCOES_EXECUTOR_CLAUDE_CODE.md`, `DIRETRIZES_FRONTEND_MARKINA_GALLERY.md` e a Fase 7 do `ROADMAP_ARQUITETURA.md` para distinguir galeria anônima proibida de filtro na Galeria pública autenticada, preservando revisão do fotógrafo quando houver ampliação de acesso; verificar coerência textual com proposal, specs e design.
- [x] 1.2 Confrontar os contratos e o código de `consolidate-shared-private-galleries-and-progressive-sales`, registrar qualquer dependência ainda ausente e validar por testes existentes que biblioteca agrupada, seleção pública persistente e unicidade `Galeria pública + cliente` estão disponíveis antes do acoplamento.
  - Evidência 2026-09-05: `DerivedGalleryMembership` impõe `uq_membership_parent_client`, `PhotoSelection` inclui `client_id` na unicidade e `derive_client_selection` reutiliza o resolvedor transacional. Quatro regressões direcionadas de biblioteca agrupada, primeira seleção, isolamento e corrida passaram (`4 passed`); nenhuma dependência funcional ausente foi encontrada.
- [x] 1.3 Incorporar manifesto, atribuições e download verificado de YuNet/SFace em build facial separado, sem pesos no Git; testar falha fechada para hash, versão, arquitetura ou licença divergente.
  - Evidência 2026-09-05: `Dockerfile.face` usa manifesto fixado na revisão oficial `47534e2`, valida tamanho/SHA-256/licença/arquitetura e mantém os pesos fora do Git; Ruff passou e `test_facial_model_assets.py` aprovou `3 passed`, incluindo bytes, versão, origem, arquitetura e licença divergentes.

## 2. Schema aditivo e configuração

- [x] 2.1 Modelar política, embeddings, jobs, requests, candidatas e outbox facial com UUIDs, UTC, checks/FKs de escopo, estados, índices e defaults desativados; validar modelos SQLAlchemy sem segredo, imagem ou score persistido indevidamente.
  - Evidência 2026-09-05: as estruturas faciais aditivas foram modeladas com envelopes `LargeBinary`, snapshot mínimo por foto, FKs compostas e checks de estado/progresso; `Base.metadata.create_all` passou em SQLite, Ruff passou e `test_facial_models.py` aprovou `4 passed`.
- [x] 2.2 Criar migration Alembic aditiva no head vigente, com upgrade vazio e compatibilidade de rollback da aplicação; testar upgrade em banco limpo e existente, constraints entre galerias e ausência de ativação/backfill implícitos.
  - Evidência 2026-09-05: a revision `20260905_0043` cria somente as tabelas faciais (incluindo itens mínimos do snapshot) e a chave composta de escopo de foto, sem inserir política, embedding, job, consulta ou candidata. Ruff passou; `test_facial_migration.py` validou upgrade em banco limpo e em `0042` com foto preexistente preservada, FKs/uniques compostas e downgrade local. A validação conjunta com os modelos aprovou `6 passed`.
- [x] 2.3 Adicionar configurações documentadas para kill switch, versões jurídicas/modelo/calibração, AEAD, retenção, fila e recursos, mantendo valores seguros no `.env.example`; testar startup fail-closed e garantir ausência de segredos no Git/frontend.
  - Evidência 2026-09-05: `app.facial.config` centraliza flag global, vínculo ao `APP_ENV`, manifesto/modelos verificados, versões jurídicas e técnicas, chaveiro AEAD versionado, retenções máximas e limites de worker/upload. A feature permanece `false` no `.env.example`, menores continuam bloqueados e chaves não aparecem no `repr` nem no frontend. Ruff passou e os testes de configuração/modelos aprovaram `7 passed`, incluindo configuração incompleta, ambiente divergente, retenção insegura e gate infantil.

## 3. Criptografia e provedores isolados

- [x] 3.1 Implementar envelope AEAD versionado com AAD de ambiente/galeria/foto ou request/modelo, rotação explícita e erros sanitizados; testar round-trip, adulteração, troca de escopo e chave ausente.
  - Evidência 2026-09-05: `FacialCipher` usa AES-256-GCM, nonce aleatório e AAD canônica com ambiente, galeria, foto/request, finalidade, modelo e versão do dado; aceita chave anterior somente enquanto autorizada e faz rotação explícita para a chave ativa. Ruff passou e `test_facial_crypto.py` mais configuração aprovaram `12 passed`, cobrindo round-trip, adulteração, transplante entre cinco dimensões de escopo, chave removida e sanitização.
- [x] 3.2 Adaptar o provider YuNet/SFace validado no spike para detectar zero ou mais rostos do acervo e exatamente um na consulta, sem importar ferramentas de benchmark no runtime; testar dimensões, normalização e estados de qualidade com fixtures sintéticas.
  - Evidência 2026-09-05: o runtime isolado `app.facial.provider` carrega somente YuNet/SFace, aceita zero ou mais faces no acervo, normaliza exatamente 128 dimensões e exige uma única face tecnicamente utilizável na referência. Não há import de `scripts.face_spike`; Ruff passou e `test_facial_provider.py` aprovou `3 passed` com fixtures sintéticas de zero/uma/múltiplas faces, embedding inválido, desfoque, baixa confiança e rosto pequeno.
- [x] 3.3 Implementar armazenamento temporário cifrado por request com limites de path, bytes, pixels, formato e prazo; testar upload malformado, decompression bomb, cancelamento e limpeza idempotente sem sair do diretório autorizado.
  - Evidência 2026-09-05: `FacialReferenceStore` valida JPEG, bytes e pixels antes da gravação, trata `DecompressionBomb`, cifra conteúdo e localizador com AAD do request/galeria/modelo, usa escrita atômica e restringe nomes ao diretório dedicado. Ruff passou e a suíte conjunta de crypto/store aprovou `11 passed`, incluindo formato inválido, limites, transplante de locator, arquivo externo preservado e exclusão repetida.
- [x] 3.4 Implementar avaliador técnico OpenCV versionado por rosto correspondente (nitidez local, corte, tamanho/proeminência e pose), com bandas `best|other` não destrutivas; testar grupos, rosto periférico, desfoque e corte sem inferir atributos ou estética.
  - Evidência 2026-09-05: `assess_face_quality` classifica somente a face correspondente com nitidez local, enquadramento, tamanho relativo, proeminência no grupo e proxies geométricas de pose; retorna apenas `best|other`, mantendo indicadores detalhados destinados ao envelope cifrado e sem campo de idade, gênero, emoção, estética ou identidade. Ruff passou e provider/qualidade aprovaram `10 passed` com casos sintéticos de grupo, face pequena/periférica, desfoque, corte e pose.
- [ ] 3.5 Executar spike comparativo isolado do OFIQ no ARM com corpus sintético, licenças de código/pesos/dependências, throughput e memória; registrar decisão sem acoplar o runtime antes da aprovação dos gates.
  - Bloqueio 2026-09-05: a execução comparativa exige provisionar dependências OFIQ e medir no host ARM compartilhado. Nenhuma alteração nesse ambiente será feita sem inventário e autorização operacional específicos; o runtime principal permanece desacoplado do OFIQ e as tasks locais independentes continuam.

## 4. Jobs, indexação e purge

- [x] 4.1 Implementar repositório e dispatcher de jobs faciais com idempotência, lease, retentativa, prioridade e erros sanitizados; testar concorrência, retomada após crash e ausência de bloqueio da fila de mídia.
  - Evidência 2026-09-05: `FacialJobRepository` usa idempotency key, `FOR UPDATE SKIP LOCKED`, token/expiração de lease, prioridade, progresso e retentativa terminal; `FacialJobDispatcher` persiste antes do wake-up Redis e toca somente a fila facial. Ruff passou e `test_facial_jobs.py` aprovou `5 passed`, cobrindo despacho duplicado, reserva exclusiva, prioridade de purge, retomada após lease expirado, rejeição do worker antigo e erro sanitizado.
- [x] 4.2 Enfileirar indexação somente após `client_preview` pronta e política ativa, usando fingerprint de prévia/versões e backfill único paginado ao ativar; testar ausência de scan recorrente e que upload/publicação continua funcionando quando a face está desativada ou falha.
  - Evidência 2026-09-05: a geração de derivados dispara somente o evento da `client_preview` recém-pronta; elegibilidade exige foto disponível, política ativa e todas as versões iguais ao gate de ambiente. A idempotência inclui hash da prévia/versões e o backfill existe apenas como chamada administrativa paginada, sem loop recorrente. Ruff passou e os testes direcionados aprovaram `5 passed`, provando deduplicação, paginação explícita, política pendente ignorada e mídia publicada mesmo com configuração facial inválida.
- [x] 4.3 Implementar no `face-worker` a substituição atômica do índice e qualidade por foto/modelo/fingerprint e a comparação linear vetorizada apenas dentro da galeria; testar múltiplos rostos, zero rosto, troca de versão e sentinela de outro evento.
  - Evidência 2026-09-05: `replace_photo_index` revalida foto, prévia e política, calcula fingerprint, cifra embedding/indicadores por face e substitui o conjunto em savepoint; `search_gallery_index` decifra e compara vetorizadamente somente registros da galeria/versão/fotos disponíveis, reduz múltiplas faces por foto e ordena `best` antes de `other`. O executor consome o lease e persiste progresso/conclusão. Ruff passou e engine/jobs aprovaram `8 passed`, incluindo rollback em falha intermediária e sentinela idêntica de outra galeria ausente do resultado.
- [x] 4.4 Integrar purge prioritário a desativação, revogação, troca de modelo, exclusão de foto/galeria e limpeza reconciliadora; testar que índice e candidatas somem mesmo quando mídia/histórico comercial permanecem.
  - Evidência 2026-09-05: purge prioridade zero é enfileirado por suspensão/revogação, mudança de versão e foto; exclusões de foto/Galeria pública eliminam inferências antes dos registros operacionais, preservando mídia referenciada e histórico. O worker de purge não cancela o próprio lease, e o reconciliador remove índice/candidatas com política ou foto inválida sem reprocessar conteúdo. Ruff passou e `test_facial_purge.py` aprovou `4 passed`, incluindo repetição idempotente e origem/mídia preservadas.
- [x] 4.5 Implementar espera bloqueante, limite de jobs por processo e descarregamento dos modelos após ociosidade configurável; testar fila vazia sem reprocessamento/CPU ativa e retomada após reload.
  - Evidência 2026-09-05: `BlockingFacialWorker` consulta jobs duráveis, bloqueia em `BRPOP` quando vazio, carrega o provider somente sob demanda, descarrega após o timeout ocioso e recicla ao atingir o limite configurado por processo; não existe varredura de galeria no loop. Ruff passou e `test_facial_runtime.py` aprovou `2 passed`, provando espera vazia sem carregar modelo, descarga, novo carregamento e limite de jobs.

## 5. Política e operação administrativa

- [x] 5.1 Implementar leitura/preparação/ativação/suspensão da política facial com inventário de requisitos ausentes e auditoria sem biometria; testar papel administrativo, defaults, versões e recusa de ativação incompleta.
  - Evidência 2026-09-05: endpoints administrativos leem, preparam, ativam e suspendem a política; o inventário expõe requisitos ausentes, a ativação revalida configuração/modelos e agenda backfill explícito paginado, enquanto suspensão/mudança ativa agenda purge. Auditoria contém somente IDs/geração. Ruff passou e política/purge aprovaram `7 passed` (incluindo rota anônima recusada, kill switch desligado, versões, ativação, suspensão idempotente e troca de modelo).
- [x] 5.2 Implementar status agregado com progresso real `prontas/total`, fila, processamento e falhas, além de retentativa administrativa seletiva sem expor vetores, scores ou PII; testar paginação, contagens, falhas parciais e idempotência.
  - Evidência 2026-09-05: jobs de índice agora registram modelo, versão de qualidade e fingerprint, permitindo que o painel conte a última execução compatível por foto, inclusive quando zero rostos gera índice vazio. O endpoint agregado expõe prontas/total, fila, processamento, não indexadas e falhas paginadas só com categoria sanitizada; retry é seletivo, escopado e idempotente. Ruff passou e status/jobs/modelos/migration aprovaram `14 passed`, incluindo versão obsoleta fora da prontidão.
- [x] 5.3 Integrar ação de revogação/eliminação e prova de limpeza ao lifecycle existente sem apagar pedidos, seleções ou mídia histórica; testar galeria pública removida com privada preservada.
  - Evidência 2026-09-05: revogação administrativa é idempotente, desativa a política e agenda purge; o endpoint de prova conta somente embeddings, candidatas, referências e notificações pendentes. O purge está acoplado à remoção operacional pública antes da exclusão de fotos e não alcança pedidos, seleções ou mídia histórica/privada preservada. Ruff passou e política/purge aprovaram `8 passed`, incluindo prova antes/depois, revogação repetida e origem/foto preservadas pelo purge isolado.

## 6. Consulta e autorização da cliente

- [x] 6.1 Implementar disponibilidade, aviso/consentimento versionado e criação de request por cliente autenticada e vinculada, com declaração adulta/infantil; testar ausência de vínculo, aviso obsoleto, política suspensa e fluxo infantil bloqueado sem controles.
  - Evidência 2026-09-05: rotas da Galeria pública repetem sessão/vínculo no backend, disponibilidade mantém seleção manual, expõe versões/retenção e progresso real, e o upload JPEG cria request/job duráveis com referência cifrada. Consentimento divergente e declaração infantil falham antes da gravação; busca sozinha cria zero privadas/memberships. Ruff passou e busca/configuração aprovaram `7 passed`; junto à regressão de autoridade pública foram `4 passed`, cobrindo sessão/vínculo ausentes e modos protegidos.
- [x] 6.2 Implementar processamento durável da referência com snapshot do índice, estados/contagens reais `waiting_index|validating_reference|searching|ranking`, consulta por galeria e persistência somente de foto/rank/banda autorizados; testar saída da tela, retomada, nenhum rosto, múltiplos rostos, baixa qualidade, índice incompleto, nenhum candidato e falha técnica.
  - Evidência 2026-09-05: cada request congela IDs e fingerprints mínimos antes do upload ser aceito; o worker retoma por lease em outra sessão, espera apenas itens pendentes, compara estritamente os fingerprints do snapshot e persiste somente foto/rank/banda. A referência é eliminada em estados terminais e erro técnico é sanitizado. Ruff passou e `test_facial_search_worker.py` aprovou `7 passed`, cobrindo retomada sem UI, quatro resultados normativos, foto posterior excluída, índice incompleto e falha terminal.
- [x] 6.3 Implementar leitura, cancelamento/exclusão e rejeição de candidata com autorização repetida e expiração; testar troca de `request_id`, `gallery_id`, `photo_id` e cliente sem revelar existência.
  - Evidência 2026-09-05: leitura, cancelamento e rejeição repetem o escopo composto cliente/request/Galeria pública, registro ativo, lifecycle, disponibilidade e prazo; IDs trocados recebem somente indisponibilidade genérica. Cancelar elimina referência/candidatas e invalida job pendente de modo idempotente. Ruff passou e a suíte direcionada de busca aprovou `15 passed`, incluindo trocas de quatro dimensões de autoridade.
- [x] 6.4 Reutilizar a mutation de seleção manual para candidatas e provar por testes de banco que busca sem seleção cria zero privadas/vínculos/pedidos, enquanto a primeira seleção cria ou reutiliza exatamente uma privada e atualiza a cotação existente.
  - Evidência 2026-09-05: a rota de seleção de candidata apenas revalida o resultado e chama `derive_client_selection`, devolvendo o mesmo carrinho/cotação da seleção pública manual. Busca concluída isolada mantém zero privadas e vínculos; duas seleções da mesma candidata resultaram em exatamente uma privada, um membership e uma seleção. As regressões de seleção pública/cotação passaram (`3 passed`).

## 7. Retenção, auditoria e mensagens

- [x] 7.1 Implementar exclusão automática da referência/embedding em todo estado terminal ou 15 minutos e expiração de candidatas em 24 horas; testar relógio, crash entre etapas, repetição da limpeza e ausência de dado biométrico residual.
  - Evidência 2026-09-05: estados terminais apagam a referência antes da confirmação; jobs `cleanup` independentes recuperam expiração após crash, removem referência em até 15 minutos e candidatas em até 24 horas e adiam sem varredura ativa até o próximo prazo. Purge físico elimina arquivos, envelopes e índices de origem inválida de modo idempotente. Ruff passou e busca/retention/purge aprovaram os casos de relógio, arquivo já eliminado, repetição e ausência residual.
- [x] 7.2 Cobrir política, consentimento, consulta, feedback, revogação e purge com auditoria minimizada em UTC; testar que logs, traces, métricas e `AuditEvent` não contêm imagem, vetor, score, landmarks, caixa, nome inferido ou telefone.
  - Evidência 2026-09-05: política, consentimento, conclusão, cancelamento, rejeição, retenção, revogação e purge gravam somente evento, UUIDs, versão/estado, contagem e motivo sanitizado usando timestamps UTC do modelo. A suíte direcionada inspeciona todos os `facial.*` e rejeita imagem, embedding, score, landmarks, caixa e telefone; payloads públicos e de status também excluem campos internos.
- [x] 7.3 Implementar outbox idempotente de conclusão `ready|no_candidates|failed` e consumo pelo canal transacional configurado mesmo sem UI ativa, com cancelamento antes do envio; testar retry/callback duplicado, canal indisponível e payload/link sem biometria ou nova capacidade de acesso.
  - Evidência 2026-09-05: conclusão cria outbox única por request/resultado com conteúdo neutro cifrado; o consumidor revalida request, cliente, vínculo e canal WhatsApp configurado, limpa o payload após envio e não repete entrega ambígua. Cancelamento/revogação invalidam pendências. Os testes cobrem idempotência, envio único, canal/transiente, lease vencido, link sujeito à sessão e ausência de rosto, score, identidade, contagem ou telefone no payload persistido.

## 8. Interfaces administrativa e cliente

- [x] 8.1 Adicionar tipos e cliente HTTP para os contratos faciais sem transportar campos internos; validar typecheck e testes de contrato contra respostas backend.
  - Evidência 2026-09-05: `facial-search-client.ts` tipa política, status, disponibilidade, request, progresso e candidatas, e mantém galeria/request/foto no path das mutations sem aceitar vetor, score ou imagem em resposta. `npx tsc --noEmit`, lint e três testes de contrato passaram.
- [x] 8.2 Construir painel administrativo de política, prontidão, progresso, suspensão, revogação e retentativa com linguagem clara e defaults desligados; testar estados vazio/carregando/erro/parcial e acessibilidade por teclado.
  - Evidência 2026-09-05: o painel integrado à etapa Imagens apresenta gates, prontas/total, fila, processamento, falhas, preparação, ativação, suspensão, revogação confirmada e retry seletivo, mantendo ativação indisponível quando incompleta. Quatro testes cobrem carregando, vazio/default desligado, parcial, erro sanitizado e ação por teclado; TypeScript e lint passaram.
- [x] 8.3 Construir ação `Procurar por reconhecimento facial`, consentimento destacado, upload mobile-first, progresso real/retomável, cancelamento e confirmação de exclusão automática sem guardar referência no navegador; testar fechamento/refresh, estados normativos e reenvio seguro.
  - Evidência 2026-09-05: a cliente envia JPEG com consentimento/declaracão explícitos, acompanha progresso real, pode fechar/retomar pelo único `request_id` em `sessionStorage`, cancelar/excluir e iniciar outra busca somente após limpeza bem-sucedida. A referência nunca é renderizada nem armazenada no navegador; estados terminais orientam sem bloquear seleção manual. Testes cobrem consentimento, retomada, foco/Escape e exclusão.
- [x] 8.4 Renderizar `Melhores resultados encontrados` e `Outros resultados encontrados` acima do acervo integral usando os cards, favoritos, seleção e cotação existentes; testar ordenação por rosto correspondente, deduplicação de estado, nova busca, desmarcar, retomada e ausência de segundo card/rota de galeria facial.
  - Evidência 2026-09-05: grupos `best|other` aparecem em uma região distinta antes das pastas e reutilizam as mesmas fotos, mutation de seleção, estado e resumo/cotação do acervo; rejeitar remove somente a candidata. Não existe rota ou card de galeria facial. A suíte pública validou ordenação, seleção, rejeição, desmarcação e retomada.
- [ ] 8.5 Validar responsividade, contraste, foco, leitores de tela e layouts com fotos horizontais/verticais em viewport móvel e desktop; registrar evidência visual somente com imagens sintéticas adultas.
  - Parcial 2026-09-05: CSS usa grade responsiva de quatro, duas e uma colunas, preserva proporções horizontal/vertical, alvos móveis de 40 px e contraste explícito dos marcadores. Regiões, títulos, `aria-live`, progresso e diálogo têm nomes/descrições; modal recebe foco e fecha por Escape. Dezesseis testes de UI compartilhada/facial passaram, além de TypeScript e lint sem erros. Falta a evidência visual autenticada em viewport móvel/desktop, que só será executada após publicação autorizada com a feature ainda desligada para dados reais.

## 9. Segurança, integração e desempenho

- [x] 9.1 Executar testes negativos completos entre dois ambientes, galerias, clientes e membros, incluindo IDs válidos trocados; verificar zero vazamento em API, prévia, resultado, outbox e auditoria.
  - Evidência 2026-09-05: AEAD rejeita transplante entre ambientes, galerias, objetos e versões; busca vetorizada exclui sentinela idêntica de outro evento; leitura/rejeição/seleção/cancelamento recusam IDs válidos de outra galeria, cliente e foto, além de request divergente, sempre com indisponibilidade genérica. Constraints compostas impedem cruzamento no banco. Contratos públicos, outbox cifrada e todos os `AuditEvent facial.*` foram inspecionados sem imagem, vetor, score, landmarks, caixa, nome ou telefone; os testes direcionados passaram.
- [x] 9.2 Executar teste integrado de upload → prévia → índice → consentimento → filtro → seleção → privada/cotação → revogação/limpeza usando apenas adultos sintéticos; verificar que falha facial preserva a jornada manual.
  - Evidência 2026-09-05: `test_facial_integration.py` gera dois JPEGs abstratos/sintéticos, produz prévias, agenda e conclui índices, cria consulta adulta consentida, encontra candidata, só então deriva uma privada e calcula R$ 7,00. Nova referência sem rosto mantém a privada/seleção e permite seleção manual da segunda foto/cotação de R$ 14,00. Revogação e purge removem índice/candidatas e preservam as duas fotos, a privada e duas seleções (`1 passed`).
- [ ] 9.3 Medir worker local/ARM com limites de concorrência, lote de 500–1.000 JPEGs, carga simultânea de prévias e ciclo carregar/processar/descarregar; registrar throughput, CPU, memória, fila, progresso e backpressure sem habilitar dados reais.
  - Parcial 2026-09-05: o spike já mediu 504 JPEGs sintéticos no AMD64 (3,525 fotos/s; RSS +89,6 MiB) e Oracle ARM64 físico limitado a 2 CPUs/1 GiB (14,362 fotos/s; RSS +132,7 MiB; consulta mediana 0,943 ms), sem violações. O runtime atual prova bloqueio ocioso, carregamento sob demanda, descarga e reciclagem; resta medir no ARM alvo a concorrência real com a fila de prévias antes de ativar o profile.
  - Bloqueio: o ensaio restante exige iniciar carga limitada no host ARM compartilhado e observar os containers da Markina; depende de inventário imediatamente anterior e autorização operacional específica, sem ampliar a autorização antiga do spike.
- [x] 9.4 Executar backend lint/typecheck/testes, frontend lint/typecheck/testes/build, OpenSpec estrito, gitleaks e `git diff --check`; investigar regressões antes de marcar a integração pronta.
  - Evidência 2026-09-05: Ruff aprovou `app` e `tests`; a suíte integral do backend aprovou `335 passed, 1 skipped` após reconciliar um teste legado que proibia qualquer rota facial, preservando o stub/endpoint antigo desabilitado. O backend não configura mypy, pyright ou ty no `pyproject.toml`, dependências ou CI, portanto não há typecheck Python separado aplicável. No frontend, ESLint encerrou com zero erros e 22 avisos preexistentes, `tsc --noEmit` passou, Vitest aprovou `142 passed` e o build Next de produção concluiu. Alembic confirmou `20260905_0043 (head)` e migration/modelos aprovaram `6 passed`; Compose validou o profile padrão e o profile facial opcional. OpenSpec estrito aprovou `27 passed, 0 failed`. Gitleaks 8.24.3 encontrou zero vazamentos nos 161 commits e zero no código-fonte/artefatos novos da árvore de trabalho; `git diff --check` passou apenas com avisos de normalização LF/CRLF.

## 10. Operação e gates humanos

- [x] 10.1 Adicionar `face-worker` opcional ao Compose sem porta, scan recorrente ou CPU ativa em fila vazia, com limites/healthcheck/ociosidade, além de runbook de modelos, secrets, backup, migration, limpeza e rollback; validar `docker compose config` com flag desligada.
  - Evidência 2026-09-05: o profile `facial` adiciona `face-worker` sem `ports`, com 1 CPU/768 MiB, healthcheck, derivados read-only e referências exclusivas. O entrypoint real consome jobs duráveis e outbox, bloqueia em Redis, não carrega modelos para cleanup/purge e verifica manifesto no startup. O profile padrão mantém `FACIAL_PROCESSING_ENABLED=false`; `docker compose config --quiet` passou e exibiu somente o bind Nginx `8080`. Runtime aprovou `3 passed`; Ruff passou.
- [x] 10.2 Documentar RIPD operacional, matriz controlador/operador, textos versionados, direitos, fluxo de representação legal e critérios de calibração/equidade; manter ativação real bloqueada até revisão jurídica e humana registrada.
  - Evidência 2026-09-05: `privacy-operating-assessment.md` separa finalidades, papéis, minimização, retenção, transparência, direitos, revogação, riscos, controles infantis e critérios mensuráveis de calibração/equidade. O documento é explicitamente preliminar e mantém dados reais, menores e ativação bloqueados até revisões jurídica/humana e demais gates.
- [x] 10.3 Preparar inventário zero-impact e roteiro sintético de homologação sem crianças; não executar migration, deploy ou ativação sem autorização humana específica posterior.
  - Evidência 2026-09-05: `homologation-plan.md` registra projeto/arquivo Compose, subdomínio, única porta, serviços/volumes/redes preservados, migration aditiva, recursos do novo profile e roteiro adulto sintético com healthchecks, métricas, limpeza e rollback. Nenhuma migration, ativação ou implantação foi executada.
- [ ] 10.4 Após deploy autorizado, confirmar migration no head, flag desligada, containers/portas inalterados, `/healthz`, `/api/health`, smoke sintético, métricas e rollback; submeter experiência administrativa/cliente e ativação a revisão humana antes de sincronizar ou arquivar a change.
  - Em execução 2026-09-05: autorização específica concedida para commit, push, migration aditiva, deploy com flag desligada e smoke sintético; ativação real e uso de dados reais permanecem fora do escopo autorizado.
