## 1. Contratos da fronteira comercial

- [x] 1.1 Adicionar testes backend inicialmente falhos para `/library/purchases`, verificando que rascunhos e pedidos `awaiting_payment` sem comunicação não aparecem, enquanto `payment_reported` e `purchased` permanecem separados por cliente, pedido e galeria. Evidência: o contrato isolado falhou porque a resposta anterior continha exatamente o pedido `awaiting_payment` sem comunicação; rascunho e dados da outra cliente já permaneceram isolados.
- [x] 1.2 Adicionar testes frontend inicialmente falhos com carrinhos em duas galerias, verificando grupos, subtotais, total geral informativo, CTAs independentes e ausência de uma ação de checkout global. Evidência: o teste reproduziu a ausência da região `Carrinho` embora as duas seleções independentes existissem nos cards antigos.
- [x] 1.3 Cobrir em teste frontend a navegação `Carrinho`/`Compras`, a remoção de `Ver pedido`, a ausência de filtros e controles de seleção no histórico e o estado vazio sem compras comunicadas. Evidência: dois contratos falharam pela ausência da região `Compras`, pelo rótulo/destino legado `Ver pedido` e pelo estado vazio `Nenhum pedido`; os 11 testes anteriores continuaram passando.

## 2. Projeção autoritativa do histórico

- [x] 2.1 Inventariar consumidores de `/library/purchases` e ajustar somente a projeção autenticada da cliente para excluir estados anteriores à comunicação; verificar os testes de isolamento e compatibilidade focados. Evidência: a busca de consumidores encontrou somente a biblioteca cliente e testes; o endpoint agora seleciona apenas pedidos confirmados ou vinculados a uma `PaymentCommunication` da própria cliente, e os cinco testes focados de autorização, histórico removido e consultas em lote passaram.
- [x] 2.2 Verificar que a resposta bem-sucedida de `Informar pagamento` move o pedido da projeção de carrinho para a de compras sem esperar confirmação administrativa e sem alterar preço, PIX, comunicação ou transições persistidas. Evidência: o teste integrado comprova seleção `1` e histórico vazio antes/depois do checkout, seguido por seleção `0` e uma compra `payment_reported` imediatamente após a comunicação idempotente; o frontend também recarrega revisão, carrinho e comunicações após sucesso.

## 3. Carrinho global agrupado

- [x] 3.1 Reorganizar `/library` com uma seção `Carrinho` derivada das jornadas autorizadas, um card por galeria, subtotal e ação `Revisar carrinho`; verificar que seleções de galerias diferentes nunca são combinadas em um checkout. Evidência: teste com duas galerias valida dois cards, subtotais e dois destinos privados distintos, sem botão ou link de finalização global.
- [x] 3.2 Implementar quantidade e total geral apenas informativos, conter erro de cotação no grupo afetado e manter carregamento/erro dos carrinhos independente de `Compras`; verificar todos os estados em teste direcionado. Evidência: o total é rotulado como informativo e omitido quando uma cotação falha; teste mantém os dois CTAs e o subtotal válido enquanto exibe o erro somente no grupo afetado, inclusive com falha independente de `Compras`.
- [x] 3.3 Atualizar atalhos oficiais para `/library#cart` e preservar o destino específico de revisão por galeria, verificando rótulos visíveis, foco, responsividade e isolamento da identidade autenticada. Evidência: atalhos das galerias pública e privada apontam para `/library#cart`, os cards globais preservam `review_url?mode=review`, as seções possuem âncoras e margem de rolagem, e os 46 testes frontend direcionados passaram.

## 4. Histórico de compras essencial

- [x] 4.1 Renomear a seção e os destinos cliente para `Compras`/`Ver compra`, apontar para `/library#purchases` e remover o uso cliente de `Ver pedido` como entrada para a galeria privada completa; verificar por busca e testes de navegação. Evidência: a busca deixa `Ver pedido` somente no painel administrativo de pagamentos; a jornada cliente usa `Ver compra` com destino `/library#purchases`, coberto por teste.
- [x] 4.2 Simplificar os cards de compra para galeria, fotos, quantidade, valor, data, estado e ampliação; preservar a grade responsiva 4/2 por pedido e verificar ausência de filtros, seleção, favorito e fotos disponíveis nessa superfície. Evidência: o card exibe galeria/evento, quantidade, valor, data comunicada/confirmada e estado; a grade isolada por pedido permanece em quatro colunas no desktop e duas no mobile, com ampliação e sem filtros ou ações de seleção/favorito, verificada pelos testes frontend.

## 5. Validação e entrega

- [x] 5.1 Executar testes backend/frontend direcionados, ESLint dos arquivos tocados, Ruff quando aplicável, TypeScript sem emissão, build, OpenSpec estrito e `git diff --check`; registrar evidências e confirmar ausência de migration ou alteração em preço, PIX, WhatsApp, mídia e reconhecimento facial. Evidência: 46 testes frontend e 5 testes backend focados passaram; Ruff aprovou, ESLint terminou com zero erros e quatro avisos conhecidos de `no-img-element`, `tsc --noEmit` e `next build` passaram, a change foi validada em modo estrito e `git diff --check` não encontrou erro. O diff não contém migration nem toca preço, PIX, WhatsApp, armazenamento de mídia ou reconhecimento facial.
- [ ] 5.2 Revisar o diff, preparar inventário de impacto zero e, usando a autorização já fornecida, executar commit, push, PR, merge e deploy em homologação; acompanhar CI, healthchecks, `FACIAL_PROCESSING_ENABLED=true` e validar o fluxo autenticado desktop/mobile sem alterar serviços de terceiros.
