## 1. Inventário e contratos de regressão

- [x] 1.1 Reconciliar esta change com `add-payment-confirmation-notifications`, `improve-gallery-and-client-data-lifecycle`, `consolidate-shared-private-galleries-and-progressive-sales`, `productionize-facial-search` e `expand-fluid-layout-and-simplify-gallery-flow`; registrar conflitos e autoridade no design antes do código e verificar com busca reproduzível e validação OpenSpec estrita. Evidência: seção `Reconciliation with active changes — 2026-09-09` confronta autoridades e proibições; buscas nos artefatos ativos e `openspec validate ... --strict` aprovadas.
- [x] 1.2 Mapear modelos, endpoints, workers, queries e telas que tratam propriedade da foto, seleção, pedido, comunicação, expiração e métricas; registrar o inventário técnico e verificar que cada produtor/consumidor possui destino nesta mudança. Evidência: seção `Technical inventory — 2026-09-09` registra persistência, APIs, workers, lifecycle, facial, agregados, interfaces e migrations após busca reproduzível com `rg`.
- [x] 1.3 Adicionar testes backend inicialmente falhos que selecionem duas fotos pelas rotas reais e comprovem os mesmos contadores na Galeria pública e privada, incluindo seleção facial autorizada, pedido pendente, confirmação e correção. Evidência: `test_real_selection_routes_keep_public_and_private_counters_in_sync_through_payment` percorre seleção pública e facial real, checkout, comunicação e confirmação; os três agregados retornam `2`, e o teste falha somente no novo contrato de correção ainda ausente (`404`).
- [x] 1.4 Adicionar testes inicialmente falhos para propriedade privada, upload isolado, escopo facial, comunicação/confirmacão/correção, reabertura e separação da mesma cliente em duas galerias; verificar que os testes falham pelas capacidades ausentes e não por fixtures inválidas. Evidência: `test_private_media_payment_correction_and_reopening_contracts_start_missing` usa autenticação e galeria válidas e falha no primeiro endpoint privado ausente (`POST /admin/derived-galleries/{id}/folders`, `404`); os contratos seguintes cobrem upload exclusivo, correção idempotente e reabertura idempotente.

## 2. Persistência e migration aditiva

- [x] 2.1 Estender pasta e foto com escopo/proprietário privado coerente e constraints que impeçam vínculo cruzado, preservando a linhagem da Galeria pública; verificar modelos e testes de integridade SQLite/PostgreSQL. Evidência: `PhotoFolder`/`PhotoAsset` mantêm `parent_gallery_id`, recebem proprietário privado opcional e FK composta de escopo; `test_private_media_scope_constraints_reject_cross_gallery_ownership` aprova posições independentes e comprova rejeição de vínculo cruzado no SQLite. A validação PostgreSQL descartável permanece concentrada em 2.4.
- [x] 2.2 Modelar o histórico append-only de correção financeira e a solicitação idempotente de reabertura com ator, timestamps UTC, estados e chaves únicas; verificar concorrência, isolamento por galeria e ausência de PII desnecessária. Evidência: modelos `PaymentConfirmationCorrection`, `GalleryReopeningRequest` e `GalleryReopeningNotificationOutbox` não persistem PII de cliente; constraints únicas/parcial e `test_financial_correction_and_reopening_constraints_are_idempotent` aprovados.
- [x] 2.3 Criar migration Alembic aditiva que classifique registros existentes como públicos sem mover arquivos ou referências e adicione índices das projeções; verificar upgrade em banco vazio e legado, downgrade seguro sem dados novos e recusa documentada quando houver estado não representável. Evidência: revision `20260909_0052` preserva IDs/chaves da mídia legada com proprietário privado `NULL`, adiciona somente colunas/tabelas/índices e recusa downgrade quando há estado novo; `test_private_operations_migration_is_additive_and_empty_downgrade_is_safe` valida upgrade legado e downgrade vazio.
- [x] 2.4 Validar upgrade, rollback de aplicação e constraints em PostgreSQL descartável com fotos públicas referenciadas, mídia privada, pedidos e solicitações; verificar preservação de UUIDs, storage keys, snapshots e contagens. Evidência: PostgreSQL 16 descartável isolado revelou e permitiu corrigir o nome de índice acima de 63 caracteres; `upgrade head -> downgrade 0051 -> upgrade head` aprovou, confirmou as três tabelas operacionais e a coluna de proprietário. O teste legado/novo e as constraints de escopo preservam UUIDs, storage keys, snapshots e contagens; o container temporário foi removido após a prova.

## 3. Mídia própria da galeria privada

- [x] 3.1 Implementar APIs administrativas backend-driven para criar/listar/renomear/excluir pastas privadas e carregar JPEGs exclusivamente nelas, rejeitando IDs públicos ou de outra privada; verificar autorização, MIME, limite vigente, idempotência e isolamento. Evidência: rotas de pasta privada e rotas comuns de registro/importação operam pelo proprietário persistido, exigem admin, prefixo privado e JPEG válido; FK composta e testes de API/isolamento aprovados.
- [x] 3.2 Adaptar processamento, derivados, limpeza e lifecycle para o escopo privado sem duplicar pipeline; verificar prévias limpas/protegidas, remoção de EXIF/GPS, falhas, retentativa e preservação comercial. Evidência: a importação privada entra no `enqueue_derivatives`/`generate_derivatives` vigente, usa os três derivados sem metadados e a mesma remoção protegida; `test_private_upload_reuses_media_pipeline_without_entering_public_facial_scope` gera/publica/lista mídia real. Consultas de lifecycle público passaram a preservar proprietário privado.
- [x] 3.3 Propagar o escopo privado para jobs, índices e métricas faciais, excluindo essas fotos de snapshots de busca pública; verificar regressão facial pública e cobertura administrativa privada com processamento habilitado e desabilitado. Evidência: `FacialJob` e `PhotoFaceEmbedding` carregam `derived_gallery_id`; enqueue propaga o escopo; engine, backfill, snapshot, worker, leitura/autorização e status públicos exigem escopo público `NULL`; endpoint privado reporta progresso/cobertura e teste comprova totais público e privado isolados.
- [x] 3.4 Atualizar contratos de revisão da cliente e do admin para combinar referências públicas legitimamente selecionadas com mídia própria da privada, sem expor a origem técnica à cliente; verificar múltiplos membros com acervo comum e seleções individuais. Evidência: listagem, pastas, revisão, prévia, seleção e checkout combinam referência pública e proprietário privado; o contrato da cliente omite `ownership`, enquanto a administração recebe a distinção operacional. Testes reais comprovam acervo combinado e seleção privada individual.

## 4. Projeção autoritativa e correção dos contadores

- [x] 4.1 Implementar uma projeção comercial compartilhada por Galeria pública, privada e cliente para acervo, seleção, compra, pedido, comunicação, expiração e reabertura; verificar queries em lote, ausência de N+1 e separação entre galerias. Evidência: `app/commercial_projection.py` agrega acervo público referenciado + privado próprio, seleções, itens confirmados, pedidos/comunicações, prazo e reabertura por chave `(gallery_id, client_id)` em número constante de queries; teste de limite existente (`<=12`) aprovado.
- [x] 4.2 Migrar os endpoints dos cards públicos, membros privados e seleção individual para a projeção comum; verificar que duas seleções persistidas retornam `2` nas duas superfícies e que pedidos não confirmados não contam como compra. Evidência: os três endpoints consomem a mesma projeção; `test_real_selection_routes_keep_public_and_private_counters_in_sync_through_payment` aprova `2` seleções e `0` compras antes da confirmação.
- [x] 4.3 Cobrir transições de confirmação, correção, nova compra e remoção elegível com testes de agregados; verificar atualização imediata e preservação de pedidos históricos. Evidência: o contrato real aprova `2` compras após confirmação e retorno imediato a `0` após correção, preservando pedido/outbox/histórico append-only; agregação histórica sem galeria continua aprovada no teste de precedência comercial.

## 5. Vendas, pagamentos e mensagens

- [x] 5.1 Ampliar a consulta administrativa financeira para incluir seleções sem pedido, pedidos sem comunicação, comunicações, confirmados, não localizados e falhas de mensagem, agrupando a mesma cliente por galeria e itens por pasta; verificar paginação, filtros e isolamento. Evidência: o dashboard retorna `selections_without_order`, pedidos e estados completos, agrupa itens pelos snapshots de pasta e mantém paginação/filtros em consultas constantes; teste do dashboard aprovado com limite de oito statements.
- [x] 5.2 Manter confirmação disponível somente para comunicação `pending_review` e tornar a transação financeira independente do consumo da outbox; verificar idempotência, lock concorrente, auditoria e pedido confirmado mesmo com WhatsApp indisponível. Evidência: decisão usa lock, capability `can_decide`, commit transacional do status e outbox posterior independente; regressões de decisão/retry e worker aprovadas.
- [x] 5.3 Implementar correção explícita e idempotente de confirmação, preservando a decisão e entrega anteriores, reabrindo a mesma comunicação para revisão e sem criar outbox; verificar recálculo de compras/estatísticas e nova confirmação posterior. Evidência: endpoint append-only de correção usa chave idempotente, retorna a comunicação a `pending_review`, mantém entregas, não cria nova outbox e a projeção retorna compras de 2 para 0; confirmação posterior usa revisão da correção na idempotência.
- [x] 5.4 Expor template global e conteúdo de prévia no contexto financeiro sem permitir override por galeria; verificar edição pelo endpoint existente, aviso de efeito global e snapshot da mensagem efetivamente enviada. Evidência: dashboard inclui templates globais, aviso e atalho de edição; confirmação renderiza e persiste snapshot no pedido/outbox, e o worker prioriza esse conteúdo imutável.

## 6. Solicitação de reabertura e prazo

- [x] 6.1 Centralizar a validação de expiração em seleção, remoção comercial, checkout e finalização, preservando consultas e histórico; verificar que chamadas diretas e interface bloqueiam igualmente após o prazo. Evidência: rotas diretas de seleção/desseleção e checkout chamam a validação comum; a interface oculta a finalização e mantém consultas/pedidos após expiração.
- [x] 6.2 Implementar API idempotente para membro ativo solicitar reabertura somente após expiração e consultar seu estado, enfileirando aviso independente ao fotógrafo; verificar duplicidade entre membros, autorização e falha de WhatsApp. Evidência: GET/POST autenticados aceitam somente privada expirada, constraint parcial mantém um pendente por galeria e cria outbox independente; worker e teste de entrega preservam a solicitação.
- [x] 6.3 Implementar APIs administrativas para listar, aprovar com nova data futura ou recusar a solicitação sob lock; verificar que aprovação reabre todos os membros, recusa preserva congelamento e repetição não amplia prazo. Evidência: listagem em lote e decisão sob lock exigem prazo futuro; teste HTTP aprova, persiste o novo prazo da galeria compartilhada e impede nova solicitação enquanto aberta.
- [x] 6.4 Integrar reaberturas à projeção, cards e área financeira com estado e retentativa de aviso; verificar atualização cruzada sem combinar outra galeria. Evidência: projeção expõe `reopening_status`, cards mostram pendência e a área financeira lista/decide/reenvia aviso pelo ID da outbox, sempre chaveada pela galeria privada.

## 7. Interface administrativa

- [x] 7.1 Remover da ficha privada os blocos `Adicionar fotos da Galeria pública`, `Adicionar ao acervo privado` e o atalho `Carregar novos JPEGs na pública`, além da montagem por catálogo na etapa Clientes; verificar por teste de componente e busca de texto que nenhum caminho equivalente permaneça. Evidência: etapa Clientes cria privada vazia sem carregar catálogo; ficha diferencia seleções automáticas da cliente e upload próprio sem botões proibidos; criação administrativa com `photo_ids`, inclusão posterior e clonagem legada retornam `410`, enquanto a rota pública real continua criando seleção e referência `client`; cinco testes direcionados aprovados.
- [x] 7.2 Implementar na ficha privada pastas, upload múltiplo do dispositivo, progresso de prévias/indexação e operação de mídia própria, reutilizando componentes compartilhados e layout fluido; verificar vazio, sucesso, falha, retentativa, desktop e smartphone. Evidência: ficha cria pasta, registra múltiplos JPEGs sob `private/<gallery>/<folder>/`, usa import/publish comuns, exibe progresso do upload e índice facial com polling; teste de componente confirma source PUT e storage key privada.
- [x] 7.3 Substituir os rótulos por `Fotos no acervo privado`, `Fotos selecionadas` e `Fotos compradas`, recarregar projeções após mutações e ao retornar ao foco e mostrar status/ação de pagamento e reabertura; verificar acessibilidade e contadores reais. Evidência: cards públicos/privados usam os rótulos inequívocos, a ficha recarrega após mutações, polling e foco, e expõe estados comercial/reabertura; testes de componentes e projeção aprovados.
- [x] 7.4 Tornar `/admin/payments` a superfície `Vendas e pagamentos`, unificar navegação e redirecionar entradas duplicadas preservadas; verificar filtros, múltiplas galerias, confirmação, correção, template, reabertura e falha de mensagem. Evidência: navegação tem entrada única, `/admin/purchases` redireciona, e a área inclui seleções abertas, pedidos/pastas, decisões, correção, templates, reabertura e retentativas; testes direcionados aprovados.

## 8. Interface da cliente

- [x] 8.1 Atualizar galeria e revisão para consumir capabilities de expiração do backend, ocultar `Prosseguir` e impedir finalização quando congelada; verificar que pedidos, compras e entregas permanecem acessíveis. Evidência: `selection_open` desabilita mutações e remove o card flutuante/Prosseguir, enquanto apresentação e acompanhamento financeiro permanecem; teste expirado aprovado.
- [x] 8.2 Exibir `Solicitar novo prazo para seleção das fotos`, botão `Solicitar reabertura da galeria` e os estados solicitado/aprovado/recusado, sem duplicar requisição; verificar mobile, desktop, teclado, loading, erro e retomada após recarregar. Evidência: interface consulta o estado persistido, usa chave idempotente e feedback acessível, desabilita duplicidade pendente e retoma por GET; teste de componente envia a solicitação e confirma o estado.
- [x] 8.3 Verificar a jornada comercial completa da mesma cliente em duas galerias e várias pastas, incluindo seleção, pedidos separados, comunicação, confirmação com falha de WhatsApp, correção e nova confirmação. Evidência: `test_same_client_commercial_journey_stays_isolated_across_two_galleries_and_folders` percorre as rotas públicas reais, cria duas privadas e pedidos independentes, agrupa Cerimônia/Confraternização no primeiro pedido, mantém a confirmação após falha do WhatsApp, corrige sem nova outbox e reconfirma com chave de entrega distinta; contadores finais permanecem `2` e `1` compradas em seus respectivos eventos.

## 9. Qualidade, documentação e entrega

- [x] 9.1 Executar Ruff, suíte backend completa, lint/typecheck/testes/build frontend, migrations PostgreSQL, regressão facial, OpenSpec estrito, gitleaks e `git diff --check`; corrigir falhas relacionadas e registrar evidências verificáveis. Evidência: Ruff aprovado; os 465 testes backend foram percorridos em partições isoladas (`223 + 64 + 1 + 12 + 165`), com o entrypoint facial adicionalmente aprovado em `5/5` repetições; frontend aprovou `30` arquivos/`192` testes, lint com `0` erros e `23` avisos conhecidos, TypeScript e build Next.js; PostgreSQL 16 descartável aprovou o ciclo `head -> 0051 -> head`; `20260909_0052` é o único head; OpenSpec estrito e `git diff --check` aprovados; Gitleaks `8.24.3` examinou `216` commits/~`9 MB` sem vazamentos. Após o endurecimento final da origem privada, 145 testes afetados foram percorridos (três expectativas legadas corrigidas e reaprovadas), os cinco contratos críticos e Ruff passaram novamente; o ensaio Bash do deploy fica para o CI Linux porque esta estação Windows não possui `/bin/bash`.
- [x] 9.2 Atualizar documentação de arquitetura, operação, estados financeiros, correção, reabertura, mídia privada, rollback e compatibilidade; verificar que nenhum texto orienta reutilização manual de fotos públicas na privada. Evidência: `docs/OPERACOES-GALERIA-PRIVADA-E-VENDAS.md` documenta as duas origens legítimas (seleção automática da cliente e novo upload privado), projeção, estados, mensagens, reabertura, migration, compatibilidade e rollback.
- [ ] 9.3 Preparar inventário zero-impact de homologação com SHA, migration, serviços, portas, volumes, backup, estado facial e rollback; não executar deploy ou migration remota sem autorização humana explícita específica para esta entrega.
- [ ] 9.4 Após deploy autorizado, confirmar SHA/migration, healthchecks, upload privado, contadores, expiração/reabertura e Vendas e pagamentos em desktop/mobile; manter revisão humana, sincronização e arquivamento pendentes até aceite.
