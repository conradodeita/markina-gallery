## 1. Contratos de regressão e desempenho

- [x] 1.1 Adicionar testes frontend inicialmente falhos que atrasem `/api/library/purchases` e verifiquem que jornadas e ação principal aparecem assim que `/api/library` conclui, enquanto loading, erro e nova tentativa do histórico permanecem locais. Evidência: `library.test.tsx` manteve a página em `Carregando sua biblioteca` quando o histórico foi atrasado, reproduzindo o bloqueio do `Promise.all`.
- [x] 1.2 Adicionar testes frontend inicialmente falhos para rótulo visível da ação, expansão das fotos por pedido, isolamento entre dois pedidos, grade 4/2, ampliação individual, filtros comerciais, ausência da capa privada e variantes visuais de pagamento. Evidência: execução direcionada registrou quatro falhas comportamentais esperadas — ação ainda com classe `primary`, hero não opcional, contador comercial ausente e inexistência da grade por pedido — com os demais `26` testes passando.
- [x] 1.3 Instrumentar testes backend direcionados de `/library` e `/library/purchases` com várias galerias e pedidos, registrar quantidade de consultas e tempo diagnóstico observado, corrigir a menor duplicação/N+1 reproduzível e verificar que o contrato de consultas em lote passa sem threshold de relógio frágil. Evidência: teste com `1 -> 5` galerias/pedidos manteve `/library` em `15 -> 15` consultas e `/library/purchases` em `6 -> 6`; tempos locais observados foram `0,0461 -> 0,0184 s` e `0,0187 -> 0,0086 s`. Nenhuma alteração backend foi necessária.

## 2. Biblioteca progressiva e conferência de pedidos

- [x] 2.1 Separar os ciclos de requisição de jornadas e histórico com cancelamento no unmount, estado e retry próprios, adicionar o estado de rota `loading.tsx` e verificar que a primeira seção útil não aguarda miniaturas de pedidos. Evidência: efeitos independentes com `AbortController`, loading de rota e retry localizado implementados; `library.test.tsx` confirma jornada utilizável durante atraso/falha do histórico e nova tentativa sem perder a galeria.
- [x] 2.2 Migrar as ações principais da biblioteca e os atalhos oficiais para o componente de link acessível com prefetch, verificando por teste que texto, contraste, foco e nome acessível permanecem visíveis dentro do shell. Evidência: ação usa `MarkinaLink`, atalhos oficiais declaram prefetch e regra específica preserva texto branco; teste confirma classe primária, rótulo e prefetch.
- [x] 2.3 Substituir o visualizador sequencial principal por uma grade expansível dentro de cada pedido, usando prévias protegidas lazy, quatro colunas em desktop, duas no mobile e diálogo de ampliação; verificar separação financeira e ausência de `Anterior`/`Próxima` como única conferência. Evidência: `LibraryOrderCard` mantém uma grade por pedido, miniaturas lazy e diálogo individual; `8` testes da biblioteca passam, incluindo isolamento de dois pedidos e contrato CSS 4/2.

## 3. Galeria privada e estados comerciais

- [x] 3.1 Tornar o hero de `GalleryPresentation` opcional com padrão ligado e omiti-lo somente na galeria privada comercial, verificando que `Capa ainda não definida` desaparece dessa rota e a Galeria pública mantém sua capa. Evidência: `showHero` usa padrão `true` e somente a rota privada passa `false`; o teste compartilhado confirma ambos os estados.
- [x] 3.2 Migrar filtros e contadores principais para `commercial_state` com `Todas`, `Carrinho`, `Aguardando pagamento`, `Pagamento informado` e `Compradas`, verificando contagens e fotos exibidas para coexistência de todos os estados. Evidência: cenário com cinco estados confirma total `5`, quatro subtotais `1` e filtragem exclusiva da foto com pagamento informado.
- [x] 3.3 Aplicar espaçamento e variantes semânticas aos cards de acompanhamento conforme estado autorizado, preservando rótulo textual, ações e ordem dos pedidos; verificar distinção acessível sem depender somente da cor. Evidência: cards mantêm `StatusBadge` e rótulo, recebem classe por estado e CSS com gap de `18px`, borda/fundo próprios; `16` testes da galeria passam.

## 4. Validação focada e entrega controlada

- [x] 4.1 Executar apenas os testes frontend da biblioteca, galeria privada, apresentação e estados de pedido, além dos testes backend de projeção/consultas; investigar falhas relacionadas e ampliar a suíte somente se houver evidência de regressão compartilhada. Evidência: frontend direcionado `5` arquivos/`44` testes passou; backend direcionado `4` testes de biblioteca, histórico, isolamento e batching passou. Somente três warnings preexistentes de depreciação/namespace foram emitidos, sem evidência para ampliar às suítes completas.
- [x] 4.2 Executar ESLint nos arquivos tocados, TypeScript sem emissão, validação OpenSpec estrita e `git diff --check`, registrar resultados no `tasks.md` e revisar que o diff não inclui mudanças de biometria, preço, PIX, mídia, dados ou infraestrutura. Evidência: ESLint direcionado concluiu com `0` erros e apenas `8` avisos conhecidos de `<img>` para prévias autenticadas; `tsc --noEmit`, Ruff, OpenSpec estrito e `git diff --check` passaram. A revisão de escopo confirmou somente UI/testes/documentação, sem alteração de biometria, preço, PIX, mídia, banco, dados ou infraestrutura.
- [ ] 4.3 Preparar inventário zero-impact, SHA e roteiro autenticado de `/library` e galeria privada; solicitar autorização específica antes de push/merge/deploy e, somente após autorizada, validar healthchecks e o fluxo 4/2 em homologação sem alterar serviços de terceiros. Inventário e roteiro preparados; push, merge, deploy e validação remota aguardam autorização humana específica.
